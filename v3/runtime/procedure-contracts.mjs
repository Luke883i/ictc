import { readFile } from 'node:fs/promises';
const raw=JSON.parse(await readFile(new URL('../procedure-contracts-1-3.json',import.meta.url),'utf8'));const required=['id','code','label','purpose','archetype','entryPoints','stateModel','transitions','humanCheckpoints','accessPolicy','evidencePolicy','exit','metrics','ux','aiSupport','claimBoundary'],ids=new Set(),codes=new Set();for(const item of raw.procedures||[]){for(const field of required)if(item[field]==null)throw new Error(`Procedure contract ${item.id||'<unknown>'} missing ${field}`);if(ids.has(item.id)||codes.has(item.code))throw new Error(`Duplicate procedure contract ${item.id}/${item.code}`);ids.add(item.id);codes.add(item.code);if(!Array.isArray(item.transitions)||!item.transitions.length||item.transitions.some(t=>!t.id||t.from==null||t.to==null))throw new Error(`Procedure contract ${item.id} has non-executable transitions`);if(!Array.isArray(item.metrics)||item.metrics.some(m=>!m.id||!m.label||!m.drilldown||!m.claimBoundary))throw new Error(`Procedure contract ${item.id} has incomplete MetricSpec`);if(!item.aiSupport.allowed||!item.aiSupport.forbidden)throw new Error(`Procedure contract ${item.id} missing AI policy`);}if(ids.size!==7)throw new Error(`Expected seven business procedure contracts, got ${ids.size}`);const flattenStates=item=>[...new Set(Object.values(item.stateModel||{}).flat())],compat=item=>({...structuredClone(item),job:item.purpose,states:flattenStates(item),transitionSpecs:structuredClone(item.transitions),transitions:item.transitions.map(t=>t.id),access:structuredClone(item.accessPolicy),evidence:structuredClone(item.evidencePolicy),metricSpecs:structuredClone(item.metrics),metrics:item.metrics.map(m=>m.id)}),byId=new Map(raw.procedures.map(item=>[item.id,Object.freeze(compat(item))]));export function procedureContract(id){const item=byId.get(id);if(!item)throw Object.assign(new Error(`Contratto procedura non trovato: ${id}`),{status:404,code:'procedure-contract-not-found'});return structuredClone(item);}export function businessProcedureContracts(){return raw.procedures.map(item=>compat(item));}export function procedureContractProjection(){return{schemaVersion:raw.schemaVersion,releaseProfile:raw.releaseProfile,authority:raw.authority,commonSubstrate:structuredClone(raw.commonSubstrate||{}),procedures:businessProcedureContracts()};}
