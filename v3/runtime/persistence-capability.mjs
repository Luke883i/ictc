export const PERSISTENCE_CAPABILITY_VERSION='1.0.0';
export const PERSISTENCE_REQUIRED_METHODS=Object.freeze(['init','hasSnapshot','load','save','latestSubjectVersion','findSubjectVersion','findCommandResult','semanticHistoryCounts','close']);
export function persistenceCapabilityProjection(adapter){const methods=Object.fromEntries(PERSISTENCE_REQUIRED_METHODS.map(name=>[name,typeof adapter?.[name]==='function']));return{schemaVersion:PERSISTENCE_CAPABILITY_VERSION,authority:'structural-runtime-capability',methods,complete:Object.values(methods).every(Boolean),limitations:['Structural capability does not imply a specific backend, HA, or deployment durability.']};}
export function assertPersistenceCapability(adapter){const projection=persistenceCapabilityProjection(adapter);if(!projection.complete){const missing=Object.entries(projection.methods).filter(([,ok])=>!ok).map(([name])=>name);throw Object.assign(new Error(`Persistence capability incomplete: ${missing.join(', ')}`),{code:'persistence-capability-incomplete',details:{missing}});}return projection;}
