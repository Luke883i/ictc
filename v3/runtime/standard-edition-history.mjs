import { sha256 } from '../domain.mjs';
import { importStandardPack } from './standard-library.mjs';
export function ensureStandardEditionHistory(state){state.standardPackHistory||=[];return state;}
export function archiveAndImportStandardPack(state,input,actor){ensureStandardEditionHistory(state);const id=String(input?.id||'').trim().toLowerCase(),current=(state.standardPacks||[]).find(x=>x.id===id);if(current){const archived={...structuredClone(current),archivedAt:new Date().toISOString(),supersededByEdition:String(input?.edition||'').trim()||null},key=archived.sha256||sha256(archived);if(!state.standardPackHistory.some(x=>(x.sha256||sha256(x))===key))state.standardPackHistory.push(archived);}return importStandardPack(state,input,actor);}
export function standardEditionHistoryProjection(state,actor){ensureStandardEditionHistory(state);const records=(state.standardPackHistory||[]).map(x=>({id:x.id,edition:x.edition||null,issuer:x.issuer||null,sourceAuthority:x.sourceAuthority||null,sourceUrl:x.sourceUrl||null,sha256:x.sha256||sha256(x),importedAt:x.importedAt||null,archivedAt:x.archivedAt||null,supersededByEdition:x.supersededByEdition||null}));return{schemaVersion:'1.0.0',authority:'standard-edition-history',readOnly:actor?.role==='auditor',records,limitations:['Imported standard editions are retained as historical metadata/content snapshots instead of being destructively overwritten.','A newer edition creates review-needed work; it does not silently migrate prior applicability or mapping decisions.']};}
