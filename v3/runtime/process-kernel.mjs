import { readFile } from 'node:fs/promises';const source=JSON.parse(await readFile(new URL('../process-kernel.json',import.meta.url),'utf8'));const ROLE_IDS=new Set(['admin','user','auditor']),ARCHETYPES=new Set(['monitor-review','case-workflow','assurance-view','governance','registry-extension']);function clone(v){return structuredClone(v);}function fail(message,code='process-kernel-invalid'){const error=new Error(message);error.code=code;throw error;}function active(def){return def.service==='grc'&&def.kind==='service'?{...def,surface:true}:def;}export function validateProcessKernel(kernel=source){if(!kernel||typeof kernel!=='object')fail('Process kernel mancante');if(kernel.authority!=='runtime')fail('Il process kernel deve avere authority runtime');const ids=new Set(),codes=new Set();for(const d of kernel.processes||[]){if(!d?.id||ids.has(d.id))fail(`ProcessDefinition id non valido o duplicato: ${d?.id||'<missing>'}`);ids.add(d.id);if(d.code){if(codes.has(d.code))fail(`ProcessDefinition code duplicato: ${d.code}`);codes.add(d.code);}if(!ARCHETYPES.has(d.archetype))fail(`Archetipo non valido per ${d.id}: ${d.archetype}`);if(!d.label||!d.kind||!d.authority||d.authority.ai==null||!d.evidence||typeof d.evidence.bundle!=='boolean'||!d.decision||typeof d.decision.humanRequired!=='boolean')fail(`ProcessDefinition incompleta: ${d.id}`);const roles=Object.keys(d.roleModes||{});if(!roles.length||roles.some(r=>!ROLE_IDS.has(r)))fail(`Role mode non valido: ${d.id}`);}for(const d of kernel.processes||[])if(d.parent&&!ids.has(d.parent))fail(`Parent process inesistente: ${d.id} -> ${d.parent}`);for(const[id,r]of Object.entries(kernel.relations||{}))if(!id||!Array.isArray(r.from)||!r.from.length||!Array.isArray(r.to)||!r.to.length||!r.semantics)fail(`RelationDefinition incompleta: ${id}`);return{ok:true,processCount:ids.size,relationCount:Object.keys(kernel.relations||{}).length,archetypeCount:Object.keys(kernel.archetypes||{}).length};}validateProcessKernel(source);export function processDefinitions(){return clone(source.processes).map(active);}export function surfaceProcessDefinitions(){return processDefinitions().filter(x=>x.surface);}export function processDefinition(id){const item=processDefinitions().find(x=>x.id===id);if(!item)fail(`ProcessDefinition sconosciuta: ${id}`,'process-definition-not-found');return item;}export function relationDefinition(id){const item=source.relations[id];if(!item)fail(`Relazione fuori grammatica: ${id}`,'relation-not-in-grammar');return{id,...clone(item)};}export function assertRelation(id,fromType=null,toType=null){const r=relationDefinition(id);if(fromType&&!r.from.includes(fromType))fail(`Relazione ${id} non ammette from=${fromType}`,'relation-endpoint-invalid');if(toType&&!r.to.includes(toType))fail(`Relazione ${id} non ammette to=${toType}`,'relation-endpoint-invalid');return id;}export function processKernelProjection(){const p=clone(source);p.processes=processDefinitions();return p;}export function compileProcessRegistry(extraDefinitions=[]){const candidate=clone(source);candidate.processes.push(...clone(extraDefinitions));const validation=validateProcessKernel(candidate);return{...validation,definitions:candidate.processes.map(active),expectedCoreEdits:0};}
