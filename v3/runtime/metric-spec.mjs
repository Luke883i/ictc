import { procedureMetricSpecs } from './procedure-registry.mjs';
import { procedureAdapterIds } from './procedure-adapters.mjs';
export function metricSignal(procedureId,metricId,value,context,severity='neutral'){const spec=procedureMetricSpecs(procedureId).find(item=>item.id===metricId);if(!spec)throw Object.assign(new Error(`MetricSpec sconosciuta: ${procedureId}/${metricId}`),{code:'metric-spec-missing'});return{id:metricId,label:spec.label,value,severity,basis:{scope:context.scope,scopeRef:structuredClone(context.scopeRef||null),transactionAsOf:context.transactionAsOf||context.asOf,validAsOf:context.validAsOf||context.asOf,asOf:context.validAsOf||context.asOf,stateRevision:context.stateRevision??null,procedurePolicyVersion:context.procedurePolicyVersion||null,accessPolicyVersion:context.accessPolicyVersion||null,computationVersion:`${procedureId}:${metricId}@1`,denominator:spec.denominator||null,claimBoundary:spec.claimBoundary,drilldown:spec.drilldown}};}
export function metricCatalogProjection(){const ids=procedureAdapterIds();return{schemaVersion:'1.1.0',authority:'canonical-procedure-registry.metricSpecs',procedures:ids.map(id=>({id,metrics:procedureMetricSpecs(id)})),metrics:ids.flatMap(id=>procedureMetricSpecs(id).map(m=>({...m,procedureId:id})))};}
