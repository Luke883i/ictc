import { procedureWorklistProjection } from './procedure-worklist.mjs';
export function procedureSummaryProjection(state,actor={role:'admin',id:'system'},context={}){const worklists=procedureWorklistProjection(state,actor,context),rows=worklists.procedures.map(item=>({id:item.id,code:item.code,label:item.label,attention:item.counts.actionable,metrics:item.metrics})),attention=rows.reduce((sum,item)=>sum+Number(item.attention||0),0),healthy=rows.filter(item=>!item.attention).length,coverage=worklists.procedures.find(x=>x.id==='coverage');return{schemaVersion:'2.0.0',authority:'runtime-procedure-summary<-runtime-procedure-worklist',projection:worklists.projection,rows,counts:{processes:rows.length,healthy,attention,standardsInScope:Number(coverage?.extra?.standardsTracked||0)},policy:{enabledProcedureIds:rows.map(item=>item.id),historicalRecordsRetained:true,attentionAuthority:'procedure-worklist.counts.actionable'},worklists,limitations:['Attention is exactly the provider-local default actionable worklist count, not a compliance score.','Local metrics preserve process semantics, declare their drilldown basis and are never averaged into one maturity percentage.','Temporal metrics are evaluated against projection.validAsOf rather than an implicit wall clock.','Disabled procedures retain history and evidence but do not contribute to the current operational summary.']};}
