import assert from 'node:assert/strict';
import {assertPublicDemoConfiguration,assertPublicDemoRequestBoundary,publicDemoActor,publicDemoConfiguration,publicDemoRequested,PUBLIC_DEMO_DEFAULT_ROLE,PUBLIC_DEMO_ROLES,PUBLIC_DEMO_RUNTIME_BASENAME,PUBLIC_DEMO_SUITE} from './runtime/public-demo.mjs';
const valid=()=>({ICTC_PUBLIC_DEMO:'1',ICTC_DEMO_SUITE:'3.0',ICTC_RUNTIME_DIR:`/tmp/ictc/${PUBLIC_DEMO_RUNTIME_BASENAME}`,ICTC_ALLOW_NETWORK_BIND:'1'});
const permissions={admin:new Set(['read','manage-enterprise']),user:new Set(['read','contribute-grc']),auditor:new Set(['read','verify-integrity'])};
assert.equal(publicDemoRequested({}),false);assert.equal(publicDemoRequested(valid()),true);
const off=publicDemoConfiguration({});assert.equal(off.enabled,false);assert.equal(off.valid,true);assert.equal(off.readOnly,false);
const posture=publicDemoConfiguration(valid());assert.equal(posture.enabled,true);assert.equal(posture.valid,true);assert.equal(posture.suite,PUBLIC_DEMO_SUITE);assert.deepEqual([...posture.roles],['admin','user','auditor']);assert.equal(posture.defaultRole,PUBLIC_DEMO_DEFAULT_ROLE);assert.equal(posture.actorStrategy,'server-derived-by-role');assert.equal(posture.readOnly,true);assert.equal(posture.anonymous,true);assert.equal(posture.syntheticOnly,true);
for(const role of PUBLIC_DEMO_ROLES){const actor=publicDemoActor(permissions,valid(),role);assert.equal(actor.id,`local-${role}`);assert.equal(actor.role,role);assert.equal(actor.identityMode,'public-demo');assert.equal(actor.identityStrategy,'synthetic-demo-role');assert.deepEqual([...actor.permissions],[...permissions[role]]);}
for(const invalidRole of['root','ADMIN ','','unknown']){const actor=publicDemoActor(permissions,valid(),invalidRole);const expected=String(invalidRole).trim().toLowerCase()==='admin'?'admin':'auditor';assert.equal(actor.role,expected);assert.equal(actor.id,`local-${expected}`);}
for(const method of['GET','get','HEAD','head'])assert.equal(assertPublicDemoRequestBoundary({method},valid()).valid,true);
for(const method of['POST','PUT','PATCH','DELETE','OPTIONS','CONNECT','TRACE'])assert.throws(()=>assertPublicDemoRequestBoundary({method},valid()),e=>e.code==='public-demo-read-only'&&e.status===405);
for(const [name,value,code] of[['ICTC_DEMO_SUITE','2.2','public-demo-requires-suite-3-0'],['ICTC_RUNTIME_DIR','/tmp/ictc/runtime','public-demo-runtime-dir-required'],['ICTC_ALLOW_NETWORK_BIND','0','public-demo-network-opt-in-required'],['ICTC_IDENTITY_MODE','trusted-header','public-demo-identity-mode-must-be-unset'],['ICTC_TRUSTED_PROXY_SECRET','x'.repeat(64),'public-demo-proxy-secret-must-be-unset'],['ICTC_MULTI_TENANT','1','public-demo-single-tenant-only'],['ICTC_ALLOW_LOCAL_ACTOR_SWITCH','1','public-demo-actor-switch-forbidden'],['ICTC_ALLOW_LOCAL_TENANT_SWITCH','1','public-demo-tenant-switch-forbidden']]){const env=valid();env[name]=value;assert.throws(()=>assertPublicDemoConfiguration(env),e=>e.code===code);}
console.log(JSON.stringify({ok:true,suite:'public-demo-boundary',roles:PUBLIC_DEMO_ROLES,defaultRole:PUBLIC_DEMO_DEFAULT_ROLE,actorAuthority:'server-derived-by-bounded-role',clientActorIdAuthoritative:false,readOnly:true}));
