import assert from 'node:assert/strict';
import { mkdtemp, readdir, rm } from 'node:fs/promises';
import os from 'node:os';
import path from 'node:path';
import { Readable } from 'node:stream';
import { Store } from './store.mjs';
import { createContributionHandler } from './runtime/contributions.mjs';
import { createIncidentMarketHandler } from './runtime/incident-market-handler.mjs';

const root=await mkdtemp(path.join(os.tmpdir(),'ictc-replay-'));const store=await new Store(root).init();const permissions={admin:new Set(['read','contribute-source','report-incident','edit-own-incident','submit-own-incident','close-incident']),user:new Set(['read','contribute-source','report-incident','edit-own-incident','submit-own-incident'])};const actor={id:'local-user',role:'user',permissions:[...permissions.user]};
function request(body,commandId,url){const stream=Readable.from([Buffer.from(JSON.stringify(body))]);stream.method='POST';stream.url=url;stream.headers={'content-type':'application/json','x-ictc-command-id':commandId,'user-agent':'attachment-replay-check'};return stream;}
function response(){return{status:null,body:'',writeHead(status){this.status=status;},end(value=''){this.body+=value;}};}
const attachment={name:'proof.txt',mime:'text/plain',dataBase64:Buffer.from('same proof').toString('base64')};
try{const contribution=createContributionHandler({store,permissions});let res=response();await contribution(request({text:'materiale',attachments:[attachment]},'contribution-replay','/api/contributions'),res,'/api/contributions',actor);assert.equal(res.status,201);assert.equal((await readdir(store.attachmentsPath)).length,1);const first=store.snapshot().contributions.at(-1).attachments[0];assert.equal(first.trust.state,'recorded-unscanned');assert.equal(first.trust.reason,'scanner-not-configured');res=response();await contribution(request({text:'materiale',attachments:[attachment]},'contribution-replay','/api/contributions'),res,'/api/contributions',actor);assert.equal(res.status,200);assert.equal((await readdir(store.attachmentsPath)).length,1);const incident=createIncidentMarketHandler({store,permissions});const incidentBody={eventKind:'incident',operationalSeverity:'not-assessed',originalNarrative:'Evento osservato',awarenessAt:'2026-08-05T08:00:00.000Z',attachments:[attachment]};res=response();await incident(request(incidentBody,'incident-replay','/api/incidents/intake'),res,'/api/incidents/intake',actor);assert.equal(res.status,201);assert.equal((await readdir(store.attachmentsPath)).length,2);assert.equal(store.snapshot().incidents.at(-1).attachments[0].trust.state,'recorded-unscanned');res=response();await incident(request(incidentBody,'incident-replay','/api/incidents/intake'),res,'/api/incidents/intake',actor);assert.equal(res.status,200);assert.equal((await readdir(store.attachmentsPath)).length,2);console.log('attachment-replay-check: ok (valid JSON HTTP fixture + semantic route-bound replay + idempotent attachment storage + explicit trust state)');}finally{store.close();await rm(root,{recursive:true,force:true});}
