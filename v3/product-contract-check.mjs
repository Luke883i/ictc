import assert from 'node:assert/strict';
import { readFile } from 'node:fs/promises';
import { VERSION } from './version.mjs';
import { processDefinitions, surfaceProcessDefinitions } from './runtime/process-kernel.mjs';
const root=new URL('./',import.meta.url),contract=JSON.parse(await readFile(new URL('product-contract.json',root),'utf8')),release=JSON.parse(await readFile(new URL('v1-stable-release-contract.json',root),'utf8')),packageManifest=JSON.parse(await readFile(new URL('../package.json',root),'utf8')),server=await readFile(new URL('server.mjs',root),'utf8'),controller=await readFile(new URL('public/ui/controller.js',root),'utf8'),active=await readFile(new URL('public/ui/active-experience.js',root),'utf8'),language=await readFile(new URL('public/ui/business-language.js',root),'utf8');
assert.match(VERSION,/^\d+\.\d+\.\d+(?:-[0-9A-Za-z.-]+)?$/);assert.equal(packageManifest.version,VERSION);assert.equal(VERSION,'1.8.0');
assert.match(server,/PRODUCT_EDITION='V4\.1 Experimental · Enterprise Nexus'/);assert.match(server,/STABILITY_PROFILE='v4_experimental_stable'/);for(const token of['projected.grc=grc','projected.work=workProjection','projected.reviewInbox=canonicalReviewInbox','projected.insights=insightProjection','projected.processLandscape=processLandscapeProjection'])assert.ok(server.includes(token),token);
assert.match(controller,/state\.data\.experience\?\.product/);assert.match(active,/ictcExperience='stable-1'/);assert.match(active,/filter\(item=>item\.code&&item\.id!=='evidence'\)/);for(const forbidden of['installBusinessNexus','installProcessLandscape','installV3EntryPoint','installNoviceEntry','installExperiencePolish','installProcedureGuideLanguageBinding'])assert.ok(!active.includes(forbidden),forbidden);
assert.equal(contract.schemaVersion,'7.1.0');assert.equal(contract.productEdition,'V4.1 Experimental · Enterprise Nexus');assert.equal(contract.stabilityProfile,'v4_experimental_stable');assert.deepEqual(contract.permanentShell,['home','processes','proof']);assert.deepEqual(contract.services.map(x=>x.id),['monitoring','incidents','objects','coverage','actions','risks','assurance']);assert.deepEqual(contract.roles.map(x=>x.id),['admin','user','auditor']);assert.equal(contract.experienceMetrics.services,7);assert.equal(contract.orchestration.intentRouting.humanLaunchRequired,true);assert.equal(contract.orchestration.workQueue.aiRanking,false);assert.equal(contract.orchestration.aiMode.mandatoryBusinessAiCalls,0);assert.equal(contract.orchestration.aiMode.offPreservesBusinessProcesses,true);
assert.equal(release.releaseProfile,'1.0_stable');assert.deepEqual(release.permanentSurfaces,['home','processes','proof']);assert.equal(release.maxDisclosureLevels,5);assert.equal(release.processes.length,7);assert.equal(release.experience.fullProcessCatalogCopies,1);assert.equal(release.experience.homeFullCatalog,false);
for(const id of['objects','coverage','actions','risks','assurance'])assert.equal(surfaceProcessDefinitions().find(x=>x.id===id)?.service,'grc');assert.equal(processDefinitions().length,12);assert.ok(contract.roles.find(x=>x.id==='admin').permissions.includes('manage-grc'));assert.ok(contract.roles.find(x=>x.id==='user').permissions.includes('contribute-grc'));
for(const title of['Inventario di sistemi e oggetti','Controlli e copertura','Azioni correttive','Questionari e verifiche'])assert.ok(language.includes(title),title);
console.log(`product-contract-check: ok (runtime ${contract.productEdition}/${VERSION}; public profile ${release.releaseProfile})`);
