import assert from 'node:assert/strict';
import { readFile } from 'node:fs/promises';
import { VERSION } from './version.mjs';
import { processDefinitions, surfaceProcessDefinitions } from './runtime/process-kernel.mjs';
const root=new URL('./',import.meta.url),contract=JSON.parse(await readFile(new URL('product-contract.json',root),'utf8')),packageManifest=JSON.parse(await readFile(new URL('../package.json',root),'utf8')),server=await readFile(new URL('server.mjs',root),'utf8'),controller=await readFile(new URL('public/ui/controller.js',root),'utf8'),active=await readFile(new URL('public/ui/active-experience.js',root),'utf8'),entry=await readFile(new URL('public/ui/operational-entry.js',root),'utf8'),nexus=await readFile(new URL('public/ui/business-nexus.js',root),'utf8');
assert.match(VERSION,/^\d+\.\d+\.\d+(?:-[0-9A-Za-z.-]+)?$/);assert.equal(packageManifest.version,VERSION);assert.equal(VERSION,'1.8.0');
assert.match(server,/PRODUCT_EDITION='V4 Experimental · Enterprise Nexus'/);assert.match(server,/PRODUCT_NAME='ICTC Enterprise Nexus Compliance OS'/);for(const token of['projected.grc=grc','projected.work=workProjection','projected.reviewInbox=canonicalReviewInbox','projected.insights=insightProjection','projected.processLandscape=processLandscapeProjection'])assert.ok(server.includes(token),token);
assert.match(controller,/state\.data\.experience\?\.product/);assert.match(active,/installV3EntryPoint\(\)/);assert.match(active,/dataset\.ictcExperience='active-1'/);assert.match(active,/dataset\.ictcMarketExperience='enterprise-nexus-1'/);assert.match(entry,/id='v3OperationalHome'|root\.id='v3OperationalHome'/);assert.ok(nexus.includes('I processi di compliance gestiti da ICTC'));
assert.equal(contract.product,'ICTC Enterprise Nexus Compliance OS');assert.equal(contract.productEdition,'V4 Experimental · Enterprise Nexus');assert.deepEqual(contract.permanentShell,['home','processes','proof']);assert.deepEqual(contract.services.map(x=>x.id),['monitoring','incidents','objects','coverage','actions','risks','assurance']);assert.deepEqual(contract.roles.map(x=>x.id),['admin','user','auditor']);assert.equal(contract.experienceMetrics.services,7);assert.equal(contract.orchestration.intentRouting.maxCandidates,3);assert.equal(contract.orchestration.intentRouting.humanLaunchRequired,true);assert.equal(contract.orchestration.intentRouting.acceleratorOnly,true);assert.equal(contract.orchestration.workQueue.aiRanking,false);assert.equal(contract.orchestration.aiMode.mandatoryBusinessAiCalls,0);assert.equal(contract.orchestration.aiMode.offPreservesBusinessProcesses,true);
for(const id of['objects','coverage','actions','risks','assurance'])assert.equal(surfaceProcessDefinitions().find(x=>x.id===id)?.service,'grc');assert.equal(processDefinitions().length,12);assert.ok(contract.roles.find(x=>x.id==='admin').permissions.includes('manage-grc'));assert.ok(contract.roles.find(x=>x.id==='user').permissions.includes('contribute-grc'));
const legacy=['writeReceiptCoverage','aiTraceCoverage','humanAdoptionCoverage','leastPrivilegeProjectionCoverage','linkedEvidenceCoverage','rawInputPreservation','evidenceExportCoverage','semanticRatchetCoverage','decisionProjectionCoverage','epistemicProjectionCoverage','sameAsReadExportCoverage','grcProcessCoverage','humanHeatmapCoverage','boundedCoverageMetric','persistentTodoCoverage','progressiveDisclosureCoverage','unifiedWorkQueueCoverage','humanProcedureSelectionCoverage','procedureGuideCoverage','assistEnvelopeCoverage','reviewInboxCoverage','deterministicInsightCoverage','humanInsightValidationCoverage','procedureTraceCoverage','v3Saturation100kCoverage'];const v4=['businessProcessDiscoverability','directProcessEntryCoverage','aiOffProcessCompletionCoverage','globalSearchSubjectCoverage','verifiedActionClosureCoverage','riskTreatmentCoverage','residualRiskCoverage','evidenceTraceExplorerCoverage','profileSaturation1000Coverage','routingSaturation10000Coverage','v4StateSaturation100kCoverage','noNoveltyTail100Coverage'];for(const metric of[...legacy,...v4])assert.equal(contract.experienceMetrics[metric],1,metric);assert.equal(contract.experienceMetrics.maxPrimaryActionsPerContext,1);assert.equal(contract.experienceMetrics.roleLeakage,0);assert.ok(contract.experiencePrinciples.every(x=>x.length>25));assert.ok(contract.boundaries.some(x=>x.includes('V4 Experimental · Enterprise Nexus')));assert.ok(contract.boundaries.some(x=>x.includes('declared universe')));assert.ok(contract.boundaries.some(x=>x.includes('heatmap')));assert.ok(contract.boundaries.some(x=>x.includes('routing')));assert.ok(contract.boundaries.some(x=>x.includes('KPI')));assert.ok(contract.boundaries.some(x=>x.includes('production readiness')));console.log(`product-contract-check: ok (${contract.productEdition}, ${contract.services.length} business processes, runtime ${VERSION})`);
