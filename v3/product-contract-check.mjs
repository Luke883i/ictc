import { strict as assert } from 'node:assert';import { readFile } from 'node:fs/promises';import { VERSION } from './version.mjs';await import('./api-contract-check.mjs');const root=new URL('./',import.meta.url);const contract=JSON.parse(await readFile(new URL('product-contract.json',root),'utf8'));const dod=JSON.parse(await readFile(new URL('product-dod.json',root),'utf8'));const invariants=JSON.parse(await readFile(new URL('experience-invariants.json',root),'utf8'));const packageManifest=JSON.parse(await readFile(new URL('../package.json',root),'utf8'));const serverSource=await readFile(new URL('server.mjs',root),'utf8');const controllerSource=await readFile(new URL('public/ui/controller.js',root),'utf8');assert.match(VERSION,/^\d+\.\d+\.\d+(?:-[0-9A-Za-z.-]+)?$/);assert.equal(packageManifest.version,VERSION);assert.match(serverSource,/version:VERSION/);assert.match(serverSource,/projected\.experience\.release=VERSION/);assert.match(serverSource,/projected\.experience\.product='ICTC Control Tower'/);assert.match(controllerSource,/state\.data\.experience\?\.product/);assert.doesNotMatch(controllerSource,/Enterprise Workbench/);assert.equal(contract.product,'ICTC Control Tower');assert.equal(contract.productEdition,'V1 Experimental');assert.deepEqual(contract.permanentShell,['home','processes','proof']);assert.deepEqual(contract.services.map(item=>item.id),['monitoring','incidents']);assert.deepEqual(contract.roles.map(item=>item.id),['admin','user','auditor']);assert.equal(contract.controlPlane.id,'administration');assert.equal(contract.experienceMetrics.services,2);assert.equal(contract.experienceMetrics.roles,3);assert.equal(contract.experienceMetrics.permanentShellItems,3);assert.equal(contract.experienceMetrics.activeUiCompositions,1);assert.equal(contract.experienceMetrics.historicalRuntimeEnhancers,0);assert.equal(contract.experienceMetrics.processDefinitionCoreEditTarget,0);assert.ok(contract.experienceMetrics.monitoringMissionRequiredQuestions<=1);assert.equal(contract.experienceMetrics.incidentIntakeRequiredQuestions,2);for(const metric of ['writeReceiptCoverage','aiTraceCoverage','humanAdoptionCoverage','formulationVersionCoverage','monitoringLifecycleCoverage','aiRecoveryCoverage','reasonedDecisionCoverage','leastPrivilegeProjectionCoverage','linkedEvidenceCoverage','rawInputPreservation','evidenceExportCoverage','aiBudgetCoverage','userLifecycleCoverage','readinessCoverage','semanticRatchetCoverage','decisionProjectionCoverage','epistemicProjectionCoverage','translationPackBoundaryCoverage','sameAsReadExportCoverage'])assert.equal(contract.experienceMetrics[metric],1,metric);assert.equal(contract.experienceMetrics.maxPrimaryActionsPerContext,1);assert.equal(contract.experienceMetrics.roleLeakage,0);assert.ok(contract.experiencePrinciples.every(item=>item.length>25));assert.ok(contract.wowMechanisms.length>=7);assert.ok(contract.boundaries.some(item=>item.includes('non determina')));assert.ok(contract.boundaries.some(item=>item.includes('non verità')));assert.ok(contract.boundaries.some(item=>item.includes('V1 Experimental')));assert.ok(dod.criteria.length>=35);assert.ok(invariants.invariants.length>=18);for(const forbidden of ['reviewer','owner','analyst','viewer','tenant'])assert.ok(!contract.roles.some(role=>role.id===forbidden));console.log(`product-contract-check: ok (${contract.productEdition}, ${contract.services.length} services, ${contract.roles.length} roles, runtime ${VERSION})`);
