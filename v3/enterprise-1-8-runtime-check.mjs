import assert from 'node:assert/strict';
import { mkdir, writeFile } from 'node:fs/promises';
import { runtimeHarness } from './runtime-test-harness.mjs';
import { canonicalProcedureHub } from './runtime/workbench-projection.mjs';

const checks=[];const verify=(name,fn)=>{fn();checks.push(name);};
const ADMIN_IDS=['monitoring','incidents','evidence','objects','coverage','actions','risks','assurance','administration'];
const READ_IDS=['monitoring','incidents','evidence','objects','coverage','actions','risks','assurance'];
const pureState={missions:[{id:'m-active',state:'active'},{id:'m-needs-plan',state:'needs-plan'}],catalog:[{id:'s1',state:'candidate'}],contributions:[{id:'c-own',createdBy:'alice',state:'needs-enrichment'},{id:'c-foreign',createdBy:'bob',state:'needs-enrichment'}],incidents:[{id:'i-own',createdBy:'alice',state:'clarifying'},{id:'i-foreign',createdBy:'bob',state:'review'}],users:[{id:'a',status:'active'},{id:'u',status:'active'},{id:'x',status:'disabled'}],grcObjects:[],grcMappings:[],grcActions:[],grcRisks:[],grcAssurance:[]};
const pureReadiness={verified:1,total:3,controls:[{id:'human-authority',status:'verified',scope:'runtime'},{id:'ai-provider',status:'blocker',scope:'runtime'},{id:'tls',status:'blocker',scope:'deployment'}]};
const pureAdmin=canonicalProcedureHub(pureState,{id:'admin',role:'admin',permissions:['read','manage-enterprise']},{readiness:pureReadiness});
const pureUser=canonicalProcedureHub(pureState,{id:'alice',role:'user',permissions:['read','report-incident','contribute-source','contribute-grc']},{readiness:pureReadiness});
const pureAuditor=canonicalProcedureHub(pureState,{id:'audit',role:'auditor',permissions:['read']},{readiness:pureReadiness});
verify('procedure-hub-pure-roles',()=>{assert.deepEqual(pureAdmin.map(x=>x.id),ADMIN_IDS);assert.deepEqual(pureUser.map(x=>x.id),READ_IDS);assert.deepEqual(pureAuditor.map(x=>x.id),READ_IDS);assert.equal(pureAdmin.find(x=>x.id==='monitoring').attentionCount,2);assert.equal(pureUser.find(x=>x.id==='monitoring').attentionCount,1);assert.equal(pureUser.find(x=>x.id==='incidents').metrics[1].value,1);assert.equal(pureAuditor.find(x=>x.id==='incidents').metrics[1].value,2);assert.ok(['objects','coverage','actions','risks','assurance'].every(id=>pureAuditor.find(x=>x.id===id)?.readOnly===true));assert.equal(pureAdmin.find(x=>x.id==='administration').attentionCount,1);});
verify('procedure-hub-no-read-no-surface',()=>assert.deepEqual(canonicalProcedureHub(pureState,{id:'none',role:'user',permissions:[]},{readiness:pureReadiness}),[]));

const runtime=await runtimeHarness('ictc-enterprise-18-v2-regression');
try{
 await runtime.ok('PUT','/api/admin/settings',{organization:{name:'Enterprise Test',scope:'Cybersecurity UE',jurisdictions:['Italia','Unione europea']},llm:{endpoint:`http://127.0.0.1:${runtime.aiPort}/v1/chat/completions`,model:'mock-enterprise-18',apiKeyEnv:'ICTC_LLM_API_KEY',temperature:0.1}});
 const created=await runtime.ok('POST','/api/monitoring-jobs/draft',{jobName:'Novelty NIS2 e DORA',objective:'Individuare nuove fonti e modifiche ufficiali relative a NIS2 e DORA',cadenceHours:24,miningMode:'novelty',noveltyBaseline:'last-run',jurisdictions:['Unione europea','Italia'],authorities:['EUR-Lex','ACN'],changeTypes:['new-law','amendment','repeal','guidance','effective-date'],resultLimit:75,sourceHints:['https://eur-lex.europa.eu']});
 const mission=created.body.mission;
 verify('create-and-profile',()=>{assert.equal(created.status,201);assert.equal(mission.jobName,'Novelty NIS2 e DORA');assert.equal(mission.miningMode,'novelty');assert.equal(mission.noveltyBaseline,'last-run');assert.deepEqual(mission.jurisdictions,['Unione europea','Italia']);assert.deepEqual(mission.authorities,['EUR-Lex','ACN']);assert.equal(mission.resultLimit,75);assert.equal(mission.state,'draft');assert.equal(mission.planVersion,1);assert.equal(mission.planTrace?.purpose,'monitoring-plan');assert.ok(Array.isArray(mission.plan?.queries));});
 const bootstrap=await runtime.bootstrap('admin','test-admin');
 verify('admin-procedure-hub-v2',()=>{assert.deepEqual(bootstrap.body.procedures.map(x=>x.id),ADMIN_IDS);assert.equal(bootstrap.body.procedures.find(x=>x.id==='monitoring').code,'RN-01');assert.equal(bootstrap.body.procedures.find(x=>x.id==='evidence').code,'EV-01');assert.equal(bootstrap.body.procedures.find(x=>x.id==='objects').code,'AO-01');assert.equal(bootstrap.body.procedures.find(x=>x.id==='risks').code,'RC-01');assert.equal(bootstrap.body.procedures.find(x=>x.id==='assurance').code,'AR-01');assert.equal(bootstrap.body.procedures.find(x=>x.id==='administration').action,'open-administration');});
 const projected=bootstrap.body.missions.find(x=>x.id===mission.id);verify('bootstrap-projects-profile',()=>{assert.equal(projected.jobName,mission.jobName);assert.equal(projected.changeTypes.length,5);assert.equal(projected.evidenceUrl,`/api/evidence/mission/${mission.id}`);});
 const userBootstrap=await runtime.bootstrap('user','local-user');const auditorBootstrap=await runtime.bootstrap('auditor','local-auditor');
 verify('procedure-hub-least-privilege-projection',()=>{assert.deepEqual(userBootstrap.body.procedures.map(x=>x.id),READ_IDS);assert.deepEqual(auditorBootstrap.body.procedures.map(x=>x.id),READ_IDS);assert.equal(userBootstrap.body.procedures.some(x=>x.id==='administration'),false);assert.equal(auditorBootstrap.body.procedures.some(x=>x.id==='administration'),false);assert.equal(userBootstrap.body.procedures.find(x=>x.id==='monitoring').readOnly,false);assert.ok(READ_IDS.every(id=>auditorBootstrap.body.procedures.find(x=>x.id===id)?.readOnly===true));});
 const revised=await runtime.ok('PUT',`/api/monitoring-jobs/${mission.id}/profile`,{noveltyBaseline:'fixed-date',baselineAt:'2025-01-01T00:00:00.000Z',miningMode:'watchlist',authorities:['EUR-Lex'],changeTypes:['amendment','repeal']});
 verify('profile-revision',()=>{assert.equal(revised.body.mission.noveltyBaseline,'fixed-date');assert.equal(revised.body.mission.baselineAt,'2025-01-01T00:00:00.000Z');assert.equal(revised.body.mission.miningMode,'watchlist');assert.deepEqual(revised.body.mission.authorities,['EUR-Lex']);assert.equal(revised.body.mission.planVersion,2);assert.equal(revised.body.mission.planHistory.length,1);});
 const denied=await runtime.request('POST','/api/monitoring-jobs/draft',{jobName:'Non autorizzato',objective:'Tentativo utente'},'user','test-user');verify('least-privilege',()=>assert.equal(denied.status,403));
 const invalid=await runtime.request('POST','/api/monitoring-jobs/draft',{jobName:'Baseline incompleta',objective:'Test',noveltyBaseline:'fixed-date'});verify('fixed-baseline-validation',()=>assert.equal(invalid.status,400));
 const report={schemaVersion:'2.0.0',model:'ictc-enterprise-1-8-regression-on-v2',ok:true,checks,procedureHub:{model:'server-derived-v2',admin:9,user:8,auditor:8},limitation:'V1.8 monitoring regression preserved inside V2; discovery completeness and deployment controls remain external.'};
 await mkdir(new URL('../artifacts/',import.meta.url),{recursive:true});await writeFile(new URL('../artifacts/enterprise-1-8-runtime.json',import.meta.url),JSON.stringify(report,null,2));console.log(`enterprise-1-8-runtime-check: ok (${checks.length} checks)`);
}finally{await runtime.close();}
