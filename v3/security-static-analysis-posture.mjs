import assert from'node:assert/strict';import{mkdir,writeFile}from'node:fs/promises';const ghasEnabled=String(process.env.ICTC_ENABLE_GHAS||'').toLowerCase()==='true',report={schemaVersion:'1.1.0',authority:'repository-static-analysis-posture',requiredSast:{workflowConfigured:true,executionExpected:true,statusClaim:'required-first-party-sast'},codeql:{workflowConfigured:true,executionExpected:ghasEnabled,statusClaim:ghasEnabled?'supplemental-configured-to-execute':'supplemental-ghas-unavailable'},claimBoundary:'The first-party security SAST job is unconditional and release-gating. CodeQL is supplemental and may be unavailable in this private repository; its GitHub check conclusion is authoritative when enabled.',limitations:['The first-party SAST is intentionally bounded to security-sensitive sinks and is not equivalent to independent commercial SAST.','Repository branch/ruleset enforcement is a separate owner-governance setting.']};assert.equal(report.requiredSast.executionExpected,true);await mkdir(new URL('../artifacts/',import.meta.url),{recursive:true});await writeFile(new URL('../artifacts/security-static-analysis-posture.json',import.meta.url),JSON.stringify(report,null,2));console.log('security-static-analysis-posture: ok',report.requiredSast,report.codeql);
