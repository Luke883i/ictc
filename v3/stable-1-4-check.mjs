import assert from 'node:assert/strict';
import { mkdir, readFile, writeFile } from 'node:fs/promises';
const read=path=>readFile(new URL(path,import.meta.url),'utf8');
const contract=JSON.parse(await read('./stable-1-4-contract.json'));const packageJson=JSON.parse(await read('../package.json'));const packageLock=JSON.parse(await read('../package-lock.json'));const claims=JSON.parse(await read('./enterprise-claims.json'));const saturationArtifact=JSON.parse(await read('./artifacts-enterprise-saturation.json'));const version=await read('./version.mjs');const app=await read('./public/app.js');const styles=await read('./public/styles.css');const stableModule=await read('./public/ui/stable-1-4-home.js');const activeProof=await read('./public/ui/standard-proof-1-7.js');const stableCss=await read('./public/stable-1-4.css');const server=await read('./server.mjs');const accessProfile=await read('./access-profile.mjs');const browser=await read('./browser-stable-1-4-check.py');const readme=await read('../README.md');const verified=[];
function verify(name,fn){try{fn();verified.push(name);}catch(error){console.error(`::error title=stable-1-4:${name}::${String(error.message||error).replace(/%/g,'%25').replace(/\n/g,'%0A')}`);throw error;}}
verify('contract-shape',()=>{assert.equal(contract.schemaVersion,'1.4.0');assert.equal(contract.release,'1.4.0');assert.equal(contract.personas.length,4);assert.equal(contract.auditDimensions.length,12);assert.equal(contract.universalInvariants.length,12);assert.equal(contract.saturation.tail,100);});
verify('active-release-identity',()=>{for(const value of [packageJson.version,packageLock.version,packageLock.packages[''].version,claims.release,saturationArtifact.release])assert.equal(value,'1.7.0');assert.match(version,/VERSION = '1\.7\.0'/);assert.match(readme,/Release `1\.7\.0`/);});
verify('regression-installation-chain',()=>{assert.match(app,/installStandardProof17Experience/);assert.match(activeProof,/installStable14Experience\(\)/);assert.match(styles,/stable-1-4\.css/);assert.match(styles,/enterprise-1-7\.css/);assert.match(server,/projected\.accessProfile/);assert.match(server,/1\.7-enterprise-clarity/);});
verify('server-issued-authority',()=>{for(const role of ['admin','user','auditor'])assert.match(accessProfile,new RegExp(`${role}: Object\\.freeze`));for(const mode of ['read-write','contribute','read-only'])assert.match(accessProfile,new RegExp(mode));assert.match(accessProfile,/authoritySource: 'server-issued'/);});
verify('authority-questions',()=>{for(const id of ['homeAuthorityIdentity','homeAuthorityCan','homeAuthorityCannot','homeAuthorityEffects','homeAuthorityEvidence'])assert.match(stableModule,new RegExp(`id=\\"${id}\\"`));assert.match(stableModule,/actor\.role === 'auditor'\) authority\.open = true/);});
verify('plain-language-ontology',()=>{assert.match(stableModule,/Aggiungi materiale/);assert.match(stableModule,/Fascicoli evento/);assert.doesNotMatch(stableModule,/Aggiungi una fonte/);assert.match(stableModule,/Ruolo attivo/);});
verify('compact-accessible-geometry',()=>{assert.match(stableCss,/min-height:220px/);assert.match(stableCss,/min-height:44px/);assert.equal(contract.metrics.minimumInteractiveTargetPx,44);});
verify('browser-assurance',()=>{for(const role of ['admin','user','auditor'])assert.match(browser,new RegExp(role));assert.match(browser,/homeAuthority/);assert.match(browser,/authoritySource/);});
const report={schemaVersion:'1.4.0',release:'1.4.0',activeRelease:'1.7.0',model:contract.model,ok:true,metrics:contract.metrics,saturation:contract.saturation,verified,limitations:[contract.claimBoundary]};await mkdir(new URL('../artifacts/',import.meta.url),{recursive:true});await writeFile(new URL('../artifacts/stable-1-4-audit.json',import.meta.url),JSON.stringify(report,null,2));console.log(`stable-1-4-check: ok (${verified.length} regression groups, active release ${report.activeRelease})`);
