import assert from 'node:assert/strict';
import { readFile } from 'node:fs/promises';
const read=path=>readFile(new URL(path,import.meta.url),'utf8');
const [index,copy,frame,router,primitives,proof,anatomy,contractsRaw,meta,epistemic,controller,stableShell,visualRuntime,composition,model,styles,primitiveCss]=await Promise.all([read('./public/index.html'),read('./public/ui/product-copy.js'),read('./public/ui/procedure-frame.js'),read('./public/ui/surface-router.js'),read('./public/ui/surface-primitives.js'),read('./public/ui/proof-surface.js'),read('./public/ui/procedure-anatomy.js'),read('./procedure-contracts-1-2.json'),read('./runtime/meta-procedure-contracts.mjs'),read('./public/ui/epistemic-lattice.js'),read('./public/ui/controller.js'),read('./public/ui/stable-shell.js'),read('./public/ui/visual-epistemic-runtime.js'),read('./public/ui/semantic-composition-runtime.js'),read('./public/ui/semantic-composition-model.js'),read('./public/styles.css'),read('./public/surface-primitives.css')]);
const contracts=JSON.parse(contractsRaw),codes=contracts.procedures.map(item=>item.code);assert.deepEqual(codes,['RN-01','EC-01','AO-01','MC-01','AP-01','RC-01','AR-01']);assert.equal(contracts.procedures.length,7);assert.ok(meta.includes("code:'EP-01'")&&meta.includes('businessProcess:false')&&meta.includes('crossCutting:true'));
for(const token of ['>Home</button>','>Processi di Compliance</button>','>Evidenze ICTC</button>','<h1>Processi di Compliance</h1>'])assert.ok(index.includes(token),`shell canonical language missing ${token}`);assert.ok(copy.includes("home:'Home'")&&copy.includes("processes:'Processi di Compliance'")&&copy.includes("proof:'Evidenze ICTC'"));
for(const token of ['PROCEDURE_COMPOSITION','data-procedure-frame-variant="row"','Consulta registrazioni'])assert.ok(frame.includes(token),`procedure composition missing ${token}`);assert.equal(frame.includes('semanticSignals'),false,'numeric procedure signal wall returned');assert.ok(stableShell.includes("textContent='Attività di compliance'")&&stableShell.includes('pulse.hidden=true')&&stableShell.includes("homeWorkQueue='3.1'"),'Home must remain work-first without numeric dashboard');
assert.ok(router.includes('SURFACE_LABELS.processes')&&router.includes('Torna al processo precedente'));
for(const forbidden of ['ICTC_MANIFEST','SURFACE_INFORMATION','surface-information-detail','ictc-manifest-detail'])assert.equal(primitives.includes(forbidden),false,`structural primitive owns narrative ${forbidden}`);assert.ok(primitives.includes('retireInjectedNarrative'));
for(const token of ['Come leggere le prove ICTC','Decisioni e tracciabilità','proof-technical-detail','non la sufficienza sostanziale'])assert.ok(proof.includes(token),`evidence contract missing ${token}`);
assert.ok(visualRuntime.includes("visualEpistemicRuntime='compat-3.1'"));for(const forbidden of ['createTreeWalker','MutationObserver','replaceExact','proof-semantic-qualifier'])assert.equal(visualRuntime.includes(forbidden),false,`visual compatibility runtime still owns visible copy: ${forbidden}`);
for(const token of ['commonSubstrate?.epistemicFamilies','anchor.after(box)','Decisioni umane visibili','Versioni registrate'])assert.ok(anatomy.includes(token),`procedure technical disclosure missing ${token}`);for(const token of ['Letture proposte','loadedStateRevision','requestedRevision','loadSequence','Vista trasversale','Tutti i Processi di Compliance'])assert.ok(epistemic.includes(token),`EP runtime contract missing ${token}`);assert.ok(controller.includes('ictc:projection-committed')&&controller.includes('ictcProjectionRevision'));
for(const token of ['compactHome','compactProcesses','compactNative','compactGrc','compactProof','compactEpistemic','compactAdmin','compactAiSettings'])assert.ok(composition.includes(token),`all-surface composition adapter missing ${token}`);assert.ok(model.includes("title:'Relazioni tra decisioni, fonti ed evidenze'")&&model.includes("title:'Evidenze ICTC'"));
const imports=[...styles.matchAll(/@import\s+url\(['"]?([^)'"\s]+)['"]?\)/g)].map(m=>m[1]);assert.equal(imports.at(-1),'./semantic-composition-3-1.css','3.1 must be final information-composition cascade');assert.ok(primitiveCss.includes('--surface-control-min:44px')&&primitiveCss.includes(':where(button,summary,[role="button"])'),'44px shared control grammar missing');
for(const item of contracts.procedures){assert.ok(item.claimBoundary);assert.ok(Array.isArray(item.humanCheckpoints)&&item.humanCheckpoints.length);assert.ok(Array.isArray(item.evidence)&&item.evidence.length);}console.log('visual-grace-lexical-epistemic-check: ok (canonical seven-process language + structural primitives + all-surface work-first composition + progressive technical disclosure)');