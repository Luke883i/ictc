import assert from 'node:assert/strict';
import { readFile } from 'node:fs/promises';
const read=path=>readFile(new URL(path,import.meta.url),'utf8');
const [doc,index,copy,frame,router,primitives,proof,css,anatomy,contractsRaw,meta,epistemic,controller,stableShell,visualRuntime]=await Promise.all([read('../docs/VISUAL_GRACE_LEXICAL_EPISTEMIC_AUDIT.md'),read('./public/index.html'),read('./public/ui/product-copy.js'),read('./public/ui/procedure-frame.js'),read('./public/ui/surface-router.js'),read('./public/ui/surface-primitives.js'),read('./public/ui/proof-surface.js'),read('./public/onto-compliance-v1.css'),read('./public/ui/procedure-anatomy.js'),read('./procedure-contracts-1-2.json'),read('./runtime/meta-procedure-contracts.mjs'),read('./public/ui/epistemic-lattice.js'),read('./public/ui/controller.js'),read('./public/ui/stable-shell.js'),read('./public/ui/visual-epistemic-runtime.js')]);
const contracts=JSON.parse(contractsRaw),codes=contracts.procedures.map(item=>item.code);assert.deepEqual(codes,['RN-01','EC-01','AO-01','MC-01','AP-01','RC-01','AR-01']);assert.equal(contracts.procedures.length,7);assert.ok(meta.includes("code:'EP-01'")&&meta.includes('businessProcess:false')&&meta.includes('crossCutting:true'));
for(const token of ['Processi di Compliance','M+10000','Practice','Evidence','Limit'])assert.ok(doc.includes(token),`audit contract missing ${token}`);for(const token of ['>Home</button>','>Processi di Compliance</button>','>Evidenze ICTC</button>','<h1>Processi di Compliance</h1>','Apri Processi di Compliance'])assert.ok(index.includes(token),`shell canonical language missing ${token}`);assert.ok(copy.includes("home:'Home'")&&copy.includes("processes:'Processi di Compliance'")&&copy.includes("proof:'Evidenze ICTC'"));assert.ok(!copy.includes("home:'Oggi'")&&!copy.includes("proof:'Postura ICTC'"));
for(const token of ['semanticSignals','metric.label','aria-label="Segnali del processo"','Consulta registrazioni',"entryLabel:readOnly?'Consulta':'Gestisci'"])assert.ok(frame.includes(token));for(const forbidden of ['<span>Processo di Compliance</span>','<b>Scopo del processo</b>','<small>da vedere</small>','<small>registrazioni</small>','Nessuna attenzione aperta','<span>Procedura</span>','<b>Scopo della procedura</b>','aria-label="Segnali della procedura"',"textContent='Procedure'",'Consulta record'])assert.equal(frame.includes(forbidden),false);
for(const token of ["EXPERIENCE_EDITION='1.9-experience-candidate'",'ictcExperienceEdition=EXPERIENCE_EDITION','SURFACE_LABELS.processes','SURFACE_LABELS.proof','Processi di Compliance','senza attenzione aperta'])assert.ok(stableShell.includes(token));for(const forbidden of ['processi in ordine','Tutte le procedure','Apri Processi</button>','<span>procedure</span>','entra nella procedura','nella procedura corretta','Ricostruisci procedure'])assert.equal(stableShell.includes(forbidden),false);
assert.ok(router.includes('SURFACE_LABELS.processes')&&router.includes('Torna al processo precedente'));assert.ok(primitives.includes('>Processi</button>')&&primitives.includes('surface-information-detail')&&primitives.includes('ictc-manifest-detail'));assert.equal(primitives.includes('>Processi di Compliance</button>'),false);
for(const token of ['Come leggere le prove ICTC','proofEvidenceKinds','proofBenchmarkMappings','Pratica ICTC.','Evidenza.','<b>Limite.</b>','data.proof?.evidenceKinds','data.proof?.rule','data.benchmarkFamilies'])assert.ok(proof.includes(token));assert.ok(visualRuntime.includes('Dati osservabili, metodo e limiti della prova')&&visualRuntime.includes('non è un giudizio di conformità dell’organizzazione'));assert.equal(proof.includes('Procedure con decisioni'),false);assert.equal(proof.includes('7 procedure operative'),false);
for(const token of ['commonSubstrate?.epistemicFamilies','anchor.after(box)','Decisioni umane visibili','Versioni registrate'])assert.ok(anatomy.includes(token));assert.match(anatomy,/\b(?:c|current)\.claimBoundary\b/);for(const token of ['Letture proposte','loadedStateRevision','requestedRevision','loadSequence','Vista trasversale','Scopo della vista','Tutti i Processi di Compliance','<small>Processo di Compliance</small>','<th>Processo di Compliance</th>'])assert.ok(epistemic.includes(token));for(const forbidden of ['<span>Meta-procedura</span>','<b>Scopo della procedura</b>','sr-only">Procedura</span>','>Tutte le procedure</option>','<small>Procedura</small>','<th>Procedura</th>'])assert.equal(epistemic.includes(forbidden),false);assert.ok(controller.includes('ictc:projection-committed')&&controller.includes('ictcProjectionRevision'));
for(const token of ['max-width:68ch','min-height:44px','.procedure-frame::after{display:none}','transform:none','grid-template-columns:repeat(2,minmax(0,1fr))','grid-template-columns:1fr','.proof-method-list'])assert.ok(css.includes(token));assert.equal(css.includes('body{overflow-x:hidden}'),false);for(const retiredTiny of ['font-size:.58rem','font-size:.59rem'])assert.equal(css.includes(retiredTiny),false);for(const item of contracts.procedures){assert.ok(item.claimBoundary);assert.ok(Array.isArray(item.humanCheckpoints)&&item.humanCheckpoints.length);assert.ok(Array.isArray(item.evidence)&&item.evidence.length);}console.log('visual-grace-lexical-epistemic-check: ok (canonical seven processes / Home-Processi-Evidenze shell / evidence-first method / visual restraint / epistemic boundaries)');
