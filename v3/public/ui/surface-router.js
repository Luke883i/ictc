import { $, $$, state, storageSet } from './common.js';
const surfaces=Object.freeze({home:'#homeView',processes:'#processesView',grc:'#grcView',monitoring:'#monitoringView',incidents:'#incidentsView',proof:'#proofView'});let navigationEpoch=0;
function normalizeSurface(value){return Object.hasOwn(surfaces,value)?value:'home';}
export function renderSurfaceNavigation(){const active=normalizeSurface(state.service);state.service=active;for(const [surface,selector] of Object.entries(surfaces)){const view=$(selector);if(view)view.hidden=surface!==active;}$$('.workspace-return').forEach(button=>{const owner=button.closest('.view');if(owner&&!owner.hidden)button.setAttribute('data-workspace-return','');else button.removeAttribute('data-workspace-return');});$$('[data-service]').forEach(button=>button.setAttribute('aria-current',button.dataset.service===active?'page':'false'));document.documentElement.dataset.ictcSurface=active;document.documentElement.dataset.ictcWorkspace=['monitoring','incidents','grc'].includes(active)?active:'none';return active;}
export function navigateSurface(value,{focus=true,focusTarget=null}={}){const active=normalizeSurface(value);navigationEpoch+=1;const epoch=navigationEpoch;state.service=active;storageSet('ictc-service',active);renderSurfaceNavigation();document.dispatchEvent(new CustomEvent('ictc:surface-changed',{detail:{surface:active}}));if(!focus)return active;const targetSelector=focusTarget||surfaces[active]||'#main';const focusCurrent=()=>{if(epoch!==navigationEpoch||state.service!==active)return;const target=$(targetSelector);if(!target||target.hidden)return;if(!target.hasAttribute('tabindex'))target.setAttribute('tabindex','-1');target.focus({preventScroll:false});};focusCurrent();queueMicrotask(focusCurrent);requestAnimationFrame(focusCurrent);return active;}
function isContextualWorkspaceTrigger(trigger){return trigger.hasAttribute('data-grc-process')||trigger.hasAttribute('data-home-action')||trigger.hasAttribute('data-global-kind');}
export function installSurfaceRouter(){window.addEventListener('click',event=>{const trigger=event.target.closest?.('[data-service]');if(!trigger||isContextualWorkspaceTrigger(trigger))return;event.preventDefault();event.stopImmediatePropagation();navigateSurface(trigger.dataset.service);},true);document.addEventListener('ictc:rendered',renderSurfaceNavigation);renderSurfaceNavigation();}
