import { $, state } from './common.js';

const REPOSITORY_URL='https://github.com/Luke883i/ictc';
const LICENSE_URL='https://github.com/Luke883i/ictc/blob/main/LICENSE';
let installed=false;

function canonicalServices(){const nav=$('.service-nav');return nav?[...nav.querySelectorAll(':scope > [data-service]')].map(node=>node.dataset.service):[];}
function installHeader(){const header=$('.topbar'),nav=$('.service-nav'),actions=$('.top-actions');if(!header||!nav||!actions)return;let inner=header.querySelector('.stable-header-inner');if(!inner){inner=document.createElement('div');inner.className='stable-header-inner';const brand=header.querySelector('.brand');header.prepend(inner);if(brand)inner.append(brand);inner.append(nav);inner.append(actions);}const brandSmall=header.querySelector('.brand small');if(brandSmall)brandSmall.textContent='Compliance operativa';}
function installHome(){const hero=$('.home-hero'),intro=$('.home-intro'),next=$('.home-next');$('.home-journey-panel')?.remove();$('.home-overview-grid')?.remove();if(!hero||!intro||!next)return;hero.dataset.stableHero='operational';intro.querySelector('.eyebrow')?.replaceChildren(document.createTextNode('Oggi'));const title=$('#homeTitle');if(title)title.textContent='Cosa richiede attenzione?';const summary=$('#homeSummary');if(summary&&!state.data)summary.textContent='Continua il lavoro aperto oppure scegli un processo di compliance.';if(next.parentElement===hero&&!next.classList.contains('stable-next-strip'))next.classList.add('stable-next-strip');}
function installFooter(){if($('#stableLegalFooter'))return;const footer=document.createElement('footer');footer.id='stableLegalFooter';footer.className='stable-legal-footer';footer.setAttribute('aria-label','Licenza e condizioni ICTC');footer.innerHTML=`<span>ICTC · <a href="${LICENSE_URL}" target="_blank" rel="noopener noreferrer">MIT</a></span><span aria-hidden="true">·</span><a href="${REPOSITORY_URL}" target="_blank" rel="noopener noreferrer">Repository</a><span aria-hidden="true">·</span><a href="/terms.html">Condizioni</a>`;document.body.append(footer);}
export function ensureStableShell(){document.title='ICTC · Compliance operativa';document.documentElement.dataset.ictcEdition='1.0-stable';document.documentElement.dataset.ictcStability='stable';document.documentElement.dataset.ictcExperience='stable-1';const services=canonicalServices();if(JSON.stringify(services)!==JSON.stringify(['home','processes','proof']))throw Object.assign(new Error('Shell stabile non canonica'),{code:'stable-shell-missing',details:{services}});installHeader();installHome();installFooter();}
export function installStableShell(){if(installed)return;installed=true;ensureStableShell();}
