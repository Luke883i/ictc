import { $, esc, state } from './common.js';
const REPOSITORY_URL='https://github.com/Luke883i/ictc';const LICENSE_URL='https://github.com/Luke883i/ictc/blob/main/LICENSE';const HOME_SUMMARIES=Object.freeze({admin:'Una vista unica su priorità, procedure, standard ed evidenze. L’amministrazione resta separata dal lavoro operativo.',user:'Continua il lavoro pertinente al tuo ruolo, consulta lo stato delle procedure e registra conoscenza ed evidenze nel contesto corretto.',auditor:'Ricostruisci procedure, decisioni ed evidenze nel perimetro accessibile, senza modificare lo stato operativo.'});let installed=false;
function canonicalServices(){const nav=$('.service-nav');return nav?[...nav.querySelectorAll(':scope > [data-service]')].map(node=>node.dataset.service):[];}
function contract(id){return state.data?.procedureRegistry?.procedures?.find(item=>item.id===id)||null;}
function assertNativeShell(){const html=document.documentElement,services=canonicalServices(),failures=[];if(html.dataset.ictcEdition!=='1.2-market-candidate')failures.push('edition');if(html.dataset.ictcStability!=='candidate')failures.push('stability');if(html.dataset.ictcExperience!=='market-1')failures.push('experience');if(document.title!=='ICTC · Integrated Compliance Tower Control')failures.push('title');if(JSON.stringify(services)!==JSON.stringify(['home','processes','proof']))failures.push('services');if(!$('.stable-header-inner'))failures.push('header');if($('.service-nav [data-service="proof"]')?.textContent!=='Evidenze')failures.push('proof-label');if(!$('#homePulse')||!$('#homePriorities'))failures.push('home-runtime-slots');if(!$('.home-journey-panel')?.hidden||!$('.home-overview-grid')?.hidden)failures.push('legacy-home-hidden');if(failures.length)throw Object.assign(new Error(`Shell 1.2 statica non canonica: ${failures.join(', ')}`),{code:'native-market-shell-invalid',details:{failures}});}
function processTrigger(item){const label=esc(item.label||item.code||item.id);if(item.service==='grc')return `<button type="button" data-service="grc" data-grc-process="${esc(item.id)}"><span>${label}</span><strong>${Number(item.attentionCount||0)} da vedere</strong><i aria-hidden="true">→</i></button>`;return `<button type="button" data-service="${esc(item.service||item.id)}"><span>${label}</span><strong>${Number(item.attentionCount||0)} da vedere</strong><i aria-hidden="true">→</i></button>`;}
function renderHomePulse(){const pulse=$('#homePulse'),priorities=$('#homePriorities');if(!pulse||!priorities||!state.data)return;const summary=$('#homeSummary');if(summary)summary.textContent=HOME_SUMMARIES[state.role]||'Orientati sul lavoro aperto, entra nella procedura giusta e conserva decisioni ed evidenze nello stesso contesto.';const procedures=(state.data.procedures||[]).filter(item=>item.code&&item.id!=='evidence'),enabled=Number(state.data.experience?.procedurePolicy?.enabled?.length??procedures.length),attention=procedures.reduce((sum,item)=>sum+Number(item.attentionCount||0),0),healthy=procedures.filter(item=>Number(item.attentionCount||0)===0).length,decisions=Number(state.data.decisions?.records?.length||0);pulse.innerHTML=`<div><b>${enabled}</b><span>procedure attive</span></div><div><b>${attention}</b><span>elementi da vedere</span></div><div><b>${healthy}</b><span>procedure in ordine</span></div><div><b>${decisions}</b><span>decisioni umane</span></div>`;const top=[...procedures].filter(item=>Number(item.attentionCount||0)>0).sort((a,b)=>Number(b.attentionCount||0)-Number(a.attentionCount||0)||String(a.code).localeCompare(String(b.code))).slice(0,3);priorities.innerHTML=top.length?`<div class="home-priority-head"><span>Priorità per procedura</span><button type="button" data-service="processes">Tutte le procedure</button></div>${top.map(processTrigger).join('')}`:`<div class="home-priority-clear"><span>Nessuna priorità aperta nella proiezione corrente.</span><button type="button" data-service="processes">Apri Processi</button></div>`;}
function normalizeStandaloneGrc(){const head=$('.grc-head');if(!head)return;head.querySelector('nav[aria-label="Processi GRC"]')?.remove();let id=state.activeProcessId||'';if(!id)try{id=localStorage.getItem('ictc-grc-process')||'';}catch{}const meta=contract(id);if(!meta||meta.adapter?.surface!=='grc')return;const eyebrow=head.querySelector('.eyebrow'),title=head.querySelector('h1'),description=head.querySelector('h1 + p');if(eyebrow)eyebrow.textContent=`${meta.code} · Procedura di compliance`;if(title)title.textContent=meta.label;if(description)description.textContent=meta.purpose||meta.claimBoundary||'';head.dataset.standaloneProcess=id;}
function installFooter(){if($('#stableLegalFooter'))return;const footer=document.createElement('footer');footer.id='stableLegalFooter';footer.className='stable-legal-footer';footer.setAttribute('aria-label','Licenza e condizioni ICTC');footer.innerHTML=`<span>ICTC · <a href="${LICENSE_URL}" target="_blank" rel="noopener noreferrer">MIT</a></span><span aria-hidden="true">·</span><a href="${REPOSITORY_URL}" target="_blank" rel="noopener noreferrer">Repository</a><span aria-hidden="true">·</span><a href="/terms.html">Condizioni</a>`;document.body.append(footer);}
export function ensureStableShell(){assertNativeShell();renderHomePulse();normalizeStandaloneGrc();installFooter();}
export function installStableShell(){if(installed)return;installed=true;ensureStableShell();document.addEventListener('ictc:surface-changed',event=>{if(event.detail?.surface==='grc')queueMicrotask(normalizeStandaloneGrc);});window.addEventListener('click',event=>{if(event.target.closest?.('[data-grc-process]'))queueMicrotask(normalizeStandaloneGrc);},true);}
