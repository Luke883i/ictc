import { api, esc } from './common.js';
let installed=false,current=null;
function ensureCss(){if(document.querySelector('link[data-semantic-closure]'))return;const link=document.createElement('link');link.rel='stylesheet';link.href='/semantic-closure.css';link.dataset.semanticClosure='';document.head.append(link);}
function ensureDialog(){let dialog=document.querySelector('#standardBrowserDialog');if(dialog)return dialog;dialog=document.createElement('dialog');dialog.id='standardBrowserDialog';dialog.className='standard-browser-dialog';dialog.innerHTML='<form method="dialog" class="standard-browser-shell"><header><div><p class="eyebrow">Standard e Controlli</p><h2 data-standard-title>Standard</h2><p data-standard-meta></p></div><button class="icon-button" value="cancel" aria-label="Chiudi">×</button></header><div class="standard-browser-tools"><input type="search" data-standard-search placeholder="Cerca riferimento o titolo"><span data-standard-count></span></div><div class="standard-browser-boundary" data-standard-boundary></div><div class="standard-node-list" data-standard-nodes></div></form>';document.body.append(dialog);dialog.querySelector('[data-standard-search]').addEventListener('input',renderNodes);return dialog;}
function boundary(framework){const mode=framework.contentPolicy||'';if(mode==='authorized-user-pack')return'Contenuto importato sotto attestazione di diritto d’uso. La provenienza del pack resta distinta dalla decisione organizzativa di utilizzo.';if(String(mode).startsWith('public-'))return'Riferimento pubblico indicizzato. La presenza nel catalogo non costituisce una decisione di applicabilità.';return'ICTC distribuisce metadata e riferimenti; il testo normativo proprietario non è incluso salvo pack autorizzato.';}
function nodeLabel(node){return node.label||node.title||node.ref||node.id;}
function renderNodes(){if(!current)return;const dialog=ensureDialog(),q=String(dialog.querySelector('[data-standard-search]').value||'').trim().toLowerCase(),nodes=(current.nodes||[]).filter(node=>!q||[node.id,node.ref,node.label,node.title,node.kind].some(value=>String(value||'').toLowerCase().includes(q)));dialog.querySelector('[data-standard-count]').textContent=`${nodes.length} / ${(current.nodes||[]).length} nodi`;dialog.querySelector('[data-standard-nodes]').innerHTML=nodes.map(node=>`<article class="standard-node" data-standard-node="${esc(node.id)}"><div><span class="process-code">${esc(node.ref||node.id)}</span><strong>${esc(nodeLabel(node))}</strong><small>${esc(node.kind||'requirement')} · ${esc(node.contentMode||current.contentPolicy||'metadata')}</small></div><details><summary>Contesto</summary><p>Edizione: ${esc(current.edition||'dichiarata')}</p><p>Autorità sorgente: ${esc(current.sourceAuthority||current.issuer||'non dichiarata')}</p><p class="boundary">Il nodo è consultabile indipendentemente dall’uso organizzativo dello standard. Applicabilità, mapping, efficacia e conformità restano decisioni o osservazioni separate.</p></details></article>`).join('')||'<p class="empty">Nessun nodo corrispondente.</p>';}
async function openStandard(id){const dialog=ensureDialog();current=await api(`/api/standards/${encodeURIComponent(id)}`);dialog.querySelector('[data-standard-title]').textContent=current.shortName||current.title||id;dialog.querySelector('[data-standard-meta]').textContent=[current.title,current.edition,current.issuer].filter(Boolean).join(' · ');dialog.querySelector('[data-standard-boundary]').textContent=boundary(current);dialog.querySelector('[data-standard-search]').value='';renderNodes();dialog.showModal();}
function enhanceCards(){for(const card of document.querySelectorAll('[data-framework-card]')){if(card.querySelector('[data-open-standard-browser]'))continue;const button=document.createElement('button');button.type='button';button.className='secondary standard-browser-open';button.dataset.openStandardBrowser=card.dataset.frameworkCard;button.textContent='Apri standard';(card.querySelector('footer')||card).append(button);}}
export function installStandardBrowser(){if(installed)return;installed=true;ensureCss();ensureDialog();document.addEventListener('ictc:rendered',enhanceCards);document.addEventListener('click',event=>{const button=event.target.closest('[data-open-standard-browser]');if(!button)return;openStandard(button.dataset.openStandardBrowser).catch(error=>console.error(error));});queueMicrotask(enhanceCards);}
