import { $, api, state } from './common.js';import { render } from './render.js';import { renderIncidentWorkspace, renderPlanDialog, renderSourceDialog } from './workspaces.js';export async function refresh({keepDialog=true}={}){state.data=await api('/api/bootstrap');state.role=state.data.actor.role;const product=state.data.experience?.product||'ICTC Control Tower';const edition=state.data.experience?.productEdition||state.data.version;document.title=`${product} · ${edition}`;render();if(keepDialog&&state.activeIncidentId&&$('#incidentWorkspace').open)renderIncidentWorkspace();if(keepDialog&&state.activeSourceId&&$('#sourceDialog').open)renderSourceDialog();if(keepDialog&&state.activeMissionId&&$('#planDialog').open)renderPlanDialog();document.dispatchEvent(new CustomEvent('ictc:rendered',{detail:{actorRole:state.data.actor.role}}));}
