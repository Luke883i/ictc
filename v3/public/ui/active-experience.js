import { $, esc, state } from './common.js';
import { installBindings } from './actions.js';
import { installAdminCenter } from './admin-center.js';
import { installEnterpriseExperience } from './enterprise-ux.js';
import { installFi01Reference } from './fi01-reference.js';
import { installGlobalTools } from './global-tools.js';
import { installProofSurface } from './proof-surface.js';
import { installTraceExplorer } from './trace-explorer.js';
import { installGrcWorkspace } from './grc-workspace.js';
import { installSurfaceRouter } from './surface-router.js';
import { installV3EntryPoint } from './operational-entry.js';
import { installProcessLandscape } from './process-landscape.js';
import { installAiOptionalControls } from './ai-optional-controls.js';
import { installProcessHandoffs } from './process-handoffs.js';
import { installRiskActionFidelity } from './risk-action-fidelity.js';
import { installBusinessNexus } from './business-nexus.js';
import { installNoviceEntry } from './novice-entry.js';
import { installAiModeBridge } from './ai-mode-bridge.js';
import { installExperiencePolish } from './experience-polish.js';
let installed=false;
function ensureMarketShell(){document.title='ICTC · Enterprise Compliance Nexus';document.documentElement.dataset.ictcEdition='v4-1-experimental';document.documentElement.dataset.ictcStability='v4_experimental_stable';const nav=$('.service-nav'),services=nav?[...nav.querySelectorAll('[data-service]')].map(node=>node.dataset.service):[],labels=nav?[...nav.querySelectorAll('[data-service]')].map(node=>node.textContent.trim()):[],valid=JSON.stringify(services)===JSON.stringify(['home','processes','proof'])&&JSON.stringify(labels)===JSON.stringify(['Oggi','Processi','Prove'])&&Boolean($('#processesView'))&&Boolean($('#procedureHub'));if(!valid)throw Object.assign(new Error('Shell canonica v4_experimental_stable assente o incoerente'),{code:'canonical-shell-missing',details:{services,labels,processesView:Boolean($('#processesView')),procedureHub:Boolean($('#procedureHub'))}});}
function renderProcedureHub(){const host=$('#procedureHub');if(!host||!state.data)return;const procedures=Array.isArray(state.data.procedures)?state.data.procedures:[];host.dataset.procedureHub='server-derived';host.innerHTML=procedures.map(item=>{const code=item.code?`<span class="process-code">${esc(item.code)}</span>`:'';const count=Number(item.attentionCount||0),attention=count>0?`<span class="procedure-attention">${count} da vedere</span>`:'<span class="procedure-ready">Operativo</span>';const target=item.service||(item.id==='administration'?'administration':'processes');let action;if(target==='administration')action='<button class="secondary" type="button" data-open-admin-center>Apri amministrazione</button>';else if(target==='grc')action=`<button class="secondary" type="button" data-service="grc" data-grc-process="${esc(item.id)}">${esc(item.actionLabel)}</button>`;else{const safe=['monitoring','incidents','proof','processes'].includes(target)?target:'processes';action=`<button class="secondary" type="button" data-service="${esc(safe)}">${esc(item.actionLabel)}</button>`;}return `<article class="procedure-card" data-procedure-id="${esc(item.id)}" data-process-code="${esc(item.code||'')}" data-archetype="${esc(item.archetype||'')}" data-read-only="${item.readOnly?'true':'false'}"><header>${code}<h3>${esc(item.label)}</h3></header><p>${esc(item.description)}</p>${(item.metrics||[]).length?`<div class="procedure-metrics">${item.metrics.map(m=>`<span><b>${esc(m.value)}</b> ${esc(m.label)}</span>`).join('')}</div>`:''}${item.claimBoundary?`<details class="procedure-boundary"><summary>Limiti</summary><small class="claim-boundary">${esc(item.claimBoundary)}</small></details>`:''}<footer>${attention}${action}</footer></article>`;}).join('')||'<div class="empty">Nessuna procedura disponibile per il ruolo corrente.</div>';}
function openOnly(root,current){root.querySelectorAll('[data-settings-section]').forEach(section=>{if(section!==current)section.open=false;});}
function ensureProgressiveSettings(){const form=$('#settingsForm'),body=form?.querySelector('.two-pane-form');if(!form||!body||body.dataset.canonicalSettings==='true')return;body.dataset.canonicalSettings='true';const sections=[...body.children].filter(node=>node.tagName==='SECTION');const definitions=[['organization','Contesto organizzativo','Nome, ambito operativo e territori.'],['provider','Connessione AI','Servizio, modello autorizzato e variabile segreta.']];sections.forEach((section,index)=>{const[id,label,hint]=definitions[index]||[`section-${index+1}`,`Passaggio ${index+1}`,'Configurazione'];section.querySelector('h3')?.remove();const details=document.createElement('details');details.dataset.settingsSection=id;details.className='settings-section';details.open=index===0;details.innerHTML=`<summary><span><b>${label}</b><small>${hint}</small></span></summary>`;section.before(details);details.append(section);details.addEventListener('toggle',()=>{if(details.open)openOnly(body,details);});});const advanced=body.querySelector(':scope > details.advanced');if(advanced){advanced.dataset.settingsSection='assisted-instructions';advanced.classList.add('settings-section');const summary=advanced.querySelector(':scope > summary');if(summary)summary.innerHTML='<span><b>Istruzioni assistite</b><small>Facoltative; non trasferiscono autorità all’AI.</small></span>';advanced.addEventListener('toggle',()=>{if(advanced.open)openOnly(body,advanced);});}const submit=form.querySelector('button[type="submit"]');if(submit)submit.textContent='Salva configurazione AI';}
function bindAdminShortcut(){document.addEventListener('click',event=>{const trigger=event.target.closest('[data-open-admin-center]');if(!trigger)return;event.preventDefault();$('#openAdminCenter')?.click();});}
function renderCanonicalExperience(){document.documentElement.dataset.ictcExperience='active-1';document.documentElement.dataset.ictcMarketExperience='enterprise-nexus-1';renderProcedureHub();}
export function installActiveExperience(){if(installed)return;installed=true;ensureMarketShell();ensureProgressiveSettings();installProofSurface();installTraceExplorer();installNoviceEntry();installAiModeBridge();installAiOptionalControls();installProcessHandoffs();installRiskActionFidelity();installGrcWorkspace();installSurfaceRouter();installBindings();installAdminCenter();installEnterpriseExperience();installFi01Reference();installGlobalTools();installV3EntryPoint();installBusinessNexus();installProcessLandscape();installExperiencePolish();bindAdminShortcut();document.addEventListener('ictc:rendered',renderCanonicalExperience);renderCanonicalExperience();}
