import { $, esc, state } from './common.js';import { installBindings } from './actions.js';import { installAdminCenter } from './admin-center.js';import { installEnterpriseExperience } from './enterprise-ux.js';import { installFi01Reference } from './fi01-reference.js';import { installGlobalTools } from './global-tools.js';import { installProofSurface } from './proof-surface.js';import { installSurfaceRouter } from './surface-router.js';let installed=false;
function ensureMarketShell(){document.title='ICTC · Control Tower';document.documentElement.dataset.ictcEdition='v1-experimental';const nav=$('.service-nav');if(nav){const home=nav.querySelector('[data-service="home"]');if(home)home.textContent='Oggi';nav.querySelectorAll('[data-service="monitoring"],[data-service="incidents"]').forEach(node=>node.remove());if(!nav.querySelector('[data-service="processes"]'))home?.insertAdjacentHTML('afterend','<button type="button" data-service="processes" aria-current="false">Processi</button>');if(!$('#openProofSurface'))nav.insertAdjacentHTML('beforeend','<button id="openProofSurface" type="button" data-service="proof" aria-current="false">Prove</button>');}const main=$('#main');if(main&&!$('#processesView')){const monitoring=$('#monitoringView');const section=document.createElement('section');section.id='processesView';section.className='view processes-view';section.hidden=true;section.innerHTML='<header class="processes-head"><div><p class="eyebrow">Processi</p><h1>Il lavoro disponibile per il tuo ruolo</h1><p>Ogni processo deriva dal registro server-side e apre lo stesso workspace operativo, senza creare nuove autorità.</p></div></header><div id="procedureHub" class="procedure-hub"></div>';if(monitoring)monitoring.before(section);else main.append(section);}for(const [selector,label] of [['#monitoringView','Torna ai processi'],['#incidentsView','Torna ai processi']]){const view=$(selector);if(view&&!view.querySelector('[data-workspace-return]'))view.insertAdjacentHTML('afterbegin',`<button class="workspace-return" type="button" data-service="processes" data-workspace-return>${label}</button>`);}const routes=$('.home-route-list');if(routes)routes.innerHTML='<button type="button" data-service="processes"><span><b>Processi</b><small>Apri monitoraggio, eventi, prove e strumenti disponibili per il tuo ruolo.</small></span><i aria-hidden="true">→</i></button>';}
function renderProcedureHub(){const host=$('#procedureHub');if(!host||!state.data)return;const procedures=Array.isArray(state.data.procedures)?state.data.procedures:[];host.dataset.procedureHub='server-derived';host.innerHTML=procedures.map(item=>{const code=item.code?`<span class="process-code">${esc(item.code)}</span>`:'';const count=Number(item.attentionCount||0);const attention=count>0?`<span class="procedure-attention">${count} da vedere</span>`:'<span class="procedure-ready">Operativo</span>';const target=item.service||(item.id==='administration'?'administration':'processes');const safeTarget=['monitoring','incidents','proof','processes'].includes(target)?target:'processes';const action=target==='administration'?'<button class="secondary" type="button" data-open-admin-center>Apri amministrazione</button>':`<button class="secondary" type="button" data-service="${esc(safeTarget)}">${esc(item.actionLabel)}</button>`;return`<article class="procedure-card" data-procedure-id="${esc(item.id)}" data-process-code="${esc(item.code||'')}" data-archetype="${esc(item.archetype||'')}" data-read-only="${item.readOnly?'true':'false'}"><header>${code}<h3>${esc(item.label)}</h3></header><p>${esc(item.description)}</p><small class="claim-boundary">${esc(item.claimBoundary||'')}</small><footer>${attention}${action}</footer></article>`;}).join('')||'<div class="empty">Nessun processo disponibile per il ruolo corrente.</div>';}
function openOnly(root,current){root.querySelectorAll('[data-settings-section]').forEach(section=>{if(section!==current)section.open=false;});}function ensureProgressiveSettings(){const form=$('#settingsForm');const body=form?.querySelector('.two-pane-form');if(!form||!body||body.dataset.canonicalSettings==='true')return;body.dataset.canonicalSettings='true';const sections=[...body.children].filter(node=>node.tagName==='SECTION');const definitions=[['organization','Contesto organizzativo','Nome, ambito operativo e territori.'],['provider','Connessione AI','Servizio, modello autorizzato e variabile segreta.']];sections.forEach((section,index)=>{const[id,label,hint]=definitions[index]||[`section-${index+1}`,`Passaggio ${index+1}`,'Configurazione'];section.querySelector('h3')?.remove();const details=document.createElement('details');details.dataset.settingsSection=id;details.className='settings-section';details.open=index===0;details.innerHTML=`<summary><span><b>${label}</b><small>${hint}</small></span></summary>`;section.before(details);details.append(section);details.addEventListener('toggle',()=>{if(details.open)openOnly(body,details);});});const advanced=body.querySelector(':scope > details.advanced');if(advanced){advanced.dataset.settingsSection='assisted-instructions';advanced.classList.add('settings-section');const summary=advanced.querySelector(':scope > summary');if(summary)summary.innerHTML='<span><b>Istruzioni assistite</b><small>Facoltative; non trasferiscono autorità all’AI.</small></span>';advanced.addEventListener('toggle',()=>{if(advanced.open)openOnly(body,advanced);});}const submit=form.querySelector('button[type="submit"]');if(submit)submit.textContent='Salva configurazione AI';}function bindAdminShortcut(){document.addEventListener('click',event=>{const trigger=event.target.closest('[data-open-admin-center]');if(!trigger)return;event.preventDefault();$('#openAdminCenter')?.click();});}function renderCanonicalExperience(){document.documentElement.dataset.ictcExperience='active-1';renderProcedureHub();}export function installActiveExperience(){if(installed)return;installed=true;ensureMarketShell();ensureProgressiveSettings();installProofSurface();installSurfaceRouter();installBindings();installAdminCenter();installEnterpriseExperience();installFi01Reference();installGlobalTools();bindAdminShortcut();document.addEventListener('ictc:rendered',renderCanonicalExperience);renderCanonicalExperience();}
