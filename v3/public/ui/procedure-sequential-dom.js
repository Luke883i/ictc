import { $, esc, state } from './common.js';
import { GLOBAL_UX_DOD, PROCEDURE_SEQUENCE } from './procedure-sequential-policy.js';

export const selectedGrc=()=>state.activeProcessId||(()=>{try{return localStorage.getItem('ictc-grc-process')||'';}catch{return'';}})();
export const capability=name=>Array.isArray(state.data?.capabilities)&&state.data.capabilities.includes(name);

const QUEUE_STATE_LABELS=Object.freeze({candidate:'Da validare',active:'Attivi',rejected:'Esclusi',proposed:'Da decidere',mapped:'Mappati',gap:'Gap','not-applicable':'Fuori perimetro',open:'Aperte','in-progress':'In corso',blocked:'Bloccate','ready-for-review':'Da verificare',closed:'Chiuse',cancelled:'Annullate',reviewed:'Valutati',intake:'In ingresso',review:'Da approvare',approved:'Approvate'});

export function ensureStyle(){if(document.querySelector('link[data-sequential-ux-style]'))return;const link=document.createElement('link');link.rel='stylesheet';link.href='/ui/procedure-sequential-ux-2-2.css';link.dataset.sequentialUxStyle='';document.head.append(link);}
export function setPrimary(button,label){if(!button)return;button.classList.add('primary','seq-primary');button.classList.remove('secondary');button.textContent=label;button.setAttribute('aria-label',label);button.dataset.seqPrimary='true';}
export function secondary(button,label){if(!button)return;button.classList.remove('primary','seq-primary');button.classList.add('secondary');button.removeAttribute('data-seq-primary');if(label){button.textContent=label;button.setAttribute('aria-label',label);}}
const sameNodes=(left,right)=>left.length===right.length&&left.every((node,index)=>node===right[index]);
export function compactList(host,selector,budget=GLOBAL_UX_DOD.listItemsBeforeProgressiveDisclosure,label='Mostra altri elementi'){if(!host)return;let details=host.querySelector(':scope > details.seq-overflow'),slot=details?.querySelector(':scope > .seq-overflow-grid')||null;const direct=[...host.querySelectorAll(`:scope > ${selector}`)],overflow=slot?[...slot.querySelectorAll(`:scope > ${selector}`)]:[],nodes=[...direct,...overflow];if(nodes.length<=budget){if(details){for(const node of overflow)host.insertBefore(node,details);details.remove();}return;}if(!details){details=document.createElement('details');details.className='seq-overflow';details.innerHTML='<summary></summary><div class="seq-overflow-grid"></div>';host.append(details);slot=details.querySelector(':scope > .seq-overflow-grid');}const summary=details.querySelector(':scope > summary'),summaryText=`${esc(label)} · ${nodes.length-budget}`;if(summary.textContent!==summaryText)summary.textContent=summaryText;let cursor=details;for(const node of nodes.slice(0,budget).reverse()){if(node.parentElement!==host||node.nextElementSibling!==cursor)host.insertBefore(node,cursor);cursor=node;}const desiredOverflow=nodes.slice(budget),currentOverflow=[...slot.querySelectorAll(`:scope > ${selector}`)];if(!sameNodes(currentOverflow,desiredOverflow))for(const node of desiredOverflow)slot.append(node);}
function restoreLegacyOverflow(host){for(const details of [...host.querySelectorAll(':scope > details.seq-overflow')]){const slot=details.querySelector(':scope > .seq-overflow-grid');for(const node of [...slot?.children||[]])host.insertBefore(node,details);details.remove();}}
function annotateQueueControl(node,id,intent){if(!node)return;node.dataset.journeyProcess=id;node.dataset.journeyStage='orient';node.dataset.journeyIntent=intent;node.dataset.journeyAuthority='navigation';node.dataset.journeyEvidenceEffect='none';}
function queueTools(host,id,label){
  const parent=host.parentElement||host;
  let tools=parent.querySelector(`[data-procedure-queue-tools="${id}"],[data-seq-queue-tools="${id}"]`);
  if(!tools){tools=document.createElement('div');tools.className='procedure-queue-tools surface-toolbar';tools.dataset.seqQueueTools=id;tools.dataset.enduserPrimitive='ControlRail';tools.innerHTML=`<input type="search" data-seq-queue-search="${esc(id)}" placeholder="Cerca ${esc(label)}" aria-label="Cerca ${esc(label)}"><select data-seq-queue-state="${esc(id)}" aria-label="Filtra ${esc(label)} per stato"><option value="">Tutti gli stati</option></select>`;host.before(tools);}else{tools.dataset.seqQueueTools=id;tools.dataset.enduserPrimitive='ControlRail';}
  let search=tools.querySelector(`[data-seq-queue-search="${id}"]`),stateSelect=tools.querySelector(`[data-seq-queue-state="${id}"]`);
  if(id==='objects'){search=tools.querySelector('[data-seq-ao-search]')||search;stateSelect=tools.querySelector('[data-seq-ao-filter]')||stateSelect;}
  if(!search){search=document.createElement('input');search.type='search';search.dataset.seqQueueSearch=id;search.placeholder=`Cerca ${label}`;tools.prepend(search);}
  if(!stateSelect){stateSelect=document.createElement('select');stateSelect.dataset.seqQueueState=id;stateSelect.innerHTML='<option value="">Tutti gli stati</option>';tools.append(stateSelect);}
  search.dataset.seqQueueSearch=id;stateSelect.dataset.seqQueueState=id;if(id==='objects'){search.dataset.seqAoSearch='';stateSelect.dataset.seqAoFilter='';}
  search.setAttribute('aria-label',`Cerca ${label}`);stateSelect.setAttribute('aria-label',`Filtra ${label} per stato`);
  let count=tools.querySelector('[data-seq-queue-count]');if(!count){count=document.createElement('span');count.className='surface-chip';count.dataset.seqQueueCount='';count.setAttribute('aria-live','polite');tools.append(count);}
  let more=tools.querySelector('[data-seq-queue-more]');if(!more){more=document.createElement('button');more.type='button';more.className='secondary seq-queue-more';more.dataset.seqQueueMore=id;more.textContent='Mostra altri';tools.append(more);}
  annotateQueueControl(search,id,'filter-queue-text');annotateQueueControl(stateSelect,id,'filter-queue-state');annotateQueueControl(more,id,'expand-queue');return{tools,search,stateSelect,count,more};
}
function populateStates(select,cards,id){if(id==='objects'&&select.options.length>1)return;const current=select.value,values=[...new Set(cards.map(card=>card.dataset.recordState||card.dataset.uiuxPhase||'').filter(Boolean))];select.innerHTML='<option value="">Tutti gli stati</option>'+values.map(value=>`<option value="${esc(value)}">${esc(QUEUE_STATE_LABELS[value]||value.replaceAll('-',' '))}</option>`).join('');if([...select.options].some(option=>option.value===current))select.value=current;}
export function ensureQueueWindow(host,selector='article',{id,label='elementi',budget=GLOBAL_UX_DOD.listItemsBeforeProgressiveDisclosure}={}){if(!host||!id)return;restoreLegacyOverflow(host);const cards=[...host.querySelectorAll(`:scope > ${selector}`)];const ui=queueTools(host,id,label);populateStates(ui.stateSelect,cards,id);if(!ui.tools.dataset.seqQueueLimit)ui.tools.dataset.seqQueueLimit=String(budget);const refresh=(reset=false)=>{restoreLegacyOverflow(host);const all=[...host.querySelectorAll(`:scope > ${selector}`)];if(id==='incidents')for(const card of all){card.classList.add('p2-record-row');card.dataset.enduserPrimitive='RecordRow';card.dataset.recordGrammar='row-list';if(!card.dataset.recordId&&card.dataset.a6RecordId)card.dataset.recordId=card.dataset.a6RecordId;}if(reset)ui.tools.dataset.seqQueueLimit=String(budget);const limit=Math.max(budget,Number(ui.tools.dataset.seqQueueLimit||budget)),q=String(ui.search.value||'').trim().toLowerCase(),filter=ui.stateSelect.value||'';const matching=all.filter(card=>{const recordState=card.dataset.recordState||card.dataset.uiuxPhase||'',due=card.dataset.recordDue==='true',text=card.textContent.toLowerCase(),textOk=!q||text.includes(q),stateOk=!filter||recordState===filter||(filter==='due'&&due);return textOk&&stateOk;});const shown=Math.min(limit,matching.length),visible=new Set(matching.slice(0,limit));for(const card of all)card.hidden=!visible.has(card);ui.count.textContent=`${shown} di ${matching.length}`;const registry=host.closest('[data-a6-registry]'),registryCount=registry?.querySelector(':scope > summary [data-a6-registry-count]');if(registryCount)registryCount.textContent=`${shown} di ${matching.length}`;const remaining=Math.max(0,matching.length-limit);ui.more.hidden=remaining===0;ui.more.textContent=remaining?`Mostra altri ${Math.min(budget,remaining)}`:'Mostra altri';};if(ui.tools.dataset.seqQueueBound!=='true'){ui.tools.dataset.seqQueueBound='true';ui.search.addEventListener('input',event=>{refresh(true);event.stopPropagation();});ui.stateSelect.addEventListener('change',event=>{refresh(true);event.stopPropagation();});ui.more.addEventListener('click',()=>{ui.tools.dataset.seqQueueLimit=String(Number(ui.tools.dataset.seqQueueLimit||budget)+budget);refresh(false);});}refresh(false);}
export function ensureSequence(host,id){if(!host||!PROCEDURE_SEQUENCE[id])return;const context=host.querySelector(':scope > .procedure-support-rail > [data-editorial-slot="advanced-context"],:scope > [data-editorial-slot="advanced-context"]');let guide=host.querySelector('[data-seq-guide]');if(!guide){guide=document.createElement('nav');guide.dataset.seqGuide=id;guide.className='seq-guide';}if(context&&guide.parentElement!==context)context.append(guide);else if(!context&&guide.parentElement!==host)host.append(guide);const p=PROCEDURE_SEQUENCE[id];guide.setAttribute('aria-label',`${p.code} percorso operativo`);guide.innerHTML=`<span class="seq-purpose">${esc(p.purpose)}</span><ol>${p.stages.map((s,i)=>`<li><span>${i+1}</span>${esc(s)}</li>`).join('')}</ol>`;}
export { $ };
