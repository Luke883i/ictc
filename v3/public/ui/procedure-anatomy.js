import { esc, state } from './common.js';
import { SURFACE_LABELS } from './product-copy.js';
import { editorialSlot } from './procedure-editorial-slots.js';

let installed=false;
const scopeLabels={all:'Organizzazione',read:'Sola lettura','created-by':'Creati da te','assigned-or-created':'Assegnati o creati da te','organization-contribute':'Organizzazione · contributo',organization:'Organizzazione','own-monitors-plus-organization-sources':'Monitor personali · fonti organizzative'};
const alignmentLabels={'aligned-and-evidenced':'Applicazione con evidenza','practice-inspired':'Pratica di riferimento','implemented-baseline':'Baseline implementata','external-validation-required':'Validazione esterna richiesta','context-only':'Contesto',reference:'Riferimento dichiarato'};
function contract(id){return state.data?.procedureRegistry?.procedures?.find(item=>item.id===id)||null;}
function summary(id){return state.data?.procedureSummary?.rows?.find(item=>item.id===id)||null;}
function landscapeItem(id){const landscape=state.data?.processLandscape;if(!landscape)return null;return[...(landscape.phases||[]).flatMap(phase=>phase.items||[]),...(landscape.ungrouped||[])].find(item=>item.processId===id)||null;}
function decisionCount(id){const types=new Set(contract(id)?.adapter?.subjectTypes||[]);return(state.data?.decisions?.records||[]).filter(item=>types.has(item.subject?.type)).length;}
function versionCount(id){return(state.data?.subjectVersions?.records||[]).filter(item=>item.procedureId===id).length;}
function hostFor(id){const current=contract(id),surface=current?.adapter?.surface;if(surface==='monitoring')return document.querySelector('#monitoringView');if(surface==='incidents')return document.querySelector('#incidentsView');if(surface==='grc'){let active=state.activeProcessId||'';try{active=active||localStorage.getItem('ictc-grc-process')||'';}catch{}if(active!==id)return null;return document.querySelector('#grcWorkspace');}return null;}
function ensureCss(){if(document.querySelector('link[data-procedure-anatomy]'))return;const link=document.createElement('link');link.rel='stylesheet';link.href='/procedure-anatomy.css';link.dataset.procedureAnatomy='';document.head.append(link);}
function journey(process,stage,intent,authority='navigation',effect='none'){return `data-journey-process="${esc(process)}" data-journey-stage="${esc(stage)}" data-journey-intent="${esc(intent)}" data-journey-authority="${esc(authority)}" data-journey-evidence-effect="${esc(effect)}"`;}
function epistemicLegend(){const families=state.data?.procedureRegistry?.commonSubstrate?.epistemicFamilies||[];return families.map(item=>`<span>${esc(item)}</span>`).join('');}
function benchmarkMarkup(id){const applications=landscapeItem(id)?.benchmarkApplications||[];if(!applications.length)return'';const rows=applications.map(application=>{const name=application.name||application.ref,methodSteps=application.methodSteps||[],evidence=application.evidence||[],alignment=alignmentLabels[application.alignment]||alignmentLabels.reference,practice=application.practice||'Pratica dichiarata nel catalogo standard ICTC.',accessibleName=name||application.ref||'Riferimento standard applicato';return `<details class="procedure-standard-application" data-procedure-standard-application="${esc(application.ref)}"><summary ${journey(id,'reference','inspect-progressive-detail')} aria-label="Dettaglio standard applicato: ${esc(accessibleName)}"><span class="procedure-standard-name">${esc(name)}</span><span class="procedure-standard-meta">${esc(methodSteps.length)} passaggi · ${esc(alignment)}</span></summary><div class="procedure-standard-body"><p class="procedure-standard-practice"><b>Come ICTC applica</b><span>${esc(practice)}</span></p><div class="procedure-standard-methods"><small>Passaggi del metodo</small><ol>${methodSteps.map(step=>`<li>${esc(step)}</li>`).join('')}</ol></div>${evidence.length?`<p class="procedure-standard-evidence"><b>Prova:</b> <span>${evidence.map(esc).join(' · ')}</span></p>`:''}${application.limit?`<p class="procedure-standard-limit"><b>Limite:</b> <span>${esc(application.limit)}</span></p>`:''}</div></details>`;}).join('');return `<details class="procedure-anatomy-standards" data-procedure-standard-applications="${esc(id)}"><summary class="procedure-anatomy-standard-head" ${journey(id,'reference','inspect-progressive-detail')} aria-label="Standard e pratiche applicate"><span>Standard e pratiche applicate</span><small>${esc(applications.length)} riferimenti · dettagli su richiesta</small></summary><div class="procedure-anatomy-standard-body">${rows}<p class="procedure-standard-boundary"><small>Il collegamento mostra dove il metodo ICTC applica il riferimento dichiarato; non è una certificazione o una decisione di applicabilità legale.</small></p></div></details>`;}
function anatomyMarkup(id,current,access,asOf,decisions,versions){const procedureLabel=current.code||current.id||id,proofAction=`Apri ${SURFACE_LABELS.proof}`;return `<summary ${journey(id,'advanced-context','inspect-progressive-detail')} aria-label="Contesto e tracciabilità"><span>Contesto e tracciabilità</span><small>${esc(decisions)} decisioni · ${esc(versions)} versioni</small></summary><div class="procedure-anatomy-grid"><div><small>Ambito visibile</small><strong>${esc(scopeLabels[access]||access)}</strong></div><div><small>Vista dati</small><strong>${asOf?esc(new Date(asOf).toLocaleString('it-IT')):'corrente'}</strong></div><div><small>Decisioni umane visibili</small><strong>${esc(decisions)}</strong></div><div><small>Versioni registrate</small><strong>${esc(versions)}</strong></div></div><div class="procedure-anatomy-epistemic" aria-label="Legenda epistemica">${epistemicLegend()}</div><p class="procedure-anatomy-boundary">${esc(current.claimBoundary)}</p><div class="procedure-anatomy-actions"><button type="button" data-service="proof" ${journey(id,'evidence','inspect-evidence','navigation','consume')} aria-label="${esc(proofAction)} per ${esc(procedureLabel)}">${esc(proofAction)}</button><small>L’AI assiste; non diventa autorità decisionale.</small></div>`;}
function syncAnatomy(contextHost,id,markup){for(const stale of contextHost.querySelectorAll(':scope > [data-procedure-anatomy]'))if(stale.dataset.procedureAnatomy!==id)stale.remove();let box=contextHost.querySelector(`:scope > [data-procedure-anatomy="${CSS.escape(id)}"]`);if(!box){box=document.createElement('details');box.className='procedure-anatomy';box.dataset.procedureAnatomy=id;contextHost.append(box);}if(box.__ictcAnatomyMarkup!==markup){const wasOpen=box.open;box.innerHTML=markup;box.open=wasOpen;box.__ictcAnatomyMarkup=markup;}return box;}
function syncStandards(referenceHost,id,markup){for(const stale of referenceHost.querySelectorAll(':scope > [data-procedure-standard-applications]'))if(stale.dataset.procedureStandardApplications!==id)stale.remove();const selector=`:scope > [data-procedure-standard-applications="${CSS.escape(id)}"]`;const existing=referenceHost.querySelector(selector);if(!markup){existing?.remove();return null;}if(existing?.__ictcStandardsMarkup===markup)return existing;const openRefs=new Set([...existing?.querySelectorAll('.procedure-standard-application[open]')||[]].map(node=>node.dataset.procedureStandardApplication).filter(Boolean)),outerOpen=Boolean(existing?.open);const template=document.createElement('template');template.innerHTML=markup.trim();const next=template.content.firstElementChild;if(!next)return existing||null;next.__ictcStandardsMarkup=markup;next.open=outerOpen;for(const detail of next.querySelectorAll('.procedure-standard-application'))if(openRefs.has(detail.dataset.procedureStandardApplication))detail.open=true;if(existing)existing.replaceWith(next);else referenceHost.append(next);return next;}
function renderOne(id){const host=hostFor(id),current=contract(id),row=summary(id);if(!host||!current||!row)return;const contextHost=editorialSlot(host,'advanced-context'),referenceHost=editorialSlot(host,'reference');if(!contextHost||!referenceHost)return;const asOf=state.data?.projectionContext?.asOf,access=current.accessPolicy?.[state.role]||current.access?.[state.role]||current.adapter?.userScope||'actor-visible',decisions=decisionCount(id),versions=versionCount(id);syncAnatomy(contextHost,id,anatomyMarkup(id,current,access,asOf,decisions,versions));syncStandards(referenceHost,id,benchmarkMarkup(id));}
function render(){for(const current of state.data?.procedureRegistry?.procedures||[])renderOne(current.id);}
function scheduleRender(){queueMicrotask(render);}
export function installProcedureAnatomy(){if(installed)return;installed=true;ensureCss();render();document.addEventListener('ictc:rendered',render);document.addEventListener('ictc:surface-changed',scheduleRender);document.addEventListener('ictc:editorial-slots-ready',scheduleRender);window.addEventListener('click',event=>{if(event.target.closest?.('[data-grc-process]'))scheduleRender();},true);document.addEventListener('click',event=>{const proof=event.target.closest('[data-procedure-anatomy] [data-service="proof"]');if(proof){document.querySelector('.service-nav [data-service="proof"]')?.click();return;}if(event.target.closest('[data-workspace-return]'))scheduleRender();});}
