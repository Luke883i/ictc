import { $, api, downloadProtected, esc, notify, state } from './common.js';
let cachedRole=null;let cachedProof=null;let loading=null;
function processForSubject(type){return(state.data?.procedureRegistry?.procedures||[]).find(item=>(item.adapter?.subjectTypes||[]).includes(type))||null;}
function ensureSurface(){const main=$('#main');if(!main||$('#proofView'))return;main.insertAdjacentHTML('beforeend',`<section id="proofView" class="view proof-view" hidden tabindex="-1" aria-labelledby="proofTitle"><header class="proof-head"><div><p class="eyebrow">Evidenze</p><h1 id="proofTitle">Evidenze e tracciabilità</h1><p>Ricostruisci le decisioni umane e il contesto che le sostiene. Integrità e provenienza non equivalgono a sufficienza o conformità.</p></div></header><div id="proofLoading" class="proof-state" role="status">Caricamento delle evidenze…</div><div id="proofError" class="proof-state boundary" role="alert" hidden></div><div id="proofContent" hidden><section class="evidence-summary" aria-label="Sintesi evidenze"><article><span>Decisioni umane</span><b id="evidenceDecisionCount">0</b></article><article><span>Processi con decisioni</span><b id="evidenceProcessCount">0</b></article><article><span>Ambito di lettura</span><b id="evidenceActor">—</b></article><article><span>Integrità locale</span><b id="proofIntegrity">—</b></article></section><section class="evidence-decisions home-panel"><header><div><p class="eyebrow">Decisioni</p><h2>Decisioni umane recenti</h2><p>Ogni voce conserva checkpoint, esito, motivazione e versione del soggetto quando disponibili.</p></div></header><div id="evidenceDecisionList" class="evidence-decision-list"></div></section><section class="home-panel evidence-boundary"><p class="eyebrow">Come leggere questa vista</p><h2>Traccia disponibile, conclusione separata</h2><p>ICTC mostra ciò che è stato registrato e come una decisione è collegata al suo soggetto. La presenza di un dossier o di un hash non dimostra automaticamente che la prova sia sufficiente.</p></section><details id="proofAdvanced" class="proof-advanced"><summary><span><b>Postura tecnica, export e limiti</b><small>Runtime, deployment, translation pack e formati same-as-read</small></span></summary><div class="proof-advanced-body"><section class="proof-metrics" aria-label="Postura tecnica"><article><span>Release</span><b id="proofRelease">—</b></article><article><span>Ruolo</span><b id="proofActor">—</b></article><article><span>Controlli runtime</span><b id="proofRuntime">—</b></article></section><section class="proof-grid"><article class="home-panel"><p class="eyebrow">Limite</p><h2>Cosa questa prova non conclude</h2><p id="proofBoundary"></p><p id="proofReadinessLimit" class="boundary"></p></article><article class="home-panel"><p class="eyebrow">Deployment</p><h2>Attestazioni ancora richieste</h2><ul id="proofDeploymentGaps" class="proof-list"></ul></article></section><section class="home-panel"><p class="eyebrow">Traduzioni esterne</p><h2>Pratica, riferimento e limite</h2><div id="translationMappings" class="proof-mappings"></div></section><div class="proof-actions"><button id="downloadViewCsv" class="secondary" type="button">Esporta CSV</button><button id="downloadViewJson" class="secondary" type="button">Esporta JSON</button><button id="refreshProof" class="secondary" type="button">Aggiorna postura</button></div></div></details></div></section>`);}
function blockerMarkup(items){if(!items.length)return'<li>Nessun blocker di deployment nella projection corrente.</li>';return items.map(item=>`<li><b>${esc(item.label)}</b><span>${esc(item.evidence||'Evidenza non disponibile')}</span>${item.action?`<small>${esc(item.action)}</small>`:''}</li>`).join('');}
function translationMarkup(){const packs=state.data?.translationPacks?.packs||[];if(!packs.length)return'<div class="empty">Nessuna translation pack disponibile.</div>';return packs.map(pack=>`<details class="proof-mapping"><summary><span><b>${esc(pack.framework)} · ${esc(pack.edition)}</b><small>non valutato</small></span></summary><div>${(pack.mappings||[]).map(item=>`<p><b>${esc(item.externalRef)}</b> ← ${esc(item.sourceRef)}<br><span>${esc(item.mappingKind)}</span></p><p class="boundary"><b>Limite.</b> ${esc(item.limitation)}</p>`).join('')}</div></details>`).join('');}
function decisionMarkup(){const records=[...(state.data?.decisions?.records||[])].reverse().slice(0,12);if(!records.length)return'<div class="empty">Nessuna decisione umana registrata nel perimetro accessibile.</div>';return records.map(record=>{const procedure=processForSubject(record.subject?.type),code=procedure?.code||'—',label=procedure?.label||'Processo';const when=record.at?new Date(record.at).toLocaleString('it-IT'):'Data non disponibile';return `<article class="evidence-decision"><div class="evidence-decision-meta"><span>${esc(code)}</span><small>${esc(label)}</small></div><div><h3>${esc(record.outcome||record.kind)}</h3><p>${esc(record.reason||'Motivazione non registrata.')}</p><small>${esc(record.checkpoint||record.kind)} · ${esc(record.actor?.id||'actor non disponibile')} · ${esc(when)}</small></div></article>`;}).join('');}
function renderBusinessEvidence(){const records=state.data?.decisions?.records||[],processes=new Set(records.map(record=>processForSubject(record.subject?.type)?.id).filter(Boolean));$('#evidenceDecisionCount').textContent=records.length;$('#evidenceProcessCount').textContent=processes.size;$('#evidenceActor').textContent=state.role==='admin'?'Amministrazione':state.role==='auditor'?'Auditor':'Utente';$('#evidenceDecisionList').innerHTML=decisionMarkup();}
function renderProof(data){cachedProof=data;cachedRole=state.role;const posture=data.proof?.posture||{},runtime=posture.runtime||{verified:0,total:0},deployment=posture.deployment||{blockers:[]},integrity=data.proof?.integrity||{};renderBusinessEvidence();$('#proofRelease').textContent=data.release||'Non disponibile';$('#proofActor').textContent=`${data.actor?.label||data.actor?.role||'Ruolo'} · ${data.actor?.modeLabel||data.actor?.mode||'modalità non disponibile'}`;$('#proofRuntime').textContent=`${runtime.verified||0}/${runtime.total||0} verificati`;$('#proofIntegrity').textContent=integrity.ok?`Coerente · r${integrity.revision||0}`:'Da verificare';$('#proofBoundary').textContent=data.claimBoundary||'Nessuna conclusione sostanziale viene inferita dalla sola presenza di evidenze.';$('#proofReadinessLimit').textContent=posture.limitation||'Un controllo senza evidenza resta non verificato.';$('#proofDeploymentGaps').innerHTML=blockerMarkup(deployment.blockers||[]);$('#translationMappings').innerHTML=translationMarkup();$('#proofLoading').hidden=true;$('#proofError').hidden=true;$('#proofContent').hidden=false;}
async function loadProof(force=false){if(!force&&cachedProof&&cachedRole===state.role){renderProof(cachedProof);return cachedProof;}if(loading)return loading;$('#proofLoading').hidden=false;$('#proofContent').hidden=true;$('#proofError').hidden=true;loading=api('/api/standard-proof').then(renderProof).catch(error=>{$('#proofLoading').hidden=true;$('#proofError').hidden=false;$('#proofError').textContent=`Evidenze non disponibili: ${error.message}`;throw error;}).finally(()=>{loading=null;});return loading;}
export function installProofSurface(){ensureSurface();$('#refreshProof')?.addEventListener('click',()=>loadProof(true).catch(()=>{}));$('#downloadViewCsv')?.addEventListener('click',async()=>{try{await downloadProtected('/api/export/current.csv','ictc-current-view.csv');}catch(error){notify(error.message,true);}});$('#downloadViewJson')?.addEventListener('click',async()=>{try{await downloadProtected('/api/export/current.json','ictc-current-view.json');}catch(error){notify(error.message,true);}});document.addEventListener('ictc:surface-changed',event=>{if(event.detail?.surface==='proof')loadProof().catch(()=>{});});document.addEventListener('ictc:rendered',()=>{if(state.service==='proof')loadProof(true).catch(()=>{});});}
