import { $, esc, state } from './common.js';
let installed=false;
function ensureStyle(){if(document.querySelector('link[data-process-landscape-style]'))return;const link=document.createElement('link');link.rel='stylesheet';link.href='process-landscape.css';link.dataset.processLandscapeStyle='true';document.head.append(link);}
function ensureHost(){ensureStyle();const view=$('#processesView'),hub=$('#procedureHub');if(!view||!hub)return null;if(!$('#processLandscape')){const section=document.createElement('section');section.id='processLandscape';section.className='process-landscape';section.innerHTML='<header class="process-landscape-intro"><div><p class="eyebrow">Ciclo di compliance</p><h2>Scegli il processo, anche senza AI</h2><p>Questa mappa raggruppa le procedure per scopo operativo. Non è una sequenza obbligatoria: puoi aprire direttamente il processo che conosci.</p></div><div class="process-landscape-legend"><span>● lavoro aperto</span><span>◆ decisione umana</span><span>□ prova</span></div></header><div id="processLandscapePhases"></div><details class="process-landscape-fallback"><summary>Tutte le procedure e i dettagli</summary><p>Il catalogo completo qui sotto resta la vista esaustiva, con metriche, limiti e accesso diretto.</p></details>';hub.before(section);}return $('#processLandscape');}
function itemMarkup(item){const badges=[item.attentionCount?`${item.attentionCount} da vedere`:null,item.reviewCount?`${item.reviewCount} review umane`:null,item.workCount?`${item.workCount} lavori`:null,item.aiOptional?'AI opzionale':null].filter(Boolean);const action=item.processId==='evidence'?'<button class="secondary" type="button" data-service="proof">Apri prove</button>':`<button class="primary" type="button" data-v3-guide="${esc(item.processId)}">${esc(item.actionLabel)}</button>`;return`<article class="process-landscape-node" data-process-id="${esc(item.processId)}" data-read-only="${item.readOnly?'true':'false'}"><header><span class="process-code">${esc(item.processCode||'')}</span><h3>${esc(item.label)}</h3></header><p>${esc(item.purpose)}</p><div class="process-landscape-badges">${badges.map(x=>`<span>${esc(x)}</span>`).join('')}</div><div class="process-landscape-next"><small>Cosa succede dopo</small><b>${esc(item.checkpoint?`Checkpoint umano: ${item.checkpoint}`:'Consultazione e prova')}</b></div><div class="process-landscape-proof"><small>Prova che resta</small><span>${esc(item.proofHint)}</span></div><footer>${action}</footer></article>`;}
function render(){const host=ensureHost(),data=state.data?.processLandscape;if(!host||!data)return;const phases=$('#processLandscapePhases');phases.innerHTML=(data.phases||[]).map(phase=>`<section class="process-landscape-phase"><header><h3>${esc(phase.label)}</h3><p>${esc(phase.description)}</p></header><div class="process-landscape-row">${(phase.items||[]).map(itemMarkup).join('')||'<p class="quiet">Nessun processo disponibile per il ruolo.</p>'}</div></section>`).join('');host.dataset.landscapeAuthority=data.authority||'';}
export function installProcessLandscape(){if(installed)return;installed=true;ensureHost();document.addEventListener('ictc:rendered',render);render();}
