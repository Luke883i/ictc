import assert from 'node:assert/strict';
import { readFile } from 'node:fs/promises';
const read=p=>readFile(new URL(p,import.meta.url),'utf8');
const [frame,anatomy,market,css,styles,convergence,contracts,meta,doc,browser,compositionCss,active,compositionRuntime,native,nativeWorkspace,workspaceCss]=await Promise.all([read('./public/ui/procedure-frame.js'),read('./public/ui/procedure-anatomy.js'),read('./public/ui/procedure-market-ux.js'),read('./public/onto-compliance-v1.css'),read('./public/styles.css'),read('./public/ui-convergence.css'),read('./procedure-contracts-1-2.json'),read('./runtime/meta-procedure-contracts.mjs'),read('../docs/ONTO_COMPLIANCE_HORIZON_V1.md'),read('./browser-onto-compliance-v1.py'),read('./public/semantic-composition-3-1.css'),read('./public/ui/active-experience.js'),read('./public/ui/semantic-composition-runtime.js'),read('./public/ui/native-semantic-lattice-3-2.js'),read('./public/ui/native-workspace-3-2.js'),read('./public/enterprise-workspace-3-2.css')]);
const registry=JSON.parse(contracts).procedures;assert.equal(registry.length,7,'business process registry must remain exactly seven');assert.ok(meta.includes("businessProcess:false")&&meta.includes("code:'EP-01'"),'EP-01 must remain cross-cutting, not an eighth business process');
for(const token of ['PROCEDURE_WORKSPACE','Consulta registrazioni','openReadSurface','readTarget','data-procedure-frame-variant="matrix"'])assert.ok(frame.includes(token),`native process frame missing ${token}`);for(const forbidden of ["'In ordine'",'Nessuna attenzione aperta','<span>Processo di Compliance</span>','<b>Scopo del processo</b>','<small>da vedere</small>','<small>registrazioni</small>','semanticSignals','Consulta record','Scopo della procedura'])assert.equal(frame.includes(forbidden),false,`process frame retains forbidden pattern ${forbidden}`);
assert.ok(anatomy.includes('function workAnchor(')&&anatomy.includes('anchor.after(box)'),'technical anatomy must own placement after native work');assert.equal(anatomy.includes('host.prepend(box)'),false,'technical trace must not prepend ahead of native work');assert.ok(anatomy.includes('queueMicrotask(render)'),'anatomy rerender must converge in current event turn');assert.ok(market.includes('procedure senza attenzione aperta'),'legacy market summary may preserve observational language');
for(const token of ['market-scope-editor','scopeOptions(decision)',"decision===value?'selected':''",'f.scope?.reason'])assert.ok(market.includes(token),`coverage disclosure missing ${token}`);assert.ok(css.includes('.market-scope.in-scope'),'scope visual state remains available to native owner');
const imports=[...styles.matchAll(/@import\s+url\(['"]?([^)'"\s]+)['"]?\)/g)].map(match=>match[1]);for(const layer of ['./onto-compliance-v1.css','./procedure-finetuning-1-4.css','./procedure-executive-harmonization-1-5.css','./visual-epistemic-runtime.css','./ui-convergence.css','./semantic-composition-3-1.css'])assert.ok(imports.includes(layer),`cascade lineage layer missing ${layer}`);assert.doesNotMatch(compositionCss,/(?:^|[;{])\s*(?:color|background-color)\s*:/m,'3.1 composition lineage must not create independent palette authority');
for(const retired of ['function compactHome','function compactProcesses','function compactNative','function compactGrc','function compactProof','function compactEpistemic','function compactAdmin','function compactAiSettings'])assert.equal(compositionRuntime.includes(retired),false,`3.2 global runtime must retire local adapter ${retired}`);assert.ok(active.includes('installNativeWorkspace32')&&!compositionRuntime.includes('installNativeWorkspace32')&&compositionRuntime.includes('annotateSurface'),'3.2 composition root/local-owner/global-annotation topology missing');assert.ok(native.includes('PROCEDURE_WORKSPACE')&&native.includes('LOCAL_COMPOSITION_OWNERS'),'native lattice contract missing');assert.ok(nativeWorkspace.includes("link.href='/enterprise-workspace-3-2.css'"),'3.2 visual layer bootstrap missing');for(const token of ['#procedureHub','repeat(3,minmax(0,1fr))','--workspace-header','--workspace-footer'])assert.ok(workspaceCss.includes(token),`3.2 workspace visual contract missing ${token}`);
for(const token of ['Identity','Action','Work','Evidence / Trace','M+100','G+100','Rice'])assert.ok(doc.includes(token),`design lineage contract missing ${token}`);for(const token of ["'work':'#monitoringView > .section-block'","'work':'#incidentsView > .section-block'",'auditor-primary-leaves-process','epistemic-family-convergence'])assert.ok(browser.includes(token),`runtime visual audit missing ${token}`);
console.log('onto-compliance-horizon-check: ok (seven-process authority + native 3.2 root bootstrap + minimal semantic kernel)');
