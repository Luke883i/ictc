import assert from 'node:assert/strict';
import { readFile } from 'node:fs/promises';
import { standardCatalog, standardLibraryProjection, resolveRequirementRef } from './runtime/standard-library.mjs';
import { procedureSummaryProjection } from './runtime/procedure-summary.mjs';
import { canonicalProcedureRegistry } from './runtime/procedure-registry.mjs';
import { procedureIdsForWritePath, PROCEDURE_SCOPE } from './runtime/procedure-policy.mjs';
const read=path=>readFile(new URL(path,import.meta.url),'utf8');
const product=JSON.parse(await read('./product-contract.json')),snapshot=JSON.parse(await read('./product-contract-1-2.json')),registry=canonicalProcedureRegistry(),catalog=standardCatalog(),shell=await read('./public/ui/stable-shell.js'),index=await read('./public/index.html'),market=await read('./public/ui/procedure-market-ux.js'),server=await read('./server.mjs'),copy=await read('./public/ui/product-copy.js');
assert.equal(product.stabilityProfile,'1.2_market_candidate');assert.equal(product.productEdition,'1.2 Market Candidate');assert.equal(product.services.length,7);assert.equal(snapshot.stabilityProfile,'1.2_market_candidate');assert.ok(product.roles.find(x=>x.id==='user').permissions.includes('manage-own-monitoring'));assert.equal(product.standardLibrary.equivalenceInference,false);assert.equal(product.standardLibrary.applicabilityInference,false);assert.equal(product.standardLibrary.controlTestSeparate,true);assert.equal(product.orchestration.procedureSummary.syntheticComplianceScore,false);
assert.equal(registry.authority,'canonical-procedure-registry');assert.equal(registry.schemaVersion,'1.3.0');assert.equal(registry.procedures.length,7);assert.equal(registry.procedures.find(x=>x.id==='coverage').label,'Standard e Controlli');assert.ok(registry.procedures.find(x=>x.id==='incidents').entryPoints.includes('near-miss'));assert.ok(registry.procedures.find(x=>x.id==='monitoring').entryPoints.includes('internal-master-reference'));assert.equal(registry.procedures.find(x=>x.id==='monitoring').ux.primaryAction,'Aggiungi materiale');assert.deepEqual(registry.commonSubstrate.epistemicFamilies,['observed','derived','proposed','decided','attested']);
assert.equal(catalog.frameworks.length,21);for(const id of ['eu-gdpr-2016-679','eu-nis2-2022-2555','eu-dora-2022-2554','it-dlgs-231-2001','iso-iec-27001-2022','iso-37301-2021','coso-ic-2013','coso-erm-2017','nist-csf-2','nist-sp-800-53-r5','cis-controls-8-1','cobit-2019','pci-dss-4-0-1'])assert.ok(catalog.frameworks.some(x=>x.id===id),id);assert.equal(catalog.frameworks.find(x=>x.id==='iso-iec-27001-2022').nodes.length,93);assert.equal(catalog.frameworks.find(x=>x.id==='iso-iec-27002-2022').nodes.length,93);assert.equal(catalog.frameworks.find(x=>x.id==='nist-csf-2').nodes.length,22);assert.equal(catalog.frameworks.find(x=>x.id==='coso-ic-2013').nodes.length,17);assert.equal(catalog.frameworks.find(x=>x.id==='coso-erm-2017').nodes.length,20);assert.equal(catalog.frameworks.find(x=>x.id==='cobit-2019').nodes.length,40);for(const f of catalog.frameworks.filter(x=>x.contentPolicy==='licensed-reference-index'))assert.ok(f.nodes.every(n=>!n.text),`${f.id} must not bundle normative text`);assert.equal(resolveRequirementRef('std:eu-gdpr-2016-679:art-32',{}).ref,'Art. 32');assert.equal(resolveRequirementRef('std:iso-iec-27001-2022:A.5.1',{}).ref,'A.5.1');
assert.equal(PROCEDURE_SCOPE.monitoring,'own-monitors-plus-organization-sources');assert.deepEqual(procedureIdsForWritePath('/api/standards/eu-gdpr-2016-679/scope'),['coverage']);assert.deepEqual(procedureIdsForWritePath('/api/user/monitors/m1/run'),['monitoring']);const summary=procedureSummaryProjection({revision:0,settings:{},missions:[],runs:[],catalog:[],incidents:[],grcObjects:[],grcMappings:[],grcActions:[],grcRisks:[],grcAssurance:[],standardScopes:[],standardPacks:[],standardPackHistory:[],standardCrosswalks:[],requirementScopes:[]},{id:'admin',role:'admin'},{asOf:'2026-01-01T00:00:00.000Z'});assert.equal(summary.rows.length,7);assert.ok(summary.rows.every(x=>x.metrics.length<=4&&x.metrics.every(m=>m.basis?.drilldown)));assert.ok(!('complianceScore'in summary.counts)&&!('maturityScore'in summary.counts));assert.equal(summary.rows.find(x=>x.id==='coverage').label,'Standard e Controlli');
assert.match(index,/data-ictc-edition="1\.2-market-candidate"/);assert.match(index,/data-ictc-experience="market-1"/);assert.match(index,/<title>ICTC · Compliance operativa ed evidenze<\/title>/);assert.match(index,/>Home<\/button>/);assert.match(index,/>Evidenze ICTC<\/button>/);assert.match(index,/>Processi di Compliance<\/button>/);assert.match(copy,/home:'Home'/);assert.match(copy,/proof:'Evidenze ICTC'/);assert.match(shell,/assertNativeShell/);assert.doesNotMatch(shell,/dataset\.ictcEdition\s*=/);for(const token of ['Aggiungi materiale','Crea monitoraggio','Quasi incidente','Standard e Controlli','Obiettivi di controllo condivisi','non è un punteggio di conformità'])assert.ok(market.includes(token),token);assert.ok(server.includes("product-contract.json"));assert.ok(!server.includes("readFile(path.join(here,'product-contract-1-2.json')"));for(const token of ['procedureSummaryProjection','standardLibraryProjection','visibleMissions','projectionContext','canonicalProcedureRegistry','subjectVersionIndexProjection','controlTestProjection'])assert.ok(server.includes(token),token);
const standards=standardLibraryProjection({}, {id:'a',role:'admin'});assert.equal(standards.counts.available,21);assert.equal(standards.counts.notAssessed,21);assert.ok(standards.limitations.some(x=>/Applicability is a human scope decision/.test(x)));assert.ok(standards.limitations.some(x=>/Identifiers and structural references only/.test(x)));console.log('v1-2-market-candidate-check: ok (native shell + successor registry 1.3 + canonical Home/Processi/Evidenze navigation)');
