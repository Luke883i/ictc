{
  "schemaVersion": "1.1.0",
  "id": "bootstrap-semantic-ratchet",
  "surface": "GET /api/bootstrap",
  "authority": "server-runtime",
  "fields": {
    "capabilities": {"authority": "actor.permissions", "type": "array", "items": "permission-id"},
    "accessProfile": {"authority": "accessProfileFor(actor)", "type": "object"},
    "experience.release": {"authority": "VERSION", "type": "semver"},
    "ontology": {"authority": "runtimeOntologyProjection", "schemaVersion": "1.1.0", "kernelAuthority": "process-kernel.json via runtime/process-kernel.mjs", "required": ["archetypes", "processes", "relations", "objects", "states", "ctas"]},
    "homeNextAction": {"authority": "canonicalHomeNextAction", "schemaVersion": "1.0.0", "required": ["kind", "title", "label", "reason", "action", "service", "targetType", "targetId", "readOnly"]},
    "procedures": {"authority": "canonicalProcedureHub <- ProcessDefinition registry", "itemSchemaVersion": "1.1.0", "required": ["id", "code", "kind", "archetype", "label", "description", "action", "actionLabel", "service", "readOnly", "state", "attentionCount", "metrics", "claimBoundary"]}
  },
  "ratchetRule": "Later semantic slices extend this contract; they do not create a competing bootstrap semantic authority, local relation vocabulary, browser process catalog, decision authority or evidence authority.",
  "limitations": [
    "This contract verifies declared schema, derivation bindings and referential integrity; it does not prove authorization correctness for every request.",
    "ProcessDefinition and relation grammar are bounded runtime semantics; they do not determine legal applicability, compliance, certification or substantive truth."
  ]
}
