import assert from 'node:assert/strict';
import { readFile } from 'node:fs/promises';
import { EPISTEMIC_PROFESSIONAL_LENSES, professionalLensProjection } from './runtime/epistemic-professional-lenses.mjs';
const read=p=>readFile(new URL(p,import.meta.url),'utf8');
const [ui,lensUi,browser,suite,lattice,active]=await Promise.all([read('./public/ui/epistemic-lattice.js'),read('./public/ui/epistemic-professional-lenses.js'),read('./browser-epistemic-professional-demo.py'),read('./runtime/demo-suite-2-2.mjs'),read('./runtime/epistemic-lattice.mjs'),read('./public/ui/active-experience.js')]);
assert.equal(EPISTEMIC_PROFESSIONAL_LENSES.length,12,'declared professional-use taxonomy must stay twelve bounded lenses');
const expected=['compliance-lead','internal-auditor','dpo-privacy','security-manager','risk-manager','control-owner','assurance-reviewer','legal-231-reviewer','it-operations','supplier-procurement','quality-manager','executive-sme'];
assert.deepEqual(EPISTEMIC_PROFESSIONAL_LENSES.map(x=>x.id),expected);
for(const lens of EPISTEMIC_PROFESSIONAL_LENSES){assert.ok(lens.label&&lens.professionalRole&&lens.purpose&&lens.workingQuestion&&lens.guardrail,`${lens.id}: incomplete lens semantics`);assert.ok(['explore','flat','graph'].includes(lens.preferredMode),`${lens.id}: invalid preferred mode`);assert.ok(Array.isArray(lens.procedures)&&lens.procedures.length>=1,`${lens.id}: procedure scope missing`);assert.ok(lens.procedures.every(id=>['monitoring','incidents','objects','coverage','actions','risks','assurance'].includes(id)),`${lens.id}: unknown procedure`);assert.deepEqual(lens.allowedRoles,['admin','auditor'],`${lens.id}: lens cannot expand RBAC`);assert.match(lens.guardrail,/non|not|never|non determina|non prova|non infer/i,`${lens.id}: weak claim boundary`);}
const projection=professionalLensProjection();assert.equal(projection.schemaVersion,'1.0.0');assert.equal(projection.lenses.length,12);assert.match(projection.claimBoundary,/non.*autor|not.*author|non.*decision/i);
const productText=[ui,lensUi,lattice].join('\n');for(const token of ['data-epistemic-lens','epistemicLensContext','Lente professionale','Contesto PMI','businessThread','professionalLenses','conteggi, non score'])assert.ok(productText.includes(token),`professional EP-01 product contract missing ${token}`);
assert.ok(active.includes('installEpistemicProfessionalLenses'),'professional lens enhancer must be in active experience');assert.ok(lensUi.includes('installed=true;ensureLensUi();bind();sync();'),'professional lens structure must mount deterministically after canonical EP-01 view creation, before lazy data load');
assert.ok(browser.includes("LENSES=['compliance-lead','internal-auditor','dpo-privacy','security-manager','risk-manager','control-owner','assurance-reviewer','legal-231-reviewer','it-operations','supplier-procurement','quality-manager','executive-sme']"),'browser must enumerate the exact bounded professional lens taxonomy');assert.ok(browser.includes("shot(page,f'epistemic-lens-{lens_id}.png')"),'browser must materialize one screenshot per enumerated lens');
for(const token of ['DEMO_SUITE_22_VERSION','demoSuite22Projection','projectionAuthority','stressVisible:false','positiveRecords:188'])assert.ok(suite.includes(token),`Suite 2.2 professional DEMO contract missing ${token}`);assert.ok(suite.includes("from './demo-suite-2-2-fixture.mjs'")&&suite.includes('export { DEMO_SUITE_22_ENV, DEMO_SUITE_22_EXPECTED_DIGEST, DEMO_SUITE_22_ID, DEMO_SUITE_22_SCENARIO, DEMO_SUITE_22_VERSION'),'Suite 2.2 version/digest identity must remain owned by fixture and re-exported by runtime owner');
assert.ok(browser.includes("demo['projectionAuthority']=='demo-suite-2-2'")&&browser.includes("demo['positiveRecords']==188")&&browser.includes("demo['stressVisible'] is False"),'professional browser must assert the canonical Suite 2.2 projection boundary');
assert.ok(lensUi.includes('currentThread')&&lensUi.includes('data-epistemic-basis')&&lensUi.includes('epistemicContextSearch'),'EP-01 professional context search must route back through canonical atom navigation');
assert.ok(lattice.includes("schemaVersion:'1.3.0'")&&lattice.includes('epistemicProfessionalDiagnostics')&&lattice.includes('rnSemanticAtoms'),'lattice projection v1.3 must expose diagnostics plus bounded RN semantic atoms without changing decision authority');
assert.ok(browser.includes('sameProjectionDigestAcrossLenses')&&browser.includes('demoProjectionAuthority'),'browser must prove lens digest conservation and Suite 2.2 projection authority');
console.log('epistemic-professional-use-check: ok (12 bounded lenses / EP-01 v1.3 / deterministic lens mount / Suite 2.2 synthetic projection / screenshot matrix contract)');
