import assert from 'node:assert/strict';import {id} from './domain.mjs';import {normalizeAssuranceCase,attachAssuranceProposal,approveAssurance,assuranceProjection} from './runtime/grc-assurance.mjs';const admin={id:'admin',role:'admin'};const item=normalizeAssuranceCase({title:'Questionario cliente',requestText:'Descrivere MFA e logging.'},admin,{idFactory:id});assert.equal(item.state,'intake');assert.match(item.requestSha256,/^[a-f0-9]{64}$/);attachAssuranceProposal(item,{summary:'2 domande',questions:[{id:'q1',question:'MFA?',draftAnswer:'MFA attivo',evidenceRefs:['obj1']},{id:'q2',question:'Logging?',draftAnswer:'Log presenti',evidenceRefs:['ev1']}]},{purpose:'assurance-draft'});assert.equal(item.state,'review');assert.equal(item.approvedAnswers.length,0);approveAssurance(item,{reason:'Validato dal responsabile',answers:[{questionId:'q1',answer:'MFA attivo e verificato',evidenceRefs:['obj1']},{questionId:'q2',answer:'Logging centralizzato',evidenceRefs:['ev1']}]},admin);assert.equal(item.state,'approved');assert.equal(item.approvedAnswers.length,2);const p=assuranceProjection({grcAssurance:[item]},admin);assert.equal(p.counts.approved,1);console.log('grc-assurance-check: ok');
