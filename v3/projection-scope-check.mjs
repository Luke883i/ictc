import assert from 'node:assert/strict';
import { canReadProcedureRecord, normalizeScopeRef, projectionBasis, projectionContext, scopeRefMatches } from './runtime/projection-context.mjs';
const actor={id:'u1',role:'user'};const ctx=projectionContext(actor,{asOf:'2026-08-09T00:00:00.000Z',scopeRef:{organization:'Org',unit:'Risk',jurisdictions:['IT','EU']}});assert.equal(ctx.scopeRef.organization,'Org');assert.equal(ctx.scopeRef.unit,'Risk');assert.deepEqual(ctx.scopeRef.jurisdictions,['IT','EU']);assert.equal(projectionBasis(ctx).scopeRef.unit,'Risk');assert.equal(scopeRefMatches({organization:'Org',unit:'Risk'},ctx.scopeRef),true);assert.equal(scopeRefMatches({organization:'Org',unit:'HR'},ctx.scopeRef),false);assert.equal(scopeRefMatches({organization:'Other'},ctx.scopeRef),false);assert.equal(canReadProcedureRecord('objects',{id:'o1',scopeRef:{organization:'Org',unit:'Risk'}},ctx),true);assert.equal(canReadProcedureRecord('objects',{id:'o2',scopeRef:{organization:'Org',unit:'HR'}},ctx),false);assert.equal(canReadProcedureRecord('objects',{id:'legacy'},ctx),true,'legacy unscoped records remain organization-visible');const restricted={id:'o3',scopeRef:{organization:'Org',unit:'Risk',participants:['u2']}};assert.equal(canReadProcedureRecord('objects',restricted,ctx),false);assert.deepEqual(normalizeScopeRef('actor-visible').participants,[]);console.log('projection-scope-check: ok',ctx.scopeRef);
