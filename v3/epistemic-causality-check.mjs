import assert from 'node:assert/strict';
import { buildEpistemicStep,verifyEpistemicStep } from './runtime/epistemic-step.mjs';
const version={id:'sv-map-1',subject:{type:'mapping',id:'map-1'},payloadSha256:'a'.repeat(64)};
const aiEvent={id:'event-ai',revision:1,at:'2026-08-10T10:00:00.000Z',actorId:'maria',role:'admin',action:'grc.mapping.ai.proposed',inputSha256:'b'.repeat(64),metadata:{semanticManifestSha256:'c'.repeat(64)}};
const ai=buildEpistemicStep(aiEvent,[version],[]);assert.deepEqual(ai.execution.initiatorRef,{type:'principal',id:'maria'});assert.deepEqual(ai.effects[0].producerRef,{type:'service',id:'ai-provider'});assert.deepEqual(ai.effects[0].families,['proposed']);assert.equal(ai.effects[0].classificationSource,'safe-action-fallback');assert.equal(verifyEpistemicStep(ai),true);
const declaredEvent={...aiEvent,id:'event-declared',action:'custom.recorded',metadata:{semanticManifestSha256:'d'.repeat(64),epistemicEffects:[{subject:{type:'mapping',id:'map-1'},families:['attested'],producerRef:{type:'principal',id:'auditor-1'},basisRefs:[{type:'evidence',id:'ev-1'}]}]}};const declared=buildEpistemicStep(declaredEvent,[version],[]);assert.deepEqual(declared.execution.initiatorRef,{type:'principal',id:'maria'});assert.deepEqual(declared.effects[0].producerRef,{type:'principal',id:'auditor-1'});assert.deepEqual(declared.effects[0].families,['attested']);assert.equal(declared.effects[0].classificationSource,'declared-effect');assert.equal(declared.effects[0].basisRefs[0].id,'ev-1');
console.log('epistemic-causality-check: ok (initiator/executor/producer separated; safe fallback cannot mint human authority)');
