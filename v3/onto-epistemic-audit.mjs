import { strict as assert } from 'node:assert';
import { readFile } from 'node:fs/promises';
import { canonicalProcedureRegistry } from './runtime/procedure-registry.mjs';
const root=new URL('./',import.meta.url);const read=name=>readFile(new URL(name,root),'utf8');
const [contractText,invariantsText,questions,monitoring,incidents,incidentMarket,incidentContract,contributions,model,store,common,enterprise,kernelText,grcRuntime,grcCompatibility,grcInvariant,grcProjection,epistemicV13]=await Promise.all([read('product-contract.json'),read('experience-invariants.json'),read('question-engine.mjs'),read('runtime/monitoring.mjs'),read('runtime/incidents.mjs'),read('runtime/incident-market-handler.mjs'),read('runtime/incident-contract.mjs'),read('runtime/contributions.mjs'),read('runtime/model.mjs'),read('store.mjs'),read('public/ui/common.js'),read('enterprise.mjs'),read('process-kernel.json'),read('runtime/grc-runtime.mjs'),read('runtime/grc-runtime-legacy.mjs'),read('runtime/procedure-invariant-handler.mjs'),read('runtime/grc-projection.mjs'),read('runtime/epistemic-projection-1-3.mjs')]);
const contract=JSON.parse(contractText),invariants=JSON.parse(invariantsText),kernel=JSON.parse(kernelText),registry=canonicalProcedureRegistry(),checks=[];function check(id,condition,detail){assert.ok(condition,`${id}: ${detail}`);checks.push({id,detail});}const expectedServices=['monitoring','incidents','objects','coverage','actions','risks','assurance'],grcCapabilities=`${grcRuntime}\n${grcCompatibility}`;
check('seven-services-one-contract',JSON.stringify(contract.services.map(item=>item.id))===JSON.stringify(expectedServices),'seven business services are declared by one product contract');check('one-business-procedure-authority',registry.authority==='canonical-procedure-registry'&&JSON.stringify(registry.procedures.map(item=>item.id))===JSON.stringify(expectedServices),'business lifecycle/name/checkpoint authority is the canonical procedure registry');check('relation-routing-compat-authority',kernel.authority==='runtime'&&expectedServices.every(id=>kernel.processes.some(item=>item.id===id)),'process-kernel remains relation/routing compatibility substrate');check('control-plane',contract.controlPlane?.id==='administration','administration is a control plane, not a business service');check('three-roles',contract.roles.map(item=>item.id).join(',')==='admin,user,auditor','admin, user and read-only auditor');check('invariant-depth',invariants.invariants.length>=18,`${invariants.invariants.length} explicit invariants`);check('monitoring-raw-first',monitoring.indexOf('monitoring.mission.intent.recorded')<monitoring.indexOf('planningEnvelope = await completePlan'),'objective is persisted before planning AI');check('contribution-raw-first',contributions.indexOf('contribution.recorded')<contributions.indexOf('enrichOne(recordedId'),'material is persisted before enrichment AI');check('incident-current-owner',incidentMarket.includes("'/api/incidents/intake'")&&incidentMarket.includes("'/api/incidents/:id/close'")&&!incidents.includes("'/api/incidents/intake'")&&!incidents.includes("'/api/incidents/:id/close'"),'incident market handler uniquely owns intake and close; compatibility incident handler owns only continuation lifecycle');check('incident-raw-first',incidentMarket.indexOf('incident.intake.recorded')<incidentMarket.indexOf('analyzeOne(recordedId'),'narrative is persisted before analysis AI under the current intake owner');check('human-adoption',questions.includes('ai-suggestion-confirmed')&&questions.includes('human-corrected-or-entered'),'AI suggestions require a recorded human relation');check('question-purpose',questions.includes('whyNow')&&questions.includes('evidenceUse'),'adaptive questions explain purpose and evidence use');check('draft-gate',incidents.includes("'questions-open'")&&incidents.indexOf("'questions-open'")<incidents.indexOf('const ai = await draftIncident'),'draft is blocked while gaps remain');check('versioned-formulation',incidents.includes('formulationVersions')&&incidents.includes('incident.formulation.saved'),'wording is versioned before submission');check('saved-digest-submit',incidents.includes('formulationSha256')&&incidents.includes('formulation-conflict'),'submission binds the confirmed version digest');check('recoverable-ai',monitoring.includes('needs-plan')&&contributions.includes('/api/contributions/:id/enrich')&&incidents.includes('/api/incidents/:id/analyze'),'AI entry points preserve raw input and retry');check('grc-ai-proposal-only',grcCapabilities.includes('grc.mapping.ai.proposed')&&grcCapabilities.includes('grc.action.ai.prioritized')&&grcCapabilities.includes('grc.risk.ai.proposed')&&grcCapabilities.includes('grc.assurance.ai.proposed'),'GRC AI outputs remain explicit proposal events through current compatibility implementation');check('grc-human-checkpoints',grcCapabilities.includes('grc.object.reviewed')&&grcCapabilities.includes('grc.mapping.decided')&&grcCapabilities.includes('grc.action.adopted')&&grcCapabilities.includes('grc.risk.reviewed')&&grcCapabilities.includes('grc.assurance.approved'),'GRC authority changes remain separate human writes');check('single-action-progress-owner',grcInvariant.includes("'grc.action.progressed'")&&grcRuntime.includes("/api/grc/actions/:id/progress")&&grcRuntime.includes('return false'),'current AP progress semantic owner is procedure-invariant-handler; compatibility route is suppressed');check('human-heatmap',grcProjection.includes('heatmap')&&/rating umani/i.test(grcProjection),'consolidated risk posture remains bounded to human ratings');check('reasoned-decisions',monitoring.includes('reason-required')&&incidentContract.includes('closure-note-required'),'source, pause and current incident close decisions require reasons');check('least-privilege',model.includes('visibleContributions')&&model.includes('visibleIncidents')&&/prompts\s*:\s*null/.test(model),'user projection excludes unrelated private data and prompts');check('enterprise-directory',enterprise.includes('authorizeEnterpriseActor')&&enterprise.includes('identity-not-provisioned'),'trusted identities require directory provisioning');check('ai-budget',enterprise.includes('ai-budget-exhausted')&&enterprise.includes('ai-model-not-allowed'),'budget and allowlist fail closed');check('observation-history',model.includes('mergeCatalogObservation')&&model.includes('observations'),'rediscovery appends observations');check('linked-evidence',store.includes('relatedSha256')&&store.includes('eventsSha256')&&store.includes('formulations'),'bundles link related objects, versions and events');check('subject-version-materialization',store.includes('appendSubjectVersion')&&store.includes('subjectVersions'),'governed writes materialize version occurrences before append persistence cutover');check('epistemic-five-family-grammar',['observed','derived','proposed','decided','attested'].every(token=>epistemicV13.includes(token)),'same-as-read epistemic projection exposes five-family grammar');check('attachment-cleanup',store.includes('deleteAttachments')&&contributions.includes('deleteAttachments')&&incidentMarket.includes('deleteAttachments'),'failed raw writes remove unreferenced files under current intake owner');check('protected-download',common.includes('downloadProtected')&&common.includes("'x-ictc-role'"),'evidence download carries active identity headers');check('anti-overclaim',contract.boundaries.some(item=>item.includes('non determina'))&&contract.boundaries.some(item=>item.includes('non verità')),'legal and evidentiary limits remain explicit');await import('node:fs/promises').then(({mkdir,writeFile})=>Promise.all([mkdir(new URL('../artifacts/',import.meta.url),{recursive:true}),writeFile(new URL('../artifacts/onto-epistemic-audit.json',import.meta.url),JSON.stringify({ok:true,checks},null,2))]));console.log(`onto-epistemic-audit: ok (${checks.length} checks; canonical authority + current incident owner + single AP owner)`);
