import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const read=p=>readFile(new URL(p,import.meta.url),'utf8'),files={store:await read('./store.mjs'),persistence:await read('./sqlite-state-persistence.mjs'),census:await read('./semantic-closure-census.mjs'),epistemic:await read('./runtime/epistemic-step.mjs'),review:await read('./runtime/review-need-ledger.mjs'),inbox:await read('./runtime/review-inbox.mjs'),context:await read('./runtime/projection-context.mjs'),falsification:await read('./semantic-falsification-check.mjs'),contract:await read('./semantic-api-contract.json'),security:await read('./security-static-analysis-posture.mjs')},gates=[['I01',files.store.includes('semanticManifestSha256')&&files.persistence.includes('semantic-manifest-mismatch')&&files.persistence.includes('epistemic-step-digest-mismatch')],['I02',files.census.includes('controlTestWriteObserved')&&files.census.includes('duplicateRoutes.length')],['I03',files.epistemic.includes('initiatorRef:initiator')&&files.epistemic.includes('producerRef')&&files.epistemic.includes('declared-effect')],['I04',files.review.includes("causeMode='all'")&&files.inbox.includes("dependencyReviewAuthority:'persisted-review-need-ledger'")],['I05',files.persistence.includes('SUBJECT_VERSION_WORKING_SET_LIMIT')&&files.context.includes('historical-transaction-selection-unsupported')],['I06',files.falsification.includes("code:'TP-01'")&&files.falsification.includes('faultMutation')],['I07',files.contract.includes('6.3.0')&&files.contract.includes('semanticManifestBoundToAudit')&&files.security.includes('conditional-not-executed')]];for(const[id,ok]of gates)assert.equal(Boolean(ok),true,`${id} semantic integrity/authority gate failed`);console.log('semantic-integrity-authority-dod: ok',{passed:gates.length,total:gates.length,ids:gates.map(([id])=>id)});
