import assert from 'node:assert/strict';
import { readFile } from 'node:fs/promises';
import { VERSION } from './version.mjs';
import { runtimeOntologyProjection } from './runtime/ontology.mjs';
import { processKernelProjection } from './runtime/process-kernel.mjs';
import { canonicalDecisionProjection } from './runtime/decision-projection.mjs';
import { canonicalEpistemicProjection } from './runtime/epistemic-projection.mjs';
import { canonicalHomeNextAction, canonicalProcedureHub } from './runtime/workbench-projection.mjs';
const root = new URL('./', import.meta.url); const contract = JSON.parse(await readFile(new URL('semantic-api-contract.json', root), 'utf8')); const product = JSON.parse(await readFile(new URL('product-contract.json', root), 'utf8')); const packageManifest = JSON.parse(await readFile(new URL('../package.json', root), 'utf8')); const server = await readFile(new URL('server.mjs', root), 'utf8');
assert.equal(contract.schemaVersion, '1.3.0'); assert.equal(contract.surface, 'GET /api/bootstrap'); assert.equal(contract.authority, 'server-runtime'); assert.equal(packageManifest.version, VERSION); assert.match(VERSION, /^\d+\.\d+\.\d+(?:-[0-9A-Za-z.-]+)?$/);
const ontology = runtimeOntologyProjection(); const kernel = processKernelProjection(); assert.equal(ontology.schemaVersion, contract.fields.ontology.schemaVersion); assert.equal(ontology.authority, 'runtime'); for (const field of contract.fields.ontology.required) assert.ok(Object.hasOwn(ontology, field), `ontology missing ${field}`);
assert.deepEqual(Object.fromEntries(['monitoring','incidents','evidence','identity','ai'].map(id => [id, ontology.processes[id].code])), { monitoring: 'RN-01', incidents: 'EC-01', evidence: 'EV-01', identity: 'IA-01', ai: 'GA-01' }); assert.deepEqual(Object.keys(ontology.relations).sort(), Object.keys(kernel.relations).sort()); assert.deepEqual(Object.keys(ontology.archetypes).sort(), Object.keys(kernel.archetypes).sort()); assert.deepEqual(Object.keys(ontology.processes).sort(), kernel.processes.map(item => item.id).sort());
for (const binding of [/projected\.capabilities=\[\.\.\.\(actor\.permissions\|\|\[\]\)\]/,/projected\.accessProfile=accessProfileFor\(actor\)/,/projected\.experience\.release=VERSION/,/projected\.ontology=runtimeOntologyProjection\(\)/,/projected\.homeNextAction=canonicalHomeNextAction\(/,/projected\.procedures=canonicalProcedureHub\(/,/projected\.decisions=canonicalDecisionProjection\(/,/projected\.epistemic=canonicalEpistemicProjection\(/]) assert.match(server, binding, `bootstrap derivation binding missing: ${binding}`);
const emptyState = { missions: [], catalog: [], contributions: [], incidents: [], users: [] }; const readiness = { controls: [], verified: 0 }; const roles = Object.fromEntries(product.roles.map(role => [role.id, role])); const requiredNext = contract.fields.homeNextAction.required; const requiredProcedure = contract.fields.procedures.required;
for (const roleId of ['admin','user','auditor']) {
  const role = roles[roleId]; assert.ok(role); const actor = { id: `contract-${roleId}`, role: roleId, permissions: role.permissions }; const next = canonicalHomeNextAction(emptyState, actor, { llmReady: false }); assert.equal(next.schemaVersion, contract.fields.homeNextAction.schemaVersion); for (const field of requiredNext) assert.ok(Object.hasOwn(next, field)); assert.equal(next.readOnly, roleId === 'auditor');
  const procedures = canonicalProcedureHub(emptyState, actor, { readiness }); assert.ok(procedures.length >= 3); for (const item of procedures) { assert.equal(item.schemaVersion, contract.fields.procedures.itemSchemaVersion); for (const field of requiredProcedure) assert.ok(Object.hasOwn(item, field)); const process = ontology.processes[item.id]; assert.ok(process); assert.equal(item.label, process.label); assert.equal(item.code, process.code); assert.equal(item.archetype, process.archetype); } assert.equal(procedures.some(item => item.id === 'administration'), roleId === 'admin');
  const decisions = canonicalDecisionProjection(emptyState, actor); assert.equal(decisions.schemaVersion, contract.fields.decisions.schemaVersion); assert.equal(decisions.authority, 'human-decision-projection'); assert.ok(Array.isArray(decisions.records));
  const epistemic = canonicalEpistemicProjection(emptyState, actor); assert.equal(epistemic.schemaVersion, contract.fields.epistemic.schemaVersion); assert.equal(epistemic.authority, 'runtime-epistemic-projection'); assert.ok(Array.isArray(epistemic.records)); assert.deepEqual(Object.keys(epistemic.grammar).sort(), contract.fields.epistemic.facetGrammar.sort());
}
assert.ok(contract.limitations.length >= 2); assert.match(contract.ratchetRule, /extend/i); assert.match(contract.ratchetRule, /relation/i); console.log(`semantic-api-contract-check: ok (${contract.id}, schema ${contract.schemaVersion}, release ${VERSION})`);
