import assert from 'node:assert/strict';
import { readFile } from 'node:fs/promises';
import { assertActiveInstallers } from './test-helpers/active-experience-install.mjs';
const read=p=>readFile(new URL(p,import.meta.url),'utf8');
const [index,styles,frame,router,tools,shell,active,css,market,copy,refined]=await Promise.all([
  read('./public/index.html'),read('./public/styles.css'),read('./public/ui/procedure-frame.js'),read('./public/ui/surface-router.js'),read('./public/ui/global-tools.js'),read('./public/ui/stable-shell.js'),read('./public/ui/active-experience.js'),read('./public/experience-1-9.css'),read('./public/ui/procedure-market-ux.js'),read('./public/ui/product-copy.js'),read('./public/ui/refined-product.js')
]);
assert.match(index,/data-ictc-edition="1\.2-market-candidate"/,'semantic edition remains 1.2');
assert.match(shell,/ictcExperienceEdition=EXPERIENCE_EDITION/);assert.match(shell,/EXPERIENCE_EDITION='1\.9-experience-candidate'/);assert.doesNotMatch(shell,/dataset\.ictcEdition\s*=/,'experience layer must not rewrite semantic edition');
assert.match(copy,/home:'Home'/);assert.match(copy,/processes:'Processi di Compliance'/);assert.match(copy,/proof:'Evidenze ICTC'/);assert.doesNotMatch(copy,/home:'Oggi'|proof:'Postura ICTC'/);
assert.match(shell,/SURFACE_LABELS\.home/);assert.match(shell,/SURFACE_LABELS\.proof/);assert.match(tools,/SURFACE_LABELS\.proof/);
assertActiveInstallers(active,['installProcedureFrame','installRefinedProduct'],{label:'V1.9 active experience'});
for(const token of ['semanticSignals','metric.label','procedure-purpose','data-procedure-primary','data-process-code','Consulta registrazioni','data-nav-back','enabledProcedures','grcProcedureIds','meta.ux?.primaryAction'])assert.ok(frame.includes(token),token);
for(const retired of ['<b>Scopo del processo</b>','<span>Processo di Compliance</span>','<small>da vedere</small>','<small>registrazioni</small>','Nessuna attenzione aperta'])assert.equal(frame.includes(retired),false,`retired repeated process framing returned: ${retired}`);
assert.match(frame,/state\.data\?\.procedureRegistry\?\.procedures/);assert.match(frame,/state\.role==='auditor'/);assert.ok(frame.includes('openReadSurface(id)'),'auditor primary must remain process-bound in the current experience');assert.doesNotMatch(frame,/navigateSurface\('proof'\)/,'process primary must not redirect auditor out of the current process');assert.doesNotMatch(frame,/FALLBACK_ACTIONS|WORKSPACE_IDS/,'process membership and primary labels must come from canonical registry');assert.doesNotMatch(frame,/className='grc-head procedure-frame'/,'canonical process frame must not expose the legacy GRC header hook');assert.doesNotMatch(frame,/button\.remove\(\)/,'presentation layer must not delete unknown controls to hide accessibility bugs');
for(const token of ['pushState','replaceState','popstate','view','procedureId','getBackLabel','navigateBack'])assert.ok(router.includes(token),token);for(const token of ['metaKey','RECENT_KEY','ArrowDown','ArrowUp','Escape','enabledProcedures','allowedRecent'])assert.ok(tools.includes(token),token);assert.ok(tools.indexOf("closeCommand({restoreFocus:false});navigateSurface('monitoring'")>=0,'source dialog focus handoff');
assert.match(styles,/@import url\('\.\/experience-1-9\.css'\);/);assert.match(styles,/@import url\('\.\/refined-product\.css'\);/);assert.match(css,/data-ictc-experience-edition="1\.9-experience-candidate"/);assert.match(css,/@media\(prefers-reduced-motion:reduce\)/);for(const bp of ['max-width:980px','max-width:760px','max-width:520px'])assert.ok(css.includes(bp),bp);assert.match(css,/\.command-dialog/);assert.match(css,/\.procedure-frame/);assert.match(css,/\.procedure-card/);assert.doesNotMatch(css,/button:empty[^\{]*\{[^}]*display\s*:\s*none/i,'unnamed controls must be audited, not hidden by presentation CSS');
for(const token of ['refined-incident-intake','refined-question','neutralizeDynamicMarketCss','PRODUCT_COPY.proofAction'])assert.ok(refined.includes(token),token);for(const forbidden of ['MutationObserver','prompt(','confirm(','complianceScore','maturityScore'])assert.ok(!`${frame}\n${router}\n${tools}\n${shell}\n${refined}`.includes(forbidden),forbidden);assert.match(market,/function renderStandardWorkspace/);
console.log('v1-9-experience-check: ok (semantic 1.2 + Experience 1.9 + canonical navigation authority + compact process semantics + process-bound auditor read)');
