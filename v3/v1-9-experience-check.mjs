import assert from 'node:assert/strict';
import { readFile } from 'node:fs/promises';
import { assertActiveInstallers } from './test-helpers/active-experience-install.mjs';
const read=p=>readFile(new URL(p,import.meta.url),'utf8');
const [index,styles,frame,router,tools,shell,active,css,market,copy,refined,composition,native]=await Promise.all([read('./public/index.html'),read('./public/styles.css'),read('./public/ui/procedure-frame.js'),read('./public/ui/surface-router.js'),read('./public/ui/global-tools.js'),read('./public/ui/stable-shell.js'),read('./public/ui/active-experience.js'),read('./public/experience-1-9.css'),read('./public/ui/procedure-market-ux.js'),read('./public/ui/product-copy.js'),read('./public/ui/refined-product.js'),read('./public/ui/semantic-composition-runtime.js'),read('./public/ui/native-semantic-lattice-3-2.js')]);
assert.match(index,/data-ictc-edition="1\.2-market-candidate"/,'semantic edition remains 1.2');assert.match(shell,/ictcExperienceEdition=EXPERIENCE_EDITION/);assert.match(shell,/EXPERIENCE_EDITION='1\.9-experience-candidate'/);assert.doesNotMatch(shell,/dataset\.ictcEdition\s*=/,'experience layer must not rewrite semantic edition');assert.match(copy,/home:'Home'/);assert.match(copy,/processes:'Processi di Compliance'/);assert.match(copy,/proof:'Evidenze ICTC'/);assert.match(shell,/SURFACE_LABELS\.home/);assert.match(shell,/SURFACE_LABELS\.proof/);assert.match(tools,/SURFACE_LABELS\.proof/);
assertActiveInstallers(active,['installProcedureFrame','installRefinedProduct','installNativeWorkspace32','installSemanticComposition'],{label:'V1.9 + Native Semantic Lattice 3.2 active experience'});
for(const token of ['PROCEDURE_WORKSPACE','procedure-purpose','data-procedure-primary','data-process-code','Consulta registrazioni','data-nav-back','enabledProcedures','grcProcedureIds'])assert.ok(frame.includes(token),token);assert.equal(frame.includes('semanticSignals'),false,'procedure frame must not prepend numeric signals');for(const retired of ['<b>Scopo del processo</b>','<span>Processo di Compliance</span>','<small>da vedere</small>','<small>registrazioni</small>','Nessuna attenzione aperta'])assert.equal(frame.includes(retired),false,`retired repeated process framing returned: ${retired}`);
assert.match(frame,/state\.data\?\.procedureRegistry\?\.procedures/);assert.match(frame,/state\.role==='auditor'/);assert.ok(frame.includes('openReadSurface(id)'),'auditor primary must remain process-bound');assert.doesNotMatch(frame,/navigateSurface\('proof'\)/,'process primary must not redirect auditor out of current process');for(const token of ['pushState','replaceState','popstate','view','procedureId','getBackLabel','navigateBack'])assert.ok(router.includes(token),token);
assert.match(styles,/@import url\('\.\/experience-1-9\.css'\);/);assert.match(styles,/@import url\('\.\/semantic-composition-3-1\.css'\);/);assert.match(css,/data-ictc-experience-edition="1\.9-experience-candidate"/);assert.match(css,/@media\(prefers-reduced-motion:reduce\)/);for(const token of ['refined-incident-intake','refined-question','neutralizeDynamicMarketCss','PRODUCT_COPY.proofAction'])assert.ok(refined.includes(token),token);assert.ok(shell.includes("textContent='Attività di compliance'")&&shell.includes('pulse.hidden=true')&&shell.includes('PRODUCT_PROPOSITION'),'Home must be work-first without numeric dashboard and own its proposition');
for(const retired of ['compactProof','compactEpistemic','compactAdmin'])assert.equal(composition.includes(retired),false,`global composition runtime must retire local adapter ${retired}`);assert.ok(active.includes('installNativeWorkspace32')&&!composition.includes('installNativeWorkspace32')&&composition.includes('annotateSurface'),'transversal surfaces must bootstrap native owners at the composition root and keep the global kernel annotation-only');assert.ok(native.includes('LOCAL_COMPOSITION_OWNERS')&&native.includes('PROCEDURE_WORKSPACE'),'native lattice contract missing');
for(const forbidden of ['MutationObserver','prompt(','confirm(','complianceScore','maturityScore'])assert.ok(!`${frame}\n${router}\n${tools}\n${shell}\n${refined}\n${composition}`.includes(forbidden),forbidden);assert.match(market,/function renderStandardWorkspace/);console.log('v1-9-experience-check: ok (canonical navigation + native 3.2 root bootstrap + process-bound auditor read)');
