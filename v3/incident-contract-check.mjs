import assert from 'node:assert/strict';
import { normalizeIncidentIntake, normalizeIncidentClosure, incidentNeedsDownstreamAck } from './runtime/incident-contract.mjs';
assert.equal(normalizeIncidentIntake({eventKind:'near-miss'}).eventKind,'near-miss');assert.equal(normalizeIncidentIntake({}).operationalSeverity,'not-assessed');assert.throws(()=>normalizeIncidentIntake({eventKind:'breach'}),e=>e.code==='incident-kind-invalid');assert.throws(()=>normalizeIncidentClosure({note:'x'}),e=>e.code==='incident-closure-disposition-required');const closure=normalizeIncidentClosure({disposition:'follow-up-required',note:'Azione ancora aperta',openDownstreamAcknowledged:true});assert.equal(closure.openDownstreamAcknowledged,true);const incident={links:[{relation:'incident-generates-action',targetType:'action',targetId:'a1'}]};assert.equal(incidentNeedsDownstreamAck(incident,{grcActions:[{id:'a1',state:'open'}]}),true);assert.equal(incidentNeedsDownstreamAck(incident,{grcActions:[{id:'a1',state:'closed'}]}),false);console.log('incident-contract-check: ok');
