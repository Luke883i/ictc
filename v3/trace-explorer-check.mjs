import assert from 'node:assert/strict';
import { readFile } from 'node:fs/promises';
import { procedureTraceFor } from './runtime/procedure-trace.mjs';
const actor={id:'admin',role:'admin'},state={catalog:[{id:'s1',title:'Fonte',state:'verified',impactAssessments:[{outcome:'relevant',reason:'impatto',affectedObjectIds:['o1'],by:'admin',at:'2026-08-08',sourceObservationSha256:'a'.repeat(64)}]}],incidents:[],grcObjects:[{id:'o1',type:'server',name:'ERP',status:'active',criticality:'high',owner:'IT',sourceAuthority:'CMDB',relations:[],reviews:[],createdAt:'x',createdBy:'admin',updatedAt:'x',versionSha256:'b'.repeat(64)}],grcMappings:[],grcActions:[{id:'a1',title:'Fix',originType:'source',originId:'s1',originRelation:'source-generates-action',state:'closed',decisions:[],updates:[],verifications:[{decision:'closed',reason:'ok',evidenceRefs:['E1'],by:'admin',at:'2026-08-08',completionSha256:'c'.repeat(64)}],createdAt:'x',createdBy:'admin',updatedAt:'x'}],grcRisks:[],grcAssurance:[],audit:[]};const source=procedureTraceFor(state,actor,'source','s1');assert.ok(source.relations.some(x=>x.relation==='source-affects-object'));const action=procedureTraceFor(state,actor,'action','a1');assert.equal(action.original.reference,'source:s1');assert.ok(action.relations.some(x=>x.relation==='source-generates-action'));assert.ok(action.decisions.some(x=>x.kind==='action-closure-verification'));const ui=await readFile(new URL('./public/ui/trace-explorer.js',import.meta.url),'utf8');for(const text of['Evidence & Trace Explorer','Originale e provenienza','Assistenza AI','Decisioni umane','Postura epistemica','Audit e receipt chain','Limiti','Scarica dossier'])assert.ok(ui.includes(text),text);assert.ok(!ui.includes('Sei conforme'));console.log('trace-explorer-check: ok');
