import { createHash } from 'node:crypto';
import { mkdir, readFile, unlink, writeFile } from 'node:fs/promises';
import path from 'node:path';
import { DEFAULT_PROMPTS, MAX_ATTACHMENT_BYTES, MAX_ATTACHMENT_TOTAL_BYTES, MAX_ATTACHMENTS, asString, canonicalJson, id, now, sha256 } from './domain.mjs';
import { appendStateBindingEvent, canonicalStateSha256, verifyReceiptAgainstState, verifyStateIntegrity } from './integrity-binding.mjs';
import { SqliteStatePersistence } from './sqlite-state-persistence.mjs';
import { appendSubjectVersion } from './runtime/subject-version.mjs';
import { buildEpistemicStep, semanticManifestSha256 } from './runtime/epistemic-step.mjs';
import { initialAttachmentTrust, normalizeAttachmentTrust } from './runtime/attachment-trust.mjs';
function normalizeAttachments(items=[]){return(items||[]).map(item=>({...item,trust:normalizeAttachmentTrust(item.trust)}));}
function initialState(){return{schemaVersion:'2.4.0',revision:0,settings:{organization:{name:'Organizzazione',scope:'Sicurezza delle informazioni in Italia e Unione europea',jurisdictions:['Italia','Unione europea'],sectors:[]},llm:{endpoint:'',model:'',apiKeyEnv:'ICTC_LLM_API_KEY',temperature:0.1},aiPolicy:'enabled',prompts:structuredClone(DEFAULT_PROMPTS),updatedAt:null,updatedBy:null},missions:[],runs:[],contributions:[],catalog:[],incidents:[],subjectVersions:[],reviewNeeds:[],audit:[],commandResults:{}};}
function mergeState(parsed){const base=initialState();return{...base,...parsed,schemaVersion:base.schemaVersion,settings:{...base.settings,...(parsed.settings||{}),organization:{...base.settings.organization,...(parsed.settings?.organization||{})},llm:{...base.settings.llm,...(parsed.settings?.llm||{})},prompts:{...base.settings.prompts,...(parsed.settings?.prompts||{})}},missions:(parsed.missions||[]).map(item=>({planHistory:[],planVersion:item.plan?1:0,...item})),contributions:(parsed.contributions||[]).map(item=>({enrichmentAttempts:0,...item,attachments:normalizeAttachments(item.attachments)})),catalog:(parsed.catalog||[]).map(item=>({observations:[],decisions:[],...item})),incidents:(parsed.incidents||[]).map(item=>({formulationVersions:[],formulationDirty:!item.finalNarrative,...item,attachments:normalizeAttachments(item.attachments)})),subjectVersions:parsed.subjectVersions||[],reviewNeeds:parsed.reviewNeeds||[],audit:parsed.audit||[],commandResults:parsed.commandResults||{}};}
function eventHash(event){const{hash,...unsigned}=event;return sha256(canonicalJson(unsigned));}
function contributionOriginIds(subject){const origins=[...(subject.observations||[]).map(item=>item.origin),subject.origin].filter(Boolean);return new Set(origins.map(item=>item.contributionId).filter(Boolean));}
function redactCatalogContributionOrigins(subject,accessibleIds){const redactOrigin=origin=>{if(!origin?.contributionId||accessibleIds.has(origin.contributionId))return origin;const redacted=structuredClone(origin);redacted.contributionId='[restricted]';return redacted;};subject.origin=redactOrigin(subject.origin);subject.observations=(subject.observations||[]).map(item=>({...item,origin:redactOrigin(item.origin)}));return subject;}
export class Store{
  constructor(root){this.root=root;this.attachmentsPath=path.join(root,'attachments');this.persistence=new SqliteStatePersistence(root);this.queue=Promise.resolve();this.state=initialState();this.initialized=false;}
  async init(){await mkdir(this.attachmentsPath,{recursive:true,mode:0o700});await this.persistence.init();let stateExisted=this.persistence.hasSnapshot();if(stateExisted)this.state=mergeState(this.persistence.load());else{const legacy=await this.persistence.importLegacyIfPresent();if(legacy.state){stateExisted=true;this.state=mergeState(legacy.state);}else{this.state=initialState();this.state=mergeState(this.persistence.save(this.state));}}let integrity=this.verifyChain();if(!integrity.chainOk||(!integrity.ok&&integrity.reason!=='state-head-mismatch'))throw new Error(`Audit chain non valida alla revisione ${integrity.atRevision??this.state.revision}`);if(integrity.reason==='state-head-mismatch')throw Object.assign(new Error('Lo stato canonico non coincide con il digest registrato nell’HEAD audit'),{status:500,code:'state-head-mismatch',details:integrity});if((this.state.audit.length&&!integrity.stateBound&&integrity.bindingMode==='legacy-unbound')||(stateExisted&&!this.state.audit.length&&this.state.revision===0)){const reason=this.state.audit.length?'legacy-head-migration':'legacy-genesis-migration';const binding=appendStateBindingEvent(this.state,{reason});this.state=await this.persist(binding.draft);integrity=this.verifyChain();if(!integrity.ok||!integrity.stateBound)throw new Error('Migrazione binding stato/audit non verificata');}this.initialized=true;return this;}
  snapshot(){return structuredClone(this.state);}
  canonicalStateSha256(){return canonicalStateSha256(this.state);}
  verifyChain(){return verifyStateIntegrity(this.state);}
  verifyReceipt(receipt){return verifyReceiptAgainstState(this.state,receipt);}
  resolvePersistedSubjectVersion(value){return this.persistence.findSubjectVersion(value);}
  close(){this.persistence.close();}
  async mutate(actor,action,subject,input,change,command={}){let envelope;const operation=this.queue.catch(()=>{}).then(async()=>{const commandId=asString(command.id,200);if(commandId&&this.state.commandResults[commandId]){const replay=this.state.commandResults[commandId];if(replay.actorId!==actor.id||replay.action!==action)throw Object.assign(new Error('Identificativo comando già usato per un’altra operazione'),{status:409,code:'command-id-conflict'});envelope=structuredClone({...replay.envelope,replayed:true});return;}if(command.expectedRevision!=null&&Number(command.expectedRevision)!==this.state.revision)throw Object.assign(new Error('I dati sono cambiati. Ricarica e verifica il nuovo stato.'),{status:409,code:'revision-conflict',details:{expectedRevision:Number(command.expectedRevision),actualRevision:this.state.revision}});const draft=structuredClone(this.state);const result=await change(draft);draft.revision+=1;const candidate=mergeState(draft);const eventAt=now(),versionStart=(candidate.subjectVersions||[]).length;appendSubjectVersion(candidate,{subject:subject?{type:asString(subject.type,80),id:asString(subject.id,200)}:null,payload:result,actor,action,at:eventAt,predecessorResolver:ref=>this.persistence.latestSubjectVersion(ref)});const semanticVersions=(candidate.subjectVersions||[]).slice(versionStart),semanticReviews=(candidate.reviewNeeds||[]).filter(item=>Number(item.openedRevision)===Number(candidate.revision)),previousHash=candidate.audit.at(-1)?.hash||'GENESIS',metadata={...structuredClone(command.metadata||{}),semanticManifestSchemaVersion:'1.0.0',semanticManifestSha256:semanticManifestSha256(semanticVersions,semanticReviews)};const event={id:id('event'),revision:candidate.revision,at:eventAt,actorId:actor.id,role:actor.role,action,subject:subject?{type:asString(subject.type,80),id:asString(subject.id,200)}:null,inputSha256:sha256(input??null),resultSha256:sha256(result??null),stateSha256:canonicalStateSha256(candidate),previousHash,metadata};const boundStep=buildEpistemicStep(event,semanticVersions,semanticReviews);event.metadata.epistemicStepSchemaVersion=boundStep.schemaVersion;event.metadata.epistemicStepSha256=boundStep.stepSha256;event.hash=eventHash(event);candidate.audit.push(event);const receipt={eventId:event.id,revision:event.revision,at:event.at,action:event.action,subject:event.subject,actorId:event.actorId,previousHash:event.previousHash,hash:event.hash,inputSha256:event.inputSha256,resultSha256:event.resultSha256,stateSha256:event.stateSha256};envelope={result:structuredClone(result),receipt,replayed:false};if(commandId){candidate.commandResults[commandId]={actorId:actor.id,action,envelope:structuredClone(envelope),storedAt:now()};const keys=Object.keys(candidate.commandResults);if(keys.length>500)for(const key of keys.slice(0,keys.length-500))delete candidate.commandResults[key];}const persisted=await this.persist(candidate);const persistedIntegrity=verifyStateIntegrity(persisted);if(!persistedIntegrity.ok||!persistedIntegrity.stateBound)throw Object.assign(new Error('La mutazione persistita non è legata all’HEAD audit'),{status:500,code:'persist-integrity-mismatch',details:persistedIntegrity});this.state=persisted;});this.queue=operation.catch(()=>{});await operation;return envelope;}
  async persist(state=this.state){const directStatePersist=this.initialized&&state===this.state;let candidate=state;if(directStatePersist){const integrity=verifyStateIntegrity(state);if(!integrity.chainOk)throw Object.assign(new Error('Persist diretto rifiutato: audit chain non valida'),{status:500,code:'integrity-chain-invalid',details:integrity});if(!integrity.stateBound)candidate=appendStateBindingEvent(state,{reason:'direct-state-persist'}).draft;}const persisted=mergeState(this.persistence.save(candidate));if(directStatePersist){const integrity=verifyStateIntegrity(persisted);if(!integrity.ok||!integrity.stateBound)throw Object.assign(new Error('Persist diretto non legato all’HEAD audit'),{status:500,code:'persist-integrity-mismatch',details:integrity});this.state=persisted;}return persisted;}
  async saveAttachments(files=[]){const inputs=Array.isArray(files)?files:[];if(inputs.length>MAX_ATTACHMENTS)throw Object.assign(new Error(`Troppi allegati: massimo ${MAX_ATTACHMENTS}`),{status:413,code:'attachment-count'});const candidates=[];let totalBytes=0;for(const file of inputs){const name=asString(file.name,240)||'allegato';const mime=asString(file.mime,160)||'application/octet-stream';const encoded=asString(file.dataBase64,8_000_000).replace(/^data:[^;]+;base64,/,'');const buffer=Buffer.from(encoded,'base64');if(!buffer.length)continue;if(buffer.length>MAX_ATTACHMENT_BYTES)throw Object.assign(new Error(`Allegato troppo grande: ${name}`),{status:413,code:'attachment-too-large'});totalBytes+=buffer.length;if(totalBytes>MAX_ATTACHMENT_TOTAL_BYTES)throw Object.assign(new Error(`Allegati troppo grandi nel complesso: massimo ${MAX_ATTACHMENT_TOTAL_BYTES} byte`),{status:413,code:'attachments-total-too-large'});const attachmentId=id('file');const sha=createHash('sha256').update(buffer).digest('hex');candidates.push({metadata:{id:attachmentId,name,mime,bytes:buffer.length,sha256:sha,storedAt:now(),trust:initialAttachmentTrust(process.env)},buffer});}const saved=[];try{for(const candidate of candidates){await writeFile(path.join(this.attachmentsPath,candidate.metadata.id),candidate.buffer,{mode:0o600,flag:'wx'});saved.push(candidate.metadata);}return saved;}catch(error){await this.deleteAttachments(saved).catch(()=>{});throw error;}}
  async deleteAttachments(files=[]){for(const file of files){try{await unlink(path.join(this.attachmentsPath,asString(file.id,200)));}catch(error){if(error.code!=='ENOENT')throw error;}}}
  async attachment(idValue){const attachmentId=asString(idValue,200);const all=[...this.state.contributions.flatMap(item=>item.attachments||[]),...this.state.incidents.flatMap(item=>item.attachments||[])];const metadata=all.find(item=>item.id===attachmentId);if(!metadata)return null;return{metadata:{...metadata,trust:normalizeAttachmentTrust(metadata.trust)},buffer:await readFile(path.join(this.attachmentsPath,attachmentId))};}
  evidenceBundle(type,subjectId,actor){const state=this.snapshot();let subject;if(type==='mission')subject=state.missions.find(item=>item.id===subjectId);else if(type==='catalog')subject=state.catalog.find(item=>item.id===subjectId);else if(type==='incident')subject=state.incidents.find(item=>item.id===subjectId);else if(type==='contribution')subject=state.contributions.find(item=>item.id===subjectId);if(!subject)return null;if(type==='incident'&&actor.role!=='admin'&&actor.role!=='auditor'&&subject.createdBy!==actor.id)return null;if(type==='contribution'&&actor.role!=='admin'&&subject.createdBy!==actor.id)return null;const relatedIds=new Set([subjectId]);let related=null;if(type==='mission'){const runs=state.runs.filter(item=>item.missionId===subjectId);const catalog=state.catalog.filter(item=>(item.observations||[]).some(obs=>obs.origin?.missionId===subjectId)||item.origin?.missionId===subjectId);for(const item of runs)relatedIds.add(item.id);for(const item of catalog)relatedIds.add(item.id);related={runs,catalog};}else if(type==='catalog'){const origins=(subject.observations||[]).map(item=>item.origin).filter(Boolean);if(subject.origin)origins.push(subject.origin);const missionIds=new Set(origins.map(item=>item.missionId).filter(Boolean));const runIds=new Set(origins.map(item=>item.runId).filter(Boolean));const allContributionIds=contributionOriginIds(subject);const accessibleContributions=state.contributions.filter(item=>allContributionIds.has(item.id)&&(actor.role==='admin'||item.createdBy===actor.id));const accessibleContributionIds=new Set(accessibleContributions.map(item=>item.id));for(const value of[...missionIds,...runIds,...accessibleContributionIds])relatedIds.add(value);if(actor.role!=='admin')subject=redactCatalogContributionOrigins(subject,accessibleContributionIds);related={missions:state.missions.filter(item=>missionIds.has(item.id)),runs:state.runs.filter(item=>runIds.has(item.id)),contributions:accessibleContributions,restrictedContributionCount:allContributionIds.size-accessibleContributionIds.size};}else if(type==='contribution'){const catalog=state.catalog.filter(item=>(item.observations||[]).some(obs=>obs.origin?.contributionId===subjectId)||item.origin?.contributionId===subjectId);for(const item of catalog)relatedIds.add(item.id);related={catalog};}else if(type==='incident')related={attachments:subject.attachments||[],formulations:subject.formulationVersions||[]};const events=state.audit.filter(event=>event.subject&&relatedIds.has(event.subject.id));const integrity=this.verifyChain();const manifest={subjectSha256:sha256(subject),relatedSha256:sha256(related),eventsSha256:sha256(events),integrityHead:integrity.head,canonicalStateSha256:integrity.canonicalStateSha256,auditHeadStateSha256:integrity.headStateSha256,stateBoundToAuditHead:integrity.stateBound};return{schemaVersion:'1.4.0',generatedAt:now(),generatedBy:actor.id,type,subject,related,events,manifest,integrity,limitations:['Il fascicolo dimostra le operazioni registrate dal runtime, non la verità sostanziale del contenuto.','I risultati AI restano proposte o estrazioni e richiedono controllo umano.','Le nuove mutazioni di soggetti business registrano snapshot content-addressed; i record legacy precedenti alla 1.2 Market Candidate possono non avere una versione payload ricostruibile.','Il binding esclude audit e commandResults per evitare dipendenze circolari; gli allegati sono rappresentati dai metadati, digest e stato di trust registrati nello stato.','Un allegato registrato o non scansionato non è rappresentato come malware-clean o come evidenza sostanzialmente sufficiente.','La catena hash non equivale a firma qualificata, marcatura temporale certificata o non-ripudio.','La completezza del fascicolo dipende dalle informazioni e dalle fonti effettivamente acquisite.','I riferimenti a contributi non accessibili al ruolo corrente sono redatti dal fascicolo.']};}
}
