import{strict as assert}from'node:assert';import{readFile,mkdir,writeFile}from'node:fs/promises';import path from'node:path';const root=path.resolve(new URL('..',import.meta.url).pathname),html=await readFile(path.join(root,'v3/public/index.html'),'utf8'),css=await readFile(path.join(root,'v3/public/styles.css'),'utf8'),js=await readFile(path.join(root,'v3/public/js/journey-shell.js'),'utf8'),checks=[];const check=(n,v)=>{assert.ok(v,n);checks.push({name:n,status:'passed'})};check('lang',/<html lang="it">/.test(html));check('viewport',/name="viewport"/.test(html));check('skip-link',/href="#main"/.test(html));check('main-focus',/<main id="main" tabindex="-1">/.test(html));check('named-dialogs',[...html.matchAll(/<dialog[^>]+>/g)].every(m=>/aria-labelledby=/.test(m[0])));check('button-types',[...html.matchAll(/<button\b([^>]*)>/g)].every(m=>/type=/.test(m[1])));check('labels',[...html.matchAll(/<(input|textarea|select)\b([^>]*)>/g)].every(m=>/type="hidden"/.test(m[2])||(/id="([^"]+)"/.test(m[2])&&html.includes(`for="${m[2].match(/id="([^"]+)"/)[1]}"`))));check('visible-focus',/:focus-visible/.test(css));check('target-44',/--target:44px/.test(css)&&/min-height:var\(--target\)/.test(css));check('reduced-motion',/prefers-reduced-motion/.test(css));check('forced-colors',/forced-colors/.test(css));check('reflow',/max-width:620px/.test(css)&&/max-width:900px/.test(css));check('keyboard-tabs',/ArrowLeft/.test(js)&&/ArrowRight/.test(js)&&/Home/.test(js)&&/End/.test(js));check('search-shortcut',/ctrlKey/.test(js)&&/metaKey/.test(js));check('focus-return',/dialogReturnFocus/.test(js));check('live-region',/aria-live="polite"/.test(html));check('progressive-details',/<details/.test(html)||/details/.test(js));const dir=path.join(root,'artifacts');await mkdir(dir,{recursive:true});await writeFile(path.join(dir,'journey-accessibility-audit.json'),JSON.stringify({schemaVersion:'1.0.0',generatedAt:new Date().toISOString(),result:'passed',checks,claim:'WCAG 2.2 AA-oriented engineering checks; not certification',limitations:['Manual screen reader, zoom, high contrast and user testing remain required.']},null,2));console.log(`journey-accessibility-audit: ok (${checks.length} checks)`);
