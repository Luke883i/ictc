import http from 'node:http';
const port = Number(process.env.MOCK_AI_PORT || 4899);
function responseFor(system, user) {
  const purpose = (system.match(/ICTC_PURPOSE_RUNTIME:([a-z-]+)/) || [])[1] || (system.match(/ICTC_PURPOSE:([a-z-]+)/) || [])[1];
  let input = {}; try { input = JSON.parse(user); } catch {}
  if (purpose === 'v3-dashboard-insight') { const metrics=input.basis?.metrics||[]; const attention=metrics.filter(item=>Number(item.value)>0).slice(0,3); return {summary:'Lettura AI della base KPI deterministica: verificare prima le aree con lavoro o review aperta.',observations:attention.map((item,index)=>({id:`focus-${index+1}`,title:`Verifica ${item.label}`,rationale:`Il KPI ${item.id} ha valore ${item.value} nella base deterministica fornita; la priorità operativa resta quella del runtime.`,processId:item.processId,sourceMetricIds:[item.id]}))}; }
  if (purpose === 'grc-mapping-proposal') return {targetIds: input.candidates?.[0]?.id ? [input.candidates[0].id] : [], rationale:'Corrispondenza candidata basata sul contesto fornito; richiede validazione umana.', confidence:.84};
  if (purpose === 'grc-action-priority') return {priority:4, rationale:'Priorità proposta in base a gap, criticità e stato disponibili; richiede adozione umana.', confidence:.81};
  if (purpose === 'grc-risk-proposal') return {likelihood:5, impact:4, rationale:'Scenario proposto sulla base del contesto fornito; rating da validare dal risk owner.', confidence:.79};
  if (purpose === 'grc-assurance-draft') { const evidence=input.availableEvidence?.objects?.[0]?.id ? [input.availableEvidence.objects[0].id] : []; return {summary:'Bozza strutturata in due risposte da verificare.',questions:[{id:'mfa',question:'Come sono gestiti gli accessi privilegiati?',draftAnswer:'Gli accessi privilegiati sono governati tramite controlli dichiarati e richiedono verifica dell’evidenza corrente.',evidenceRefs:evidence,limitations:['Bozza AI: validare configurazione ed efficacia.']},{id:'logging',question:'Come viene gestito il logging?',draftAnswer:'Il logging è descritto nel perimetro dichiarato e deve essere confermato con evidenze aggiornate.',evidenceRefs:evidence,limitations:['Bozza AI: non attesta copertura completa.']}]}; }
  if (purpose === 'monitoring-plan') return {queries:[`${input.objective || 'sicurezza informazioni'} site:gov.it`,`${input.objective || 'sicurezza informazioni'} site:europa.eu`],preferredSources:['EUR-Lex','Agenzia per la cybersicurezza nazionale','Garante per la protezione dei dati personali'],inclusionCriteria:['fonte ufficiale','pertinenza al perimetro dichiarato','identificativo o URL verificabile'],exclusionCriteria:['commento non ufficiale senza fonte primaria','duplicato della stessa versione'],rationale:`Piano mirato al perimetro: ${input.objective || 'non indicato'}`};
  if (purpose === 'compliance-discovery') return {items:[{title:'Direttiva (UE) 2022/2555 — NIS2',documentType:'directive',authority:'Unione europea',jurisdiction:'Unione europea',identifier:'CELEX:32022L2555',sourceUrl:'https://eur-lex.europa.eu/eli/dir/2022/2555/oj',publicationDate:'2022-12-27',effectiveDate:'2023-01-16',summary:'Misure per un livello comune elevato di cibersicurezza.',relevance:'Fonte ufficiale coerente con il perimetro di sicurezza delle informazioni.',confidence:.96},{title:'Linee guida sulla gestione degli incidenti',documentType:'guideline',authority:'Autorità nazionale',jurisdiction:'Italia',identifier:'LG-INC-001',sourceUrl:'https://example.org/official-guideline',publicationDate:'2026-01-15',effectiveDate:'',summary:'Indicazioni operative per raccolta e gestione delle segnalazioni.',relevance:'Guida ufficiale utile al processo amministrativo degli incidenti.',confidence:.82}]};
  if (purpose === 'contribution-enrichment') { const link=input.contribution?.links?.[0]||''; if (/eur-lex\.europa\.eu/i.test(link)) return {items:[{title:'Direttiva (UE) 2022/2555 — NIS2',documentType:'directive',authority:'Unione europea',jurisdiction:'Unione europea',identifier:'CELEX:32022L2555',sourceUrl:link,publicationDate:'2022-12-27',effectiveDate:'2023-01-16',summary:'Fonte pubblica UE contribuita dall’utente e proposta per verifica.',relevance:'Candidato RN-01 derivato da una fonte pubblica UE; la rilevanza organizzativa resta da verificare.',confidence:.82}]}; return {items:[{title:'Fonte caricata dall’utente',documentType:'other',authority:'Da verificare',jurisdiction:'Da verificare',identifier:'',sourceUrl:link,publicationDate:'',effectiveDate:'',summary:'Materiale contribuito da un utente.',relevance:'Da valutare nel perimetro.',confidence:.55}]}; }
  if (purpose === 'incident-analysis') {const text=String(input.originalNarrative || '').toLowerCase(),signals=[];if(/ancora|continua|in corso/.test(text))signals.push('ongoing');if(/dato personale|cliente|email/.test(text))signals.push('personal-data');if(/phishing|ransomware|attacco|malevol/.test(text))signals.push('malicious');if(/estero|europa|francia|germania/.test(text))signals.push('cross-border');if(/log|siem|alert|rilevat/.test(text))signals.push('technical-detection');return{proposedKind:text.includes('quasi')?'near-miss':'incident',kindConfidence:.78,extractedFacts:[input.originalNarrative],assumptions:signals.includes('malicious')?['La natura intenzionale deve essere confermata.']:[],signals,timeline:[{at:input.awarenessAt,event:'Conoscenza organizzativa dell’episodio',source:'user'}],affectedServices:[],impact:'',mitigations:[],indicators:[],suggestedQuestions:[]};}
  if (purpose === 'incident-draft') return {narrative:`In data ${input.awarenessAt || 'non indicata'} l’organizzazione ha acquisito conoscenza del seguente episodio: ${input.originalNarrative || ''}. Le informazioni sono state consolidate con le risposte disponibili e richiedono verifica umana.`,factsUsed:[input.originalNarrative || ''],unresolvedPoints:(input.unresolvedQuestions || []).map(item=>item.label),limitations:['La formulazione non determina obblighi di notifica.']};
  return { ok:true };
}
const server=http.createServer(async(request,response)=>{const chunks=[];for await(const chunk of request)chunks.push(chunk);let body={};try{body=JSON.parse(Buffer.concat(chunks).toString('utf8'));}catch{}const system=body.messages?.find(item=>item.role==='system')?.content||'',user=body.messages?.find(item=>item.role==='user')?.content||'{}',content=JSON.stringify(responseFor(system,user)),payload=JSON.stringify({id:`mock-${Date.now()}`,choices:[{message:{content}}]});response.writeHead(200,{'content-type':'application/json','content-length':Buffer.byteLength(payload),'x-request-id':`req-${Date.now()}`});response.end(payload);});
server.listen(port,'127.0.0.1',()=>console.log(`mock-ai ${port}`));for(const signal of['SIGINT','SIGTERM'])process.on(signal,()=>server.close(()=>process.exit(0)));
