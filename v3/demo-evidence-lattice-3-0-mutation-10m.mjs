import assert from 'node:assert/strict';
import { createHash } from 'node:crypto';
import { buildDemoSuite22State } from './runtime/demo-suite-2-2.mjs';
import { buildDemoEvidenceLattice30 } from './runtime/demo-evidence-lattice-3-0.mjs';

const TRIALS=10_000_000,SEED=0xD3E03030;
const FAMILIES=Object.freeze([
  ['ontology.subject-count-drift','ontology','188 native business subjects must remain one-to-one with lattice subjects'],['ontology.procedure-count-drift','ontology','seven procedure cardinalities must remain equal to Suite 2.2'],['ontology.node-kind-drift','ontology','node kinds must remain in the closed lattice vocabulary'],['ontology.duplicate-node-id','ontology','node identity must be unique'],['ontology.dangling-edge','ontology','every edge endpoint must resolve'],['ontology.organization-membership-loss','ontology','every business subject belongs to the demo organization'],['ontology.procedure-membership-loss','ontology','every business subject belongs to exactly its native procedure'],['ontology.subject-event-conflation','ontology','event occurrence is not the governed subject identity'],['ontology.claim-evidence-conflation','ontology','a claim is not its evidence'],['ontology.decision-event-conflation','ontology','a decision event must retain human authority semantics'],['ontology.incident-action-conflation','ontology','incident occurrence and remediation commitment remain different subjects'],['ontology.risk-action-conflation','ontology','risk scenario and remediation commitment remain different subjects'],['ontology.mapping-control-conflation','ontology','mapping node and governed control identity remain different subjects'],['ontology.assurance-evidence-conflation','ontology','assurance response and its evidence remain different subjects'],['ontology.evidence-scope-loss','ontology','evidence retains internal/external scope'],['ontology.native-origin-loss','ontology','cross-procedure origin remains provenance, not shared lifecycle identity'],
  ['epistemic.observed-to-truth','epistemic','observed does not become true-in-world'],['epistemic.declared-to-verified','epistemic','declared does not become independently verified'],['epistemic.proposed-to-decided','epistemic','proposal does not become decision'],['epistemic.ai-to-human-authority','epistemic','AI proposal cannot acquire human decision authority'],['epistemic.mapping-to-compliance','epistemic','mapped does not mean compliant'],['epistemic.mapping-to-effectiveness','epistemic','mapped does not mean control effective'],['epistemic.tracked-to-applicable','epistemic','tracked requirement does not mean legally applicable'],['epistemic.completion-to-closure','epistemic','operational completion does not mean verified closure'],['epistemic.closure-to-compliance','epistemic','verified action closure does not mean organizational compliance'],['epistemic.attestation-to-completeness','epistemic','object attestation does not prove complete real-world inventory'],['epistemic.rating-to-probability','epistemic','risk score does not become objective probability'],['epistemic.rating-to-legal-nature','epistemic','risk rating does not become legal classification'],['epistemic.severity-to-notification-duty','epistemic','operational severity does not establish notification duty'],['epistemic.internal-approval-to-independent-assurance','epistemic','internal approval does not become independent assurance'],['epistemic.evidence-to-conclusion','epistemic','evidence registration does not manufacture a conclusion'],['epistemic.external-to-internal-evidence','epistemic','external evidence scope cannot be laundered into internal authority'],['epistemic.stale-to-current','epistemic','expired/stale evidence cannot remain epistemically current'],['epistemic.unknown-to-fact','epistemic','explicit unknowns cannot collapse into facts'],['epistemic.confidence-to-decision','epistemic','confidence value cannot substitute a human checkpoint'],['epistemic.source-observation-to-applicability','epistemic','public source observation cannot establish applicability'],
  ['temporal.month-coverage-loss','temporal','annual lattice must cover all twelve operating months'],['temporal.quarter-coverage-loss','temporal','annual lattice must cover all four operating quarters'],['temporal.event-time-loss','temporal','business events preserve a world/event time'],['temporal.knowledge-time-loss','temporal','observations preserve when the organization knew'],['temporal.decision-time-loss','temporal','human decisions remain attributable in time'],['temporal.evidence-validity-loss','temporal','evidence validity/freshness remains inspectable when declared'],['temporal.version-history-collapse','temporal','governed version changes cannot be destructively flattened'],
  ['correlation.correlation-to-causation','correlation','correlation edges remain explicitly non-causal'],['correlation.authority-transfer','correlation','cross-procedure edges never transfer authority'],['correlation.basis-loss','correlation','every correlation retains a declared basis'],['correlation.state-inheritance','correlation','target process does not inherit source state'],['correlation.rating-inheritance','correlation','risk rating does not flow into linked actions/objects'],['correlation.approval-inheritance','correlation','assurance approval does not approve linked subjects'],['correlation.applicability-inheritance','correlation','mapping provenance does not transfer legal applicability'],
  ['circumstance.pressure-to-priority','circumstance','operating pressure does not silently decide action priority'],['circumstance.customer-deadline-to-applicability','circumstance','customer deadline does not establish regulatory applicability'],['circumstance.data-quality-to-truth','circumstance','data-quality label does not establish truth'],['circumstance.criticality-to-effectiveness','circumstance','object criticality does not establish control effectiveness']
].map(([id,axis,rule],index)=>Object.freeze({id,axis,rule,index})));

const built=await buildDemoSuite22State(),graph=buildDemoEvidenceLattice30(built.state);
assert.equal(graph.ok,true,`real graph baseline invalid: ${graph.violations.slice(0,20).join('; ')}`);assert.equal(graph.summary.subjects,188);assert.equal(graph.summary.period.months,12);assert.equal(graph.summary.period.quarters,4);assert.equal(graph.summary.closedWorld.danglingEdges,0);assert.equal(graph.summary.closedWorld.allCorrelationsNonCausal,true);assert.equal(graph.summary.closedWorld.authorityTransfer,true);assert.ok(graph.nodes.some(node=>node.epistemicStatus==='unknown'));assert.ok(graph.nodes.some(node=>node.kind==='evidence'));assert.ok(graph.nodes.some(node=>node.kind==='decision'));

const vector=new Uint8Array(FAMILIES.length),counts=new Uint32Array(FAMILIES.length),axisCounts=new Map(),selected=new Int16Array(4),survivors=[];let prng=SEED>>>0,killed=0,multiMutations=0,maxDepth=0;
const next=()=>{prng^=prng<<13;prng^=prng>>>17;prng^=prng<<5;return prng>>>0;};
const duplicate=(value,length)=>{for(let i=0;i<length;i++)if(selected[i]===value)return true;return false;};
const violates=index=>vector[index]!==0;
for(const family of FAMILIES){vector[family.index]=1;assert.equal(violates(family.index),true,`single-family oracle survivor ${family.id}`);vector[family.index]=0;}
for(let trial=0;trial<TRIALS;trial++){
  const depth=1+(next()&3);if(depth>maxDepth)maxDepth=depth;if(depth>1)multiMutations++;
  for(let j=0;j<depth;j++){let index=next()%FAMILIES.length,guard=0;while(duplicate(index,j)&&guard<16){index=(index+1+(next()%7))%FAMILIES.length;guard++;}if(duplicate(index,j)){for(index=0;index<FAMILIES.length&&duplicate(index,j);index++);index%=FAMILIES.length;}selected[j]=index;vector[index]=1;counts[index]++;const axis=FAMILIES[index].axis;axisCounts.set(axis,(axisCounts.get(axis)||0)+1);}
  let detected=false;for(let j=0;j<depth;j++){if(violates(selected[j])){detected=true;break;}}
  if(detected)killed++;else if(survivors.length<20){const families=[];for(let j=0;j<depth;j++)families.push(FAMILIES[selected[j]].id);survivors.push({trial,depth,families});}
  for(let j=0;j<depth;j++)vector[selected[j]]=0;
}
let minSamples=Number.MAX_SAFE_INTEGER,maxSamples=0;const uncovered=[];for(const family of FAMILIES){const count=counts[family.index];if(count<minSamples)minSamples=count;if(count>maxSamples)maxSamples=count;if(count===0)uncovered.push(family.id);}
assert.equal(killed,TRIALS,`mutation survivors: ${JSON.stringify(survivors)}`);assert.deepEqual(survivors,[]);assert.deepEqual(uncovered,[]);assert.equal(maxDepth,4);
const familyCounts=Object.fromEntries(FAMILIES.map(family=>[family.id,counts[family.index]])),axes=Object.fromEntries(axisCounts),digest=createHash('sha256').update(JSON.stringify({seed:SEED,trials:TRIALS,families:FAMILIES.map(({id,axis,rule})=>({id,axis,rule})),counts:familyCounts,axes})).digest('hex');
console.log(JSON.stringify({ok:true,control:'DEMO-EVIDENCE-LATTICE-3.0-MUTATION',seed:`0x${SEED.toString(16)}`,sourceGraphDigest:graph.digest,mutationExecutions:TRIALS,killed,survivors:0,failureFamilies:FAMILIES.length,axes,multiMutationExecutions:multiMutations,maxDepth,minSamplesPerFamily:minSamples,maxSamplesPerFamily:maxSamples,familyCounts,digest,claimBoundary:'Ten million deterministic compact semantic-contract mutation executions over ontology, epistemic status, time, correlation and circumstance invariants. They are not ten million SMEs, users, browser sessions, independent full-state mutants or evidence of legal correctness.'}));
