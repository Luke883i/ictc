import assert from 'node:assert/strict';
import { mkdirSync, writeFileSync } from 'node:fs';
import { readFile } from 'node:fs/promises';
import { fileURLToPath } from 'node:url';
import path from 'node:path';
const ROOT=path.dirname(fileURLToPath(import.meta.url)),ART=path.join(ROOT,'..','artifacts');mkdirSync(ART,{recursive:true});const read=p=>readFile(new URL(p,import.meta.url),'utf8');
const [index,copy,frame,router,primitives,proof,css,anatomy,epistemic,controller,contractsRaw,meta,standardRaw,doc,visualRuntime,stableShell,composition,model]=await Promise.all([read('./public/index.html'),read('./public/ui/product-copy.js'),read('./public/ui/procedure-frame.js'),read('./public/ui/surface-router.js'),read('./public/ui/surface-primitives.js'),read('./public/ui/proof-surface.js'),read('./public/onto-compliance-v1.css'),read('./public/ui/procedure-anatomy.js'),read('./public/ui/epistemic-lattice.js'),read('./public/ui/controller.js'),read('./procedure-contracts-1-2.json'),read('./runtime/meta-procedure-contracts.mjs'),read('./standard-proof-1-6-contract.json'),read('../docs/VISUAL_GRACE_LEXICAL_EPISTEMIC_AUDIT.md'),read('./public/ui/visual-epistemic-runtime.js'),read('./public/ui/stable-shell.js'),read('./public/ui/semantic-composition-runtime.js'),read('./public/ui/semantic-composition-model.js')]);
const contracts=JSON.parse(contractsRaw),standard=JSON.parse(standardRaw);
const source=Object.freeze({
 visual:{readablePurpose:css.includes('font-size:.78rem'),readableKicker:css.includes('font-size:.64rem'),minTargets44:css.includes('min-height:44px'),boundedMeasure:css.includes('max-width:68ch'),noHoverLift:css.includes('.procedure-card:hover{')&&css.includes('transform:none'),noFrameBlob:css.includes('.procedure-frame::after{display:none}'),responsiveGrid:css.includes('grid-template-columns:repeat(2,minmax(0,1fr))')&&css.includes('grid-template-columns:1fr'),noOverflowMask:!css.includes('body{overflow-x:hidden}'),proofMethodStyled:css.includes('.proof-method-list')&&css.includes('.proof-method-list li'),neutralObservation:css.includes('.procedure-state.ready')&&css.includes('background:#f1f4f8'),restrainedFrame:css.includes('background:var(--color-surface)')&&css.includes('box-shadow:var(--elevation-rest)')},
 lexical:{shellProcesses:index.includes('>Processi di Compliance</button>'),shellEvidence:index.includes('>Evidenze ICTC</button>'),shellHome:index.includes('>Home</button>'),processHeading:index.includes('<h1>Processi di Compliance</h1>'),canonicalCopyOwner:copy.includes("home:'Home'")&&copy.includes("processes:'Processi di Compliance'")&&copy.includes("proof:'Evidenze ICTC'"),frameSingular:frame.includes('process-code')&&!frame.includes('<span>Processo di Compliance</span>'),framePurpose:frame.includes('procedure-purpose')&&!frame.includes('<b>Scopo del processo</b>'),frameSignals:!frame.includes('semanticSignals')&&frame.includes('PROCEDURE_COMPOSITION')&&stableShell.includes("homeWorkQueue='3.1'"),auditorPlainRead:frame.includes('Consulta registrazioni'),contextCanonical:primitives.includes('retireInjectedNarrative')&&!primitives.includes('ICTC_MANIFEST')&&!primitives.includes('SURFACE_INFORMATION')&&router.includes('SURFACE_LABELS.processes'),backCanonical:router.includes('SURFACE_LABELS.processes'),proofCanonical:proof.includes('Decisioni e tracciabilità')&&proof.includes('non la sufficienza sostanziale'),retiredNavigationAbsent:!index.includes('>Oggi</button>')&&!index.includes('>Postura ICTC</button>'),retiredFrameAbsent:!['<span>Procedura</span>','<b>Scopo della procedura</b>','Segnali della procedura','Consulta record'].some(x=>frame.includes(x))},
 epistemic:{sevenProcesses:contracts.procedures.length===7,codesStable:contracts.procedures.map(x=>x.code).join('|')==='RN-01|EC-01|AO-01|MC-01|AP-01|RC-01|AR-01',epCrossCutting:meta.includes("code:'EP-01'")&&meta.includes('businessProcess:false')&&meta.includes('crossCutting:true'),commonFamilies:anatomy.includes('commonSubstrate?.epistemicFamilies'),claimBoundaryVisible:/\b(?:c|current)\.claimBoundary\b/.test(anatomy),traceAfterWork:anatomy.includes('anchor.after(box)')&&!anatomy.includes('host.prepend(box)'),humanEvidenceEverywhere:contracts.procedures.every(x=>Array.isArray(x.humanCheckpoints)&&x.humanCheckpoints.length&&Array.isArray(x.evidence)&&x.evidence.length&&x.claimBoundary),proposedDistinct:epistemic.includes('Letture proposte')&&epistemic.includes('loadedStateRevision')&&epistemic.includes('requestedRevision')&&epistemic.includes('loadSequence'),projectionCommit:controller.includes('ictc:projection-committed')&&controller.includes('ictcProjectionRevision'),proofMethodVisible:proof.includes('Come leggere le prove ICTC')&&proof.includes('data.proof?.evidenceKinds')&&proof.includes('data.proof?.rule')&&visualRuntime.includes("visualEpistemicRuntime='compat-3.1'"),standardsBounded:standard.benchmarkFamilies.every(x=>x.ictcPractice&&Array.isArray(x.evidence)&&x.evidence.length&&x.limit),noCertificationUpgrade:proof.includes('<b>Limite.</b>')&&!/percentuale di conformit|certificat[oa]\s*ictc/i.test(proof),docBoundary:(doc.includes('bounded')||doc.includes('finite declared scenario space'))&&composition.includes('compactEpistemic')&&model.includes("title:'Relazioni tra decisioni, fonti ed evidenze'")}
});
const DIMENSIONS=Object.freeze({role:['admin','user','auditor'],process:['RN-01','EC-01','AO-01','MC-01','AP-01','RC-01','AR-01'],viewport:['390x844','768x1024','1280x900','1600x1000'],density:['empty','sparse','normal','dense','very-dense'],stage:['entry','work','review','decision','closed-looking'],epistemicStatus:['observed','proposed','reviewed','decided','stale'],navigation:['home','process-hub','history','deep-link'],evidenceDepth:['none','summary','trace','dossier','external-attestation'],aiMode:['off','configured','human-on-proposed'],inputMorphology:['short','long-prose','long-token','mixed-id','unicode'],interaction:['pointer','keyboard','reduced-motion'],crossProcess:['none','linked-source','linked-action','linked-risk'],copyLength:['short','medium','long'],projection:['fresh','stale-response','revision-advance'],assuranceDepth:['snapshot','method','runtime','standards','export']});
const FAULTS=Object.freeze({visual:{'tiny-purpose':'readablePurpose','tiny-kicker':'readableKicker','small-target':'minTargets44','unbounded-measure':'boundedMeasure','hover-lift':'noHoverLift','frame-blob':'noFrameBlob','desktop-grid-on-mobile':'responsiveGrid','overflow-mask':'noOverflowMask','unstyled-proof-method':'proofMethodStyled','success-observation':'neutralObservation','decorative-frame':'restrainedFrame'},lexical:{'nav-processes-short':'shellProcesses','nav-evidence':'shellEvidence','nav-home-legacy':'shellHome','heading-procedures':'processHeading','copy-owner-split':'canonicalCopyOwner','frame-meta-duplication':'frameSingular','purpose-meta-duplication':'framePurpose','old-signals':'frameSignals','auditor-record':'auditorPlainRead','context-title-duplication':'contextCanonical','back-hardcoded':'backCanonical','proof-procedure':'proofCanonical','retired-navigation':'retiredNavigationAbsent','retired-frame':'retiredFrameAbsent'},epistemic:{'eighth-business-process':'sevenProcesses','code-drift':'codesStable','ep-business-leak':'epCrossCutting','families-hidden':'commonFamilies','boundary-hidden':'claimBoundaryVisible','trace-before-work':'traceAfterWork','missing-human-evidence':'humanEvidenceEverywhere','proposal-blur':'proposedDistinct','projection-split':'projectionCommit','proof-method-hidden':'proofMethodVisible','unbounded-standard':'standardsBounded','certification-upgrade':'noCertificationUpgrade','global-proof-claim':'docBoundary'}});
function rng(seed){let x=seed>>>0;return()=>{x^=x<<13;x^=x>>>17;x^=x<<5;return(x>>>0)/4294967296;};}const pick=(r,xs)=>xs[Math.floor(r()*xs.length)];function scenario(r){const out={};for(const[k,v]of Object.entries(DIMENSIONS))out[k]=pick(r,v);return out;}function applicable(family,key,s){if(family==='visual'&&key==='responsiveGrid')return['390x844','768x1024'].includes(s.viewport);if(family==='visual'&&key==='minTargets44')return s.interaction!=='pointer'||s.viewport==='390x844';if(family==='visual'&&key==='proofMethodStyled')return['method','standards'].includes(s.assuranceDepth);if(family==='lexical'&&key==='auditorPlainRead')return s.role==='auditor';if(family==='lexical'&&key==='proofCanonical')return['snapshot','method','standards'].includes(s.assuranceDepth);if(family==='epistemic'&&key==='proposedDistinct')return s.epistemicStatus==='proposed'||s.aiMode==='human-on-proposed';if(family==='epistemic'&&key==='projectionCommit')return s.projection!=='fresh';if(family==='epistemic'&&['proofMethodVisible','standardsBounded','noCertificationUpgrade'].includes(key))return['method','standards'].includes(s.assuranceDepth);return true;}function violations(profile,family,s){return Object.entries(profile[family]).filter(([key,value])=>!value&&applicable(family,key,s)).map(([key])=>`${family}:${key}`);}function inject(profile,family,key){const copy=structuredClone(profile);copy[family][key]=false;return copy;}
for(const[family,profile]of Object.entries(source))for(const[key,value]of Object.entries(profile))assert.equal(value,true,`${family}:${key} source invariant false`);const mutants={};for(const[family,faults]of Object.entries(FAULTS)){mutants[family]={};for(const[fault,key]of Object.entries(faults)){const mutated=inject(source,family,key),r=rng(0x5eed00+fault.length*37+family.length*101);let signature=null;for(let i=0;i<2000&&!signature;i++){const v=violations(mutated,family,scenario(r));if(v.length)signature=v[0];}assert.ok(signature,`mutant escaped detector: ${family}:${fault}`);mutants[family][fault]={killed:true,signature};}}
const saturation={};for(const[family,faults]of Object.entries(FAULTS)){const idx=Object.keys(saturation).length,r=rng(0xa71100+idx*0x1001),seen=new Set(),coverage=Object.fromEntries(Object.keys(DIMENSIONS).map(k=>[k,new Set()]));let M=0;for(let i=1;i<=60000;i++){const s=scenario(r);for(const[k,v]of Object.entries(s))coverage[k].add(v);const keys=Object.values(faults),profile=r()<.72?inject(source,family,pick(r,keys)):source;for(const sig of violations(profile,family,s))if(!seen.has(sig)){seen.add(sig);M=i;}}for(const[k,values]of Object.entries(DIMENSIONS))assert.equal(coverage[k].size,values.length,`${family} discovery missed ${k}`);assert.equal(seen.size,Object.keys(source[family]).length,`${family} discovery missed normalized classes`);const hold=rng(0xb82200+idx*0x1103);let newSignatures=0,targetViolations=0;for(let i=0;i<10000;i++){const s=scenario(hold);targetViolations+=violations(source,family,s).length;const keys=Object.values(faults),profile=hold()<.72?inject(source,family,pick(hold,keys)):source;for(const sig of violations(profile,family,s))if(!seen.has(sig)){seen.add(sig);newSignatures++;}}assert.equal(targetViolations,0,`${family} M+10000 target violation`);assert.equal(newSignatures,0,`${family} M+10000 novel signature`);saturation[family]={discovery:60000,M,normalizedSignatureCount:seen.size,holdout:10000,newSignatures,targetViolations};}
const out={ok:true,profile:'visual-grace-lexical-epistemic-m-plus-10000',source,authorityMigration:{lexical:'semantic-composition-3.1+structural-primitives',attention:'stable-shell-work-queue',visualRuntime:'compatibility-only'},mutants,saturation,totalRandomizedScenarios:Object.values(saturation).reduce((n,x)=>n+x.discovery+x.holdout,0),claimBoundary:'Bounded E2 engineering evidence over declared visual, lexical and epistemic observables; not human usability research, legal compliance, certification or proof of absence of unknown defects.'};writeFileSync(path.join(ART,'visual-grace-lexical-epistemic-saturation.json'),JSON.stringify(out,null,2));console.log(`visual-grace-lexical-epistemic-saturation: ok families=${Object.keys(saturation).length} randomized=${out.totalRandomizedScenarios} holdout=10000/family`);
