import assert from 'node:assert/strict';
import { recordSourceImpact } from './runtime/process-handoffs.mjs';
const actor={id:'admin',displayName:'Admin',role:'admin',identityMode:'local'},req={id:'req-1',type:'requirement',status:'active',versionSha256:'req-v1'},app={id:'app-1',type:'application',status:'active',versionSha256:'app-v1'},state={grcObjects:[req,app],users:[]},source={id:'src-1',title:'Regolamento aggiornato',state:'verified',observations:[{sha256:'obs-v2'}],impactAssessments:[]};
const assessment=recordSourceImpact(source,{outcome:'relevant',reason:'Impatti su obbligo e applicazione',affectedObjectIds:['app-1'],requirementRefs:['req-1','https://eur-lex.europa.eu/example']},actor,state);assert.equal(assessment.affectedObjectBindings[0].versionSha256,'app-v1');assert.equal(assessment.requirementBindings[0].scope,'internal');assert.equal(assessment.requirementBindings[0].objectType,'requirement');assert.equal(assessment.requirementBindings[1].scope,'external');assert.equal(assessment.byRef.id,'admin');assert.throws(()=>recordSourceImpact({...source,impactAssessments:[]},{outcome:'relevant',reason:'bad',affectedObjectIds:['missing']},actor,state),e=>e.code==='reference-unresolved');assert.throws(()=>recordSourceImpact({...source,impactAssessments:[]},{outcome:'relevant',reason:'ambiguous',requirementRefs:['R-1']},actor,state),e=>e.code==='requirement-ref-ambiguous');assert.throws(()=>recordSourceImpact({...source,impactAssessments:[]},{outcome:'relevant',reason:'wrong type',requirementRefs:['app-1']},actor,state),e=>e.code==='reference-type-invalid');console.log('rn-requirement-identity-check: ok (source impact binds active objects and canonical requirement identities)');
