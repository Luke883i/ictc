import assert from 'node:assert/strict';
import { readFile } from 'node:fs/promises';
import { EPISTEMIC_META_PROCEDURE } from './runtime/meta-procedure-contracts.mjs';
const identity=JSON.parse(await readFile(new URL('./release-identity.json',import.meta.url),'utf8'));
const read=p=>readFile(new URL(p,import.meta.url),'utf8');
const [readme,copy,shell,tools,browser21,ontoDoc,startHere]=await Promise.all([read('../README.md'),read('./public/ui/product-copy.js'),read('./public/ui/stable-shell.js'),read('./public/ui/global-tools.js'),read('./browser-v2-1-procedure-journey.py'),read('../docs/ONTO_COMPLIANCE_HORIZON_V1.md'),read('../docs/START_HERE.md')]);
assert.equal(identity.product,'ICTC');assert.equal(identity.semanticEdition,'1.2-market-candidate');assert.equal(identity.experienceEdition,'1.9-experience-candidate');assert.equal(identity.refinementProfile,'1.9.1-pre-candidate');assert.equal(identity.epistemicProfile,'2.0-epistemic-lattice-pre-candidate');assert.equal(identity.journeyProfile,'2.1-procedure-journey-semantic-exploration-pre-candidate');assert.equal(identity.ontoComplianceHorizon,'1.0-candidate');
assert.equal(identity.navigation.proof,'Postura Standard & Security ICTC');assert.equal(identity.navigation.epistemic,EPISTEMIC_META_PROCEDURE.label);assert.equal(identity.metaProcedures.length,1);assert.equal(identity.metaProcedures[0].businessProcess,false);assert.equal(identity.metaProcedures[0].crossCutting,true);assert.deepEqual(identity.evidenceRepresentations,['pdf','xml','md','zip']);
assert.ok(copy.includes(`proof:'${identity.navigation.proof}'`),'product-copy must own canonical proof label');assert.ok(copy.includes(`proofCompact:'${identity.navigation.proofCompact}'`),'product-copy must own compact proof label');assert.ok(readme.includes(identity.navigation.proof),'README proof label drift');assert.ok(browser21.includes('procedure-journey'),'2.1 browser contract missing');
for(const [name,source] of [['stable-shell',shell],['global-tools',tools]]){assert.ok(source.includes("from './product-copy.js'"),`${name} must import product-copy authority`);assert.ok(source.includes('SURFACE_LABELS.proof'),`${name} must consume canonical proof label`);}
for(const token of [identity.semanticEdition,identity.experienceEdition,identity.refinementProfile,identity.epistemicProfile,identity.journeyProfile,identity.ontoComplianceHorizon,EPISTEMIC_META_PROCEDURE.code,EPISTEMIC_META_PROCEDURE.label])assert.ok(readme.includes(token),`README identity drift: ${token}`);
for(const token of ['Identity','Action','Work','Evidence / Trace','M+100','G+100'])assert.ok(ontoDoc.includes(token),`Onto-Compliance contract drift: ${token}`);
assert.ok(startHere.includes('docs/ONTO_COMPLIANCE_HORIZON_V1.md'),'newcomer route must expose current design/assurance contract');
assert.ok(readme.includes('non diventa un ottavo processo business'));assert.ok(!readme.includes('Oggi / Processi / Prove'),'README keeps stale navigation');
console.log(JSON.stringify({ok:true,authority:'release-identity',identity,consumers:['README','START_HERE','stable-shell','global-tools','browser-v2-1','onto-compliance-contract','meta-procedure-contract']}));
