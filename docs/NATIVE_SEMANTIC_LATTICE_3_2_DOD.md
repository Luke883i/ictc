# Native Semantic Lattice 3.2 — DoD

## Stato corrente

**ICTC Enterprise Compliance Workspace 3.2 — Native Semantic Lattice**.

La 3.2 sposta la gerarchia informativa verso owner locali. `active-experience.js` resta l'unico composition root; `semantic-composition-runtime.js` resta annotation-only. Nessuna business write authority o participant C0.1 viene creato dalla presentation.

## DoD globale

1. Esistono esattamente sette processi business; EP-01 resta cross-cutting.
2. Il global semantic runtime contiene zero adapter locali, business-copy rewrite, local DOM reorder e bootstrap di owner locali.
3. Il composition root conserva Foundation 3.0 -> local native owners 3.2 -> global semantic annotation; C0.1 conserva harmonization -> presentation -> integrity -> journey -> annotation.
4. catalogueSummary != workspacePurpose per 7/7 procedure.
5. Processi usa capability matrix 3 -> 2 -> 1; record omogenei usano list/row grammar.
6. Home presenta la proposition ICTC e una bounded attention queue, max 5, ordinata per severity prima del volume.
7. Evidenze è epistemic-first: Reticolo epistemico, ricostruzione, quindi decisioni/tracciabilità; disclosure chiusi di default.
8. EP-01 rende ricerca/relazioni primarie; dettagli tecnici sono progressivi.
9. Admin controlla disponibilità operativa, non applicabilità legale, e conferma soltanto su diff.
10. Stable header/footer sono posseduti dal Workspace Chrome 3.3.
11. Home presentation è posseduta dal visual layer nativo `enterprise-workspace-3-2.css`.
12. Processi/Evidence presentation è posseduta dalla closure locale 3.2.1.
13. Non esiste un final cascade resolver globale corrente; il resolver 3.4 è ritirato.
14. Le superfici coperte restano bounded su mobile e non acquisiscono authority dalla sola presentation.
15. La slice resta verificata da gate statici/UI, model-vocabulary saturation, C0.1 stress e browser journey; exact-head CI resta acceptance distinta.

## Reticolo di authority

- Foundation 3.0 -> local native owners 3.2/3.2.1 -> global semantic annotation -> C0.1 final convergence.
- `active-experience.js` possiede il bootstrap; `native-workspace-3-2.js` aggrega installer e i soli stylesheet correnti 3.2, 3.2.1 e 3.3.
- `enterprise-workspace-3-2.css` può possedere Home e visuali native bounded, ma non stable chrome o `#procedureHub`.
- `semantic-workspace-closure-3-2-1.css` possiede Processi/Evidence presentation senza diventare un global finalizer.
- `workspace-chrome-3-3.css` possiede stable header/footer.
- Business Surface 2.7 e altri bridge storici restano compatibilità, non recuperano copy/presentation authority corrente.

## Metriche correnti

- Local adapters nel global semantic runtime: 0.
- Business-copy rewrites nel global semantic runtime: 0.
- Local DOM reorders nel global semantic runtime: 0.
- Procedure coverage: 7/7.
- Active global final presentation resolver: 0.
- Historical stable-chrome causes in enterprise 3.2: 0.
- Historical `#procedureHub` causes in enterprise 3.2: 0.

Le campagne deterministiche restano bounded repository/model evidence. Non equivalgono a browser session su scala, code mutations indipendenti, studio utenti, certificazione o assurance del deployment. I gate esterni non possono essere auto-chiusi dal repository.
